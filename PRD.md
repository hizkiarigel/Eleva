# Eleva — PRD untuk Claude Code

Dokumen ini ditulis supaya bisa langsung dieksekusi tanpa butuh percakapan sebelumnya sebagai konteks. Baca seluruh dokumen ini dulu sebelum mengubah kode apa pun.

## 1. Apa itu Eleva, dan kenapa scope-nya berubah

AI Character Growth System — AI berperan sebagai mentor yang memberi satu quest personal per hari berdasarkan konteks hidup pengguna, lalu menilai refleksinya.

**Perubahan penting dari versi PRD sebelumnya:** produk ini sebelumnya single-user (cuma founder). Rencana awal sempat "100 member gratis, 6 bulan" — ini DIREVISI setelah pertimbangan biaya dan risiko: **skala awal sekarang 10 orang, pilot 14 hari, closed/terundang**, bukan 100 orang langsung. Kalau pilot 10 orang ini berhasil, buka ke skala lebih besar adalah keputusan terpisah nanti — jangan asumsikan otomatis lanjut ke 100. Monetisasi belum jadi scope sesi ini (model bisnisnya SUDAH diputuskan tapi BELUM dibangun — lihat bagian 12). Tapi begitu ada member selain founder — walau cuma 10 — ini bukan lagi proyek personal: ada orang asing yang akan cerita hal personal (patah hati, ketakutan, dsb) ke AI ini. Tanggung jawabnya beda kelas dari sekadar "aplikasi dipakai sendiri", terlepas dari jumlah orangnya sedikit.

**Model AI: Claude Sonnet, bukan Haiku.** Sempat dipertimbangkan Haiku untuk hemat biaya, tapi setelah dihitung: biaya Sonnet untuk 10 orang selama 14 hari (termasuk onboarding adaptif yang panjang) hanya sekitar Rp70.000-90.000/orang untuk sebulan penuh — jauh di bawah budget yang disiapkan founder (~Rp50.000/orang/bulan, dan ini kalkulasi 10 orang 14 hari jauh lebih kecil dari itu). Di skala ini, penghematan ke Haiku (~Rp30.000/orang/bulan) tidak sepadan dengan risiko kualitas — 10 orang pertama ini adalah validasi PALING PENTING soal apakah "mentor voice" Eleva kerasa hidup. Jangan turunkan ke Haiku tanpa instruksi eksplisit founder.

**Selaraskan Task 5 (Pathway First Trial 14 hari) dengan pilot ini:** First Trial per-pengguna itu SAMA dengan durasi pilot 14 hari — bukan dua jangka waktu terpisah yang kebetulan sama angka. 10 orang pilot ini otomatis juga jadi cohort pertama yang menjalani First Trial.

First principles yang mengikat semua keputusan teknis (versi lengkap ada di `ELEVA_Constitution_Product_Bible_v1.3.docx` kalau file itu ikut di-attach):

- Quest harus personal, bukan checklist generik.
- **Growth butuh substansi nyata, bukan aktivitas kosong** (Goodhart's Law). Non-negotiable.
- Chapter naik karena pergeseran pola nyata, bukan waktu/EXP menumpuk.
- AI adalah mentor, bukan terapis — wajib punya jalur aman ke bantuan profesional kalau ada tanda krisis.
- Fitur baru harus jelas menjawab "apakah ini bantu transformasi pengguna", bukan sekadar "keren untuk ditambahkan".

## 2. Status repo saat ini

Stack: Node.js + Express, vanilla JS frontend tanpa build step. **Masih single-user** — ini yang jadi fokus utama PRD ini untuk diubah.

```
server/index.js   → routes: GET /api/state, POST /api/profile, POST /api/reflection, POST /api/reset
server/db.js      → SQLite (eleva.db, singleton row) — akan dimigrasi ke Postgres di Task 1
server/claude.js  → panggilan Claude API + fallback kalau key kosong/gagal
public/           → index.html, styles.css, app.js (vanilla JS, render manual ke #root)
```

Yang sudah bekerja dan JANGAN diubah logikanya (boleh dipindah lokasinya kalau perlu refactor untuk multi-user):
- Growth-gate: refleksi < 12 kata → `statDeltas` dipaksa kosong di server, walau AI menyarankan nilai lain.
- Chapter hanya naik tiap kelipatan 5 sesi growth valid.
- Kalau `ANTHROPIC_API_KEY` kosong, app tetap jalan pakai fallback quest generik, growth selalu 0.
- Desain saat ini sudah membatasi API call ke maksimal ~2x/user/hari (1 quest generation + 1 reflection). Ini penting dipertahankan saat multi-user — JANGAN buat pola yang bisa memicu regenerasi quest berulang kali per hari per user, supaya biaya API tetap terkendali di 100 user.

## 3. Urutan wajib — jangan lompat urutan

1. Task 0: Push ke GitHub (kalau belum)
2. Task 1: Migrasi ke Postgres
3. Task 2: Auth + multi-tenant data model + beta gate
4. Task 3: Crisis safety hardening
5. Task 4: Privacy notice minimal
6. Task 5: Pathway di onboarding — **QUEUED, mulai HANYA setelah Task 2 & 3 terverifikasi oleh founder** (bukan cuma "kode sudah ditulis") — SELESAI, lihat bagian 9
7. Task 6: Prompt caching — bisa dikerjakan kapan saja setelah Task 5 selesai, tidak diblokir gate lain — SELESAI, lihat bagian 10

**Gate keras: JANGAN buka pendaftaran ke member sungguhan sampai Task 2 dan Task 3 selesai DAN terverifikasi** — bukan cuma "sudah ditulis kodenya", tapi dites nyata: bikin 2 akun berbeda, pastikan data mereka benar-benar terpisah (user A tidak bisa lihat quest/refleksi/stats user B lewat cara apa pun, termasuk lewat manipulasi request langsung ke API).

**Gate kedua, khusus Task 5: JANGAN mulai Task 5 sebelum founder eksplisit bilang Task 2 & 3 sudah diverifikasi.** Kalau status itu belum jelas dari percakapan sejauh ini, tanyakan ke founder dulu sebelum menyentuh kode Task 5 — jangan asumsikan boleh jalan cuma karena PRD-nya sudah ada.

## 4. Task 0 — Push ke GitHub

Repo tujuan: `https://github.com/hizkiarigel/Eleva.git`

```bash
git branch -M main
git remote add origin https://github.com/hizkiarigel/Eleva.git
git push -u origin main
```

Kalau remote sudah ada, cek `git remote -v` dulu. Kalau push ditolak karena histori tidak nyambung, JANGAN `--force` — laporkan ke pengguna dulu.

## 5. Task 1 — Migrasi ke Postgres

Filesystem Railway ephemeral tanpa volume ter-attach — `eleva.db` berisiko hilang tiap deploy/restart. Sudah pernah ada beberapa hari data pengguna yang sempat berisiko karena ini.

**Langkah 0 — WAJIB sebelum kode disentuh:** Railway Shell di service Eleva, `sqlite3 eleva.db ".dump" > /tmp/backup.sql`, pastikan tersimpan aman di luar Railway sebelum lanjut.

**Langkah 1:** Railway dashboard → New → Database → PostgreSQL, di project yang sama. Railway otomatis sediakan `DATABASE_URL` (linked variable), termasuk untuk lokal.

**Langkah 2:** Ganti `better-sqlite3` → `pg`. Schema baru harus langsung dirancang multi-tenant (lihat Task 2 di bawah — jangan bikin schema single-user dulu terus diubah lagi, sekalian sekarang).

**Langkah 3:** Migrasi data lama dari backup dump — data itu jadi milik akun founder (user pertama) setelah Task 2 (auth) selesai dibuat.

**Langkah 4:** `git rm --cached eleva.db`, tambahkan `eleva.db*` ke `.gitignore`, update `README.md` dan `.env.example` (`DATABASE_URL=`).

## 6. Task 2 — Auth + multi-tenant data model + beta gate

Ini perubahan struktural terbesar. Prinsip: sederhana dulu (sesuai semangat "essential" produk ini), jangan over-engineer.

**Auth:** email + password (bcrypt untuk hash), session via cookie (httpOnly, signed) — bukan magic link dulu (butuh setup email transaksional, tambahan kompleksitas yang belum perlu untuk beta). Nggak perlu flow "lupa password" di v1 beta; cukup catatan di README bahwa itu belum ada.

**Beta gate:** target awal 10 orang terundang, pilot 14 hari (bukan buka bebas), tambahkan satu field `BETA_CODE` di env var — kode yang sama dibagikan ke 10 calon pilot tester (lewat mana pun founder mau share-nya). Signup wajib isi kode ini, dicocokkan ke env var. Sederhana, cukup untuk skala ini, dan langsung memberi founder kontrol atas kecepatan pertumbuhan (dan biaya API yang mengikutinya). Kalau pilot 10 orang ini berhasil, buka gate ke skala lebih besar adalah keputusan terpisah nanti — jangan asumsikan otomatis lanjut ke 100.

**Schema Postgres (gantikan rancangan single-user sebelumnya):**
- `users` (id, email unique, password_hash, created_at)
- `character_state` (user_id FK unique → users.id, profile jsonb, stats jsonb, chapter_number, chapter_title, growth_sessions)
- `days` (user_id FK → users.id, date, quest jsonb, insight text, reflection jsonb, PRIMARY KEY (user_id, date))

**Routes:** semua route yang sekarang ada (`/api/state`, `/api/profile`, `/api/reflection`, `/api/reset`) harus di belakang middleware auth, dan semua query ke db di-scope pakai `req.userId` dari session — bukan dari body/param yang bisa dipalsukan client. Tambahkan `/api/signup`, `/api/login`, `/api/logout`.

**Frontend:** tambahkan layar login/signup sederhana sebelum onboarding (satu field email, satu field password, satu field kode beta untuk signup). Jangan redesign besar-besaran — konsisten dengan gaya visual yang sudah ada di `public/styles.css`.

**Verifikasi wajib sebelum dianggap selesai:** buat 2 akun uji, isi onboarding beda-beda di masing-masing, konfirmasi quest/stats/history sama sekali tidak tercampur, dan konfirmasi user A tidak bisa akses data user B walau coba modifikasi request API secara langsung (bukan cuma lewat UI).

## 7. Task 3 — Crisis safety hardening (WAJIB sebelum ada beta member pertama)

Saat ini deteksi krisis cuma instruksi di system prompt (`server/claude.js`) — lemah karena bergantung penuh ke kepatuhan model. Tambahkan lapisan kedua di server, independen dari AI.

Di route reflection, sebelum memanggil `ai.processReflection`:
- Cek `text` terhadap daftar kecil frasa risiko tinggi (niat menyakiti diri sendiri/bunuh diri — frasa spesifik, bukan kata umum seperti "sedih" atau "capek", untuk menghindari false positive).
- Kalau terdeteksi: jangan panggil AI mentor seperti biasa. Refleksi tetap tersimpan, tapi `mentorReply` diganti pesan tetap yang mengarahkan ke bantuan profesional, tanpa `statDeltas`.
- Kontak resmi: **Layanan Sejiwa/Healing119, telepon 119 ekstensi 8, atau www.healing119.id — gratis, 24 jam, Kemenkes RI.**
- Simpan daftar frasa di `server/safety.js` terpisah, supaya gampang direvisi.
- Ini tambahan (defense in depth), bukan pengganti instruksi system prompt.

## 8. Task 4 — Privacy notice minimal

Beta member akan cerita hal personal ke AI. Sebelum signup selesai, tampilkan (boleh sesederhana satu paragraf + checkbox "saya mengerti"):
- Data refleksi diproses oleh AI (Claude/Anthropic) untuk menghasilkan quest & analisis.
- Data disimpan di database (Postgres, Railway), diakses oleh founder untuk keperluan pengembangan produk selama masa beta.
- Bukan pengganti layanan kesehatan mental profesional.

Ini bukan kebijakan privasi hukum yang lengkap — cukup untuk beta terkontrol berbasis kepercayaan. Kebijakan privasi formal jadi task terpisah kalau nanti masuk fase monetisasi.

## 9. Task 5 — Pathway di onboarding: adaptif, direkomendasikan AI (v8, SELESAI — menggantikan desain v3 di bawah)

**Riwayat (kenapa desain ini berubah EMPAT kali — detail lengkap di riwayat versi Bible Bab 15):**

- **v1 (implementasi pertama, di-supersede total):** Pathway dipilih manual dari 6 kartu statis di akhir onboarding. Draft PRD alternatif yang mengusulkan "identity is discovered, not chosen" (AI hypothesis + resonance scoring + auto-activate diam-diam setelah 14 hari) DITOLAK sadar saat itu — dianggap menghapus agency pengguna, bertentangan dengan FP1 dan Bible v1.3 Bab 13.
- **v2 (di-supersede total — sumber: `Eleva_PRD.pdf` + `ELEVA_Constitution_Product_Bible_v1.4.docx` Bab 13):** Bible v1.4 mengklarifikasi bahwa contoh yang memicu penolakan di v1 ("jiwa analis melatih diri jadi sales") sebenarnya soal kebutuhan Acting Method, bukan soal mekanisme pemilihan Pathway itu sendiri. Begitu dipisah, tidak ada kontradiksi dengan FP1 — Pathway direkomendasikan AI dari hipotesis, jalur override manual tetap wajib ada, Pathway baru berstatus "trial" sampai terbukti resonan. v2 menambahkan: Situasi/Values/Fear (dipertahankan sebagai step statis terpisah), Stats sebagai polygon 0-100 (default 50), Growth Focus (multi-select 1-3 dari 10 kategori tetap), dan 3 Adaptive Question TETAP (jumlah fixed).
- **v3 (di-supersede oleh v4 di satu titik — revisi kedua dari draft Task 5, ditulis SETELAH founder review langsung terhadap implementasi v2 yang sempat berjalan di production):** Growth Focus (multi-select kategori) DIGANTI TOTAL oleh radar/polygon self-assessment 8-sumbu yang SAMA dipakai buat Stats — bentuk radar chart itu sendiri (sumbu mana yang menonjol/ditekan) yang jadi sinyal minat/fokus, jadi tidak ada dua mekanisme redundan buat hal yang sama. Situasi/Values/Fear (yang di v2 eksplisit dipertahankan atas instruksi founder) DIHAPUS TOTAL — isinya digali lewat percakapan adaptif dinamis, bukan pertanyaan statis lagi. Jumlah pertanyaan adaptif jadi FLEKSIBEL 4-10 (bukan fixed 3), format free-text (user mengetik jawaban). Filosofi inti (Pathway direkomendasikan AI + override manual + status trial 14 hari) TIDAK berubah dari v2.
- **v4 (MENGGANTIKAN v3 di format pengumpulan konteksnya saja — sisanya identik):** free-text Adaptive Questions diganti **Adaptive Statement Cards** — AI generate SATU PERNYATAAN orang-pertama per kartu, pengguna cukup **thumbs up ("ini aku") / thumbs down ("bukan aku")**, tidak mengetik apa pun. Terinspirasi tes Ikigai TAPI dengan beda krusial yang PRD tekankan eksplisit: pernyataan WAJIB di-generate AI per pengguna (dari radar chart + histori swipe orang itu), BUKAN bank pernyataan statis yang sama untuk semua orang — kalau statis, ini jadi "tes kepribadian" generik yang sudah ditolak sebelumnya. Format kartu+swipe SUDAH dikonfirmasi eksplisit oleh founder; range 4-10-nya masih keputusan default dari rekomendasi Claude (belum dikonfirmasi vs fixed 10, ditandai komentar di kode supaya gampang diubah). Jumlah panggilan API SAMA seperti v3 — cuma format input pengguna yang jadi lebih cepat.
- **v4.1 (spesifikasi tambahan radar dari review founder terhadap implementasi live, sisanya identik dengan v4):** (1) angka nilai (1-10) tampil di tiap titik radar, bukan cuma posisi visual; (2) **fitur kunci maksimal 3 titik** — tap titik untuk mengunci, titik terkunci TIDAK ikut redistribusi zero-sum saat titik lain digeser (batas 3 disengaja: memaksa prioritas nyata, "orang nggak bisa mau semuanya" — mau kunci titik ke-4 harus buka salah satu dulu); (3) kombinasi ekstrem (mis. Body=10 DAN Social=10 sekaligus) jadi MUNGKIN kalau dua-duanya dikunci — total tetap 40, sisa 6 titik yang belum dikunci berbagi 20 dan saling redistribusi normal di antara mereka sendiri; (4) DILARANG ada rumus/lookup tetap di kode soal "makna" kombinasi tertentu — info titik terkunci masuk sebagai KONTEKS ke prompt statement card (AI boleh arahkan kartu berikutnya menggali spesifik kombinasi ekstrem kalau pola swipe belum menjelaskan) dan prompt Chapter Analysis (wajib mengaitkan interpretasi kombinasi ke respons kartu aktual, bukan generalisasi karangan sendiri).
- **v4.2 (SESI INI — algoritma redistribusi v2 berbasis evidence sinergi, menggantikan mekanisme redistribusi sebelumnya; UI/lock/kartu tidak berubah):** riwayat resmi dari PRD — v0 (yang sempat production) berperilaku "equal-split" (semua sumbu unlocked cenderung dipaksa rata; ditandai founder sebagai bug), v1 = proporsional murni ke nilai saat ini, v2 (final) = pakai bukti riset untuk 15 dari 28 pasangan sumbu yang punya evidence (`SYNERGY` matrix di `public/app.js`: weight kuat=3/sedang=2/lemah=1, direction searah/berlawanan), proporsional murni untuk sisanya. Saat sumbu X digeser sebesar delta: partner ber-evidence ikut tertarik SETARA delta itu sendiri dibagi menurut bobot (BUKAN dijumlah independen per partner — mode gagal yang didokumentasikan PRD sendiri: satu gesekan kecil bisa minta belasan poin dari sumbu sisa), non-partner menyerap sisa beban proporsional ke nilai mereka saat ini; kalau ada titik yang bakal keluar [1,10], SELURUH delta di-skala-turun seragam (constraint-solving real-time, bukan clamp satu titik); pembulatan largest-remainder ke semua 8 titik sekaligus di akhir supaya total tetap presisi 40; titik terkunci tak tersentuh di langkah mana pun. Konsekuensi DISENGAJA yang diverifikasi persis cocok dengan hitungan manual PRD: **Career sendirian mentok di ≈7.8 (dibulatkan 8) dan TIDAK PERNAH bisa 10** (Career punya evidence ke semua sumbu lain kecuali Finance, peredam kejut satu-satunya) — kalau QA menemukan Career bisa 10, itu tanda algoritmanya salah; Body sendirian bisa ≈9.9. Saat mentok ada feedback: getar halus (kalau device support) + teks singkat di bawah chart ("X udah di titik paling jauh yang bisa dicapai bareng kombinasi sekarang"), titik berhenti mengikuti jari di nilai maksimal yang valid — bukan macet diam-diam. Catatan: PRD merujuk `reference/Eleva_Correlation_Matrix.html` untuk sitasi lengkap tiap pasangan — file itu BELUM disertakan/ter-attach, jadi belum bisa dimasukkan ke repo; bobot+arah di kode diambil persis dari matriks di PRD, tanpa mengarang sitasi.
- **v4.3 (SESI INI — bug fix constraint-solving dari testing manual founder, UI/lock/kartu/algoritma sinergi tidak berubah selain ini):** bug ditemukan founder saat testing: reproduksi `{body:10(locked), mind:6, career:4, finance:6, emotional:6, purpose:6, explorer:1, social:1}` (total 40), geser Explorer +1 saja DITOLAK TOTAL ("sudah di titik terjauh") padahal Explorer punya ruang gerak nyata. Akar masalah: constraint-solving v4.2 memakai SATU faktor skala global — begitu satu non-partner (Social, sudah di floor 1 dari drag sebelumnya) kedeteksi butuh ruang yang sudah 0, faktor skala itu jatuh ke 0 untuk SELURUH gesekan, bukan cuma sumbu yang mentok itu. Fix: tambah **pass saturasi berulang** sebelum constraint-solving lama — sumbu (partner ATAU non-partner) yang SUDAH persis di batas (ruang gerak 0, independen dari besar delta) dikeluarkan dari pool (dipatok, kontribusinya ke `totalBurden` dihapus kalau partner), sisanya dihitung ulang, diulang sampai stabil (maks `others.length` iterasi, dijamin berhenti). Constraint-solving uniform yang lama (skalakan turun SELURUH delta) TETAP dipakai persis seperti sebelumnya untuk sumbu yang match SEBAGIAN ruang gerak (bukan yang sudah 0 dari awal) — jadi Career tetap mentok ≈7.8, Body tetap ≈9.9, tidak ada regresi. Diverifikasi: reproduksi persis founder di atas → Explorer sekarang bisa naik (dites lewat pointer event asli di browser, bukan cuma fungsi murni). Catatan jujur: estimasi founder di laporan bug ("Explorer harusnya bisa sampai ≈8.5") cuma menghitung non-partner (Finance+Emotional+Purpose) sebagai batas — tapi Mind (partner terkuat Explorer, bobot 3 dari total 4) mentok DULUAN di ceiling-nya sendiri (base 6, ruang 4) pada delta≈5.33, sebelum non-partner manapun mentok floor di delta 7.5 — jadi angka yang benar (dan diverifikasi lewat constraint-solving uniform yang sudah ada, sesuai spek "skalakan turun SELURUH delta ... sampai seluruh sistem valid" di v4.2) adalah Explorer mentok ≈6.3 (dibulatkan 6), bukan ≈8.5 — tetap jauh lebih baik dari macet di 1, dan sepenuhnya taat ke mekanisme constraint-solving yang sudah didokumentasikan, bukan penyimpangan baru.
- **v5 (SESI INI — restrukturisasi 7 elemen MECE per Bible v1.5 + algoritma re-normalisasi satu pool per revisi PRD terbaru; kartu/analysis/First Trial tidak berubah):** radar 8 sumbu → **7 sumbu MECE**: Body, **Growth** (gabungan Mind+Explorer — Openness & Intellect satu faktor Big Five), **Livelihood** (gabungan Career+Finance — salary komponen inti definisi career success), Emotional Stability, Social, Purpose, **Autonomy** (BARU — rasa kendali atas keputusan & arah hidup, Self-Determination Theory). Default 5, total **SELALU 35** (7×5). `SYNERGY` dibangun ulang: **14 dari 21 pasangan ber-evidence terarah** (semuanya positif — dua pasangan negatif lama terserap jadi "campuran" saat merge; mekanik `direction:-1` tetap tersedia di kode), 3 campuran (evidence komponen bertabrakan: body|livelihood, emotional|livelihood, body|social — non-partner, menyerap netral), 4 belum ketemu (growth|emotional, growth|social, growth|purpose, social|autonomy); sitasi lengkap per pasangan sekarang benar-benar ada di `reference/Eleva_Correlation_Matrix.html` (file akhirnya disertakan founder, ikut di-commit ke repo bersama `reference/ELEVA_Constitution_Product_Bible_v1.5.docx`). **Algoritma redistribusi diganti re-normalisasi SATU pool** (formula 5-langkah dari PRD, dipilih karena riwayat git founder eksplisit: era 8-elemen sempat menemukan Career punya NOL non-partner begitu Career|Finance ber-evidence — formula dua-tahap lama membeku total di topologi itu, makanya "normalized redistribution formula guaranteeing exact zero-sum regardless of partner count"): saat sumbu X digeser delta, SEMUA sumbu lain yang belum terkunci jadi satu pool dan totalnya berubah PERSIS −delta, dibagi sebanding kekuatan tarik masing-masing — partner: `weight × direction`; non-partner (termasuk campuran): nilai saat ini — tanpa percabangan topologi. Konsekuensi yang berubah dari era v4.2 dan DISENGAJA per PRD baru: tidak ada lagi "Career tidak pernah bisa 10" — batas maksimal tiap sumbu sekarang murni dari zero-sum budget 35 (mis. Autonomy — 5 partner, cuma Social non-partner murni — bisa mencapai 10 dengan mulus, Social tidak lagi dibebani 2×delta sendirian seperti formula lama). Pass saturasi v4.3 (sumbu yang sudah mentok dikecualikan dari pool, sisanya menyerap ulang) dan constraint-solving skala-seragam + largest-remainder (ke 35) + lock + feedback getar/teks semuanya dipertahankan. **Migrasi data production: TIDAK ADA** (keputusan PRD eksplisit — belum ada beta member asli): akun era 8-sumbu tetap jalan read-only apa adanya (dashboard menampilkan stats lama dengan label legacy, AI tetap membaca snapshot lama), jalur resminya reset (tombol ↺ di dashboard, menghapus `character_state`+`days`) lalu onboarding ulang 7 elemen. Viewbox SVG dilebarkan (332→368) karena heptagon menaruh label "Livelihood" di sudut nyaris horizontal yang kepotong di padding lama.

- **v6 (revisi sebelumnya — Adaptive Statement Cards diganti total oleh Adaptive Scenario Cards + kalibrasi radar; radar 7-sumbu MECE dari v5 tidak berubah):** format kartu diganti dari satu-pernyataan/thumbs-up-down (v4/v5, 4-10 kartu, satu sumbu per kartu) jadi satu-skenario+4-opsi (2-6 kartu, empat sumbu sekaligus per kartu — jauh lebih efisien untuk cakupan yang sama). User wajib pilih favorit DAN paling-tidak-disukai tiap kartu (tidak opsional). **Kalibrasi radar (mekanisme baru):** radar hasil drag manual (`radar_raw`) adalah hipotesis awal, diuji ulang lewat reaksi konkret ke skenario — tiap kartu menghasilkan 2 sinyal (favorit=+1, paling-tidak-disukai=−1) diterapkan lewat mesin redistribusi manual yang SAMA (`applySynergyDrag`), dibatasi ±3 net kumulatif per sumbu. Di v6, sumbu terkunci TIDAK PERNAH jadi opsi kartu sama sekali (dikecualikan struktural dari seleksi sumbu) — batasan ini DIREVISI di v7, lihat bullet di bawah. `radar_snapshot` (terkalibrasi) dan `radar_raw` (mentah) mulai disimpan terpisah di Postgres. Pergeseran kalibrasi ≥2 poin net wajib disebutkan eksplisit di Chapter Analysis (`significantShifts`, pra-hitung di kode).
- **v7 (SESI INI — locked axes BOLEH jadi opsi kartu + lock tension, radar before/after visual WAJIB, Pathway jadi 3 kartu yang bisa digeser; kalibrasi/redistribusi/radar 7-sumbu lainnya tidak berubah):** tiga revisi dari founder terhadap v6:
  1. **Seleksi sumbu opsi kartu DIBALIK dari v6: sekarang mengambil dari SEMUA 7 sumbu (locked maupun unlocked), bukan cuma unlocked.** Alasannya eksplisit: sumbu locked tetap perlu "diuji" untuk sinyal narasi, walau angkanya tidak pernah berubah. Konsekuensi: `selectCardAxes`/`coverageComplete` (`server/claude.js`) tidak lagi memfilter axis terkunci — cakupan penuh sekarang berarti SEMUA 7 sumbu (bukan cuma yang unlocked) harus pernah muncul sebagai opsi sebelum sesi boleh berhenti, tetap dalam 2 kartu minimum (⌈7/4⌉=2, tidak berubah dari v6 meski poolnya melebar). **Angka sumbu locked tetap TIDAK PERNAH bergeser** (`applyCalibrationCard` di `public/app.js` skip total langkah numerik kalau axis-nya ada di `onboardForm.locked`, apa pun yang dipilih) — tapi kalau opsi terkait sumbu locked itu dipilih (favorit atau paling-tidak-disukai), itu dicatat sebagai **`lockTension`** (`computeLockTension` di `server/claude.js`, direkonstruksi murni dari `ctx.cards`+`ctx.lockedAxes`+`ctx.radarSnapshot` — tidak perlu state client tambahan). "Kontrer" = pilihan berlawanan dari nilai kunci: sumbu dikunci TINGGI (>5) tapi dipilih paling-tidak-disukai, atau dikunci RENDAH (<5) tapi dipilih favorit; dikunci pas di default (=5) tidak pernah kontrer (tidak ada klaim arah). Sumbu dengan **≥2 pilihan kontrer** (`flaggedTensionAxes`, ambang batas keputusan implementasi — PRD bilang "berulang kali" tanpa angka pasti) memicu Chapter Analysis WAJIB memunculkannya sebagai PERTANYAAN REFLEKTIF (bukan pernyataan final, dan TIDAK pernah menyarankan mengubah angka locked-nya — cuma jalur override manual yang bisa).
  2. **Visual before/after radar WAJIB tampil di Chapter Analysis (baru).** `renderRadarComparisonSVG` (`public/app.js`) menumpuk dua outline di satu chart: radar mentah (`radarRaw`, putus-putus/muted) dan radar terkalibrasi (`radar`, solid/accent), reuse geometri `polyPoint`/`POLY_ORDER` yang sama dengan radar interaktif tapi read-only (tanpa handle/drag) — plus legenda dua warna. Selalu ditampilkan, tidak bersyarat pada ada/tidaknya pergeseran (dua outline identik kalau memang tidak ada kalibrasi berarti, itu sendiri informasi).
  3. **Pathway Recommendation jadi 3 KARTU yang bisa digeser (carousel horizontal, CSS scroll-snap), bukan lagi satu rekomendasi tetap.** Override manual tetap ada sebagai jalur ke-4, tidak dihapus. Tiga kartu: **(1) kalibrasi** — `pathway`/`pathwayNoun`/`pathwayBlurb` dari `generateChapterAnalysis`, sumber "paling tahu" karena lewat proses kalibrasi penuh (field lama, semantik tidak berubah); **(2) radar mentah** — dari `rawPathwayTop2` (BARU: array 2 kandidat pathway+blurb, diminta di prompt YANG SAMA dengan panggilan Chapter Analysis, secara eksplisit diinstruksikan mengabaikan `ctx.cards` dan HANYA melihat bentuk `radarRaw` — satu panggilan API, bukan panggilan terpisah per opsi, sesuai instruksi efisiensi PRD); pakai top-1, kalau top-1 sama dengan Opsi 1 baru fallback ke top-2 (`buildPathwayOptions` di `public/app.js`, dieksekusi CLIENT-SIDE karena murni logika dedup, tidak perlu panggilan API tambahan); **(3) wildcard** — dipilih ACAK (client-side, `Math.random`) dari pathway sisa di luar Opsi 1 & 2 (3 sisa dari total 5 sejak v8 rename, dulu 4 dari 6 sebelum v8 — lihat riwayat v8 di bawah), murni kesempatan, BUKAN hasil analisis AI (blurb generik "Coba lihat arah yang beda"), dihitung SEKALI saat Chapter Analysis pertama kali dimuat (bukan re-roll tiap render, supaya tidak berubah-ubah di bawah user). User tap satu kartu untuk memilihnya (radio-style, highlight), tombol "Mulai First Trial" nonaktif sampai ada yang dipilih, lalu submit pakai pathway+pathwayNoun dari kartu yang DIPILIH (bukan selalu kartu 1) — diverifikasi eksplisit lewat browser test (pilih kartu 2, badge dashboard akhir mencerminkan pathway kartu 2, bukan kartu 1).
- **v8 (SESI INI — Pathway direname+direframe TOTAL per Bible v1.7 Bab 13; radar/kartu/kalibrasi/lockTension/carousel dari v5-v7 tidak berubah selain daftar nama pathway-nya):** dua revisi terpisah, keduanya dari founder lewat Bible: **(1) v1.6 mempersempit Pathway dari 6 ke 5** — Explorer dihapus (wilayahnya sudah tercakup stat Growth pasca-restrukturisasi MECE v5, jadi nama Pathway terpisah jadi redundan), Seeker dipertahankan. **(2) v1.7 mengganti NAMA dan MAKNA keempat sisanya sekaligus**: Builder→Architect, Guardian→Warden, Connector→Weaver, Seeker→Pilgrim (Specialist tidak berubah nama). Bukan cuma rename kosmetik — alasannya prinsip **"Pathway ≠ Chapter"** (Bible Bab 13, ditegakkan eksplisit): Chapter menjawab "lagi di fase/masalah hidup apa" (TEMPORAL, mis. "Lost"→"Healing"), Pathway menjawab "bagaimana gaya orang ini berinteraksi dengan dunia" (TAHAN LAMA, soal gaya perilaku). Deskripsi Pathway di draf v1.2-v1.6 diam-diam mengerjakan tugas Chapter ("belajar stabil emosi", "belum tahu arah") — itu ditulis ulang total jadi murni gaya perilaku: Architect = membangun/menyusun metodis; Warden = menjaga & mempertahankan, fokus konsistensi; Weaver = menghubungkan orang & ide; Pilgrim = menjelajah luas sengaja sebelum berkomitmen (BUKAN "tersesat"); Specialist = menyelam dalam satu domain spesifik (tidak berubah). Aturan ini ditegakkan lewat instruksi eksplisit di `MENTOR_SYSTEM` (`server/claude.js`) dengan contoh benar/salah persis dari Bible, berlaku untuk SEMUA teks AI yang menyebut Pathway (insight harian, `pathwayBlurb`, `pathwayNoun`) — bukan cuma di prompt Chapter Analysis. `RADAR_AXIS_TO_PATHWAY` (fallback heuristik tanpa API key) di-remap: livelihood+purpose→Architect, body+emotional→Warden, growth→Pilgrim (mewarisi slot growth→Explorer lama, definisi Pilgrim "eksploratif" paling dekat), social→Weaver, autonomy→Specialist — pemetaan purpose→Architect adalah keputusan implementasi saya sendiri (PRD cuma menegaskan RENAME axis lain, bukan re-assignment axis yang kehilangan target karena Explorer dihapus), didokumentasikan di komentar kode. **Migrasi data: TIDAK ADA** (konsisten pola v5 — akun lama dengan pathway lama seperti "Builder" tetap tampil apa adanya di badge, bukan error, jalur resminya reset → onboarding ulang).
- **v9 (SESI INI — skala bobot `SYNERGY` diperkuat 2x, formula/struktur redistribusi TIDAK berubah):** founder melapor lewat screenshot produksi bahwa drag ke satu sumbu bikin sumbu lain bergeser "merata" (mis. Body 5→10 menghasilkan enam sumbu lain di rentang sempit 4-5) — kelihatan seperti equal-split lagi, konsep sinergi jadi tidak kebaca. Diverifikasi lewat hitungan manual bahwa ini BUKAN regresi ke equal-split — hasilnya cocok persis dengan formula re-normalisasi satu pool v5 (`pullOf`: partner = `weight × direction`, non-partner = nilai saat ini). Akar masalah sebenarnya: skala bobot lama (lemah=1/sedang=2/kuat=3) terlalu kecil dibanding pull non-partner yang pakai NILAI SAAT INI (~5 di radar default) — akibatnya sumbu TANPA evidence (non-partner) rutin menyerap LEBIH BANYAK pergeseran daripada sumbu partner ber-evidence kuat (weight 3), kebalikan dari yang seharusnya dikomunikasikan radar. Fix (dipilih founder dari 3 opsi yang diajukan: biarkan/perkuat bobot partner/redam pull non-partner): **skala bobot digandakan (lemah 1→2, sedang 2→4, kuat 3→6)**, rasio antar-partner dipertahankan persis (6:4:2 = 3:2:1), pull non-partner (nilai saat ini) TIDAK disentuh — jadi partner ber-evidence kuat sekarang bisa mengungguli non-partner di sekitar nilai default, sementara partner lemah tetap wajar kalah dari non-partner. Formula/struktur (`applySynergyDrag`, saturasi, constraint-solving, largest-remainder) SAMA SEKALI tidak berubah — cuma angka bobot. Catatan jujur: pada delta kecil dari radar default (mis. drag tunggal ±5), diferensiasi TETAP samar karena dibagi ke 6 sumbu dan dibulatkan ke integer — perbaikan paling kelihatan di delta besar/radar yang sudah tidak simetris (skenario DoD v4.2/v5 yang sudah ada, semuanya diverifikasi ulang lolos dengan bobot baru).

- **v10 (SESI INI — pull non-partner diredam 0.5x + variasi ranah skenario; dua-duanya dari testing founder di production):** dua keluhan founder setelah menjalani onboarding penuh sebagai user: **(1) kartu skenario terus-menerus soal proyek/kerjaan** — padahal tujuan pengguna bisa saja nge-gym atau belajar bahasa Inggris; onboarding memang sengaja tidak punya field tujuan free-text (Situasi/Values/Fear dihapus sejak v3), jadi satu-satunya cara kartu menangkap kehidupan non-kerjaan adalah VARIASI RANAH. Fix: prompt `generateScenarioCard` sekarang mewajibkan rotasi ranah kehidupan antar kartu (kesehatan/olahraga, belajar skill/bahasa, relasi & keluarga, waktu luang, uang, kerjaan/studi, momen sendirian), melarang mengasumsikan pengguna sedang mengejar karier/proyek, dan menyertakan daftar skenario yang sudah terpakai supaya tidak mengulang ranah yang sama. Catatan jujur: perubahan ini murni di prompt AI — tidak bisa diverifikasi otomatis tanpa API key (fallback deterministik memang sudah netral-ranah), harus dicek founder di production. **(2) radar before/after nyaris tidak berbeda walau 6 kartu dijawab serius, dan drag tunggal di radar awal terasa kurang bermakna sebarannya.** Akar masalah yang sama dengan v9 tapi lapisan kedua: pull non-partner = NILAI SAAT INI membuat (a) gradien sinergi drag tunggal tetap datar (non-partner nilai 5 setara partner sedang bobot 4), dan (b) sumbu yang di-favorit-kan konsisten justru jadi PENYERAP KOLATERAL TERBESAR sinyal-sinyal berikutnya begitu nilainya tinggi — kalibrasi menggerogoti hasil kalibrasinya sendiri, makanya before/after terlihat mirip. Fix: **`NONPARTNER_DAMP = 0.5`** — pull non-partner jadi 0.5×nilai (tetap proporsional nilai → proteksi floor alami tetap ada, normalisasi total −delta tetap presisi). Hasil terverifikasi: drag tunggal Body 5→10 sekarang menghasilkan gradien terbaca (Emotional/partner-kuat −2 > Purpose+non-partner −1 > Growth/Autonomy/partner-lemah 0 — sebelumnya nyaris seragam), dan simulasi sesi 6 kartu konsisten menghasilkan total pergeseran before/after LEBIH BESAR (18 poin vs 14 pre-v10) dengan sumbu favorit bertahan tinggi dan sumbu yang dihindari turun jelas. Batas kalibrasi ±3 sinyal langsung per sumbu TIDAK diubah (aturan founder di v6) — kalau founder mau swing lebih ekstrem lagi, knob-nya cap itu, keputusan terpisah.
- **v11 (SESI INI — perbesar kekuatan per-pilihan kalibrasi 1→2 + larangan sebut nama sumbu radar harfiah + paksa Bahasa Indonesia; dari feedback founder setelah menjalani onboarding penuh sampai dashboard):** dua keluhan terpisah. **(1) Kalibrasi masih terasa kurang "membuktikan" pilihan pengguna** — founder eksplisit fokus ke Growth/Livelihood/Autonomy tapi radar terkalibrasi tidak terasa cukup menegaskan itu meski sudah menjawab 6 kartu serius. Ditanyakan ke founder 4 opsi (biarkan, perbesar per-pilihan, naikkan cap, sentuh 2 opsi yang diabaikan) — **founder pilih perbesar per-pilihan**: `CALIBRATION_PICK_MAGNITUDE` dari 1 jadi 2 (`public/app.js`, `applyCalibrationCard`) — tiap favorit sekarang ±2, paling-tidak-disukai ∓2, per kartu. **Cap kumulatif ±3 per sumbu TIDAK diubah** (aturan v6 dipertahankan persis) — konsekuensinya cuma 2 pilihan konsisten pada sumbu yang sama sudah cukup mentok ke cap (dulu perlu 3), jadi pola yang jelas kelihatan lebih cepat tanpa memperlebar seberapa jauh satu sumbu bisa menyimpang dari radar drag manual. **(2) Teks AI (secondaryTrait, kemungkinan juga insight/pathwayBlurb) kadang menyebut nama sumbu radar secara harfiah** ("kamu fokus pada Growth, Livelihood, Autonomy") dan sempat kecolongan Bahasa Inggris di satu field (secondaryTrait "Curiosity-driven learner" — bukan Indonesia seperti field lain). Akar masalah: `MENTOR_SYSTEM` tidak pernah eksplisit melarang menyebut nama sumbu secara harfiah (aturan Pathway≠Chapter v8 cuma soal framing Pathway, bukan larangan umum sebut nama sumbu) dan tidak pernah eksplisit mewajibkan Bahasa Indonesia di semua field bebas. Fix: dua bullet baru di `MENTOR_SYSTEM` — (a) larangan eksplisit menyebut nama 7 sumbu radar secara harfiah di teks bebas mana pun, WAJIB diterjemahkan jadi deskripsi kecenderungan perilaku nyata (bukan nama kategori internal); (b) semua field teks bebas WAJIB Bahasa Indonesia natural, kecuali istilah produk yang memang tidak diterjemahkan (nama Pathway). Catatan jujur: dua-duanya perubahan prompt AI murni — tidak bisa diverifikasi otomatis tanpa API key nyata di production, cuma dites string-level (aturan ada di `MENTOR_SYSTEM`) dan browser end-to-end di mode fallback (yang memang sudah netral-bahasa/nama-sumbu by construction, jadi tidak representatif buat kasus AI yang dilaporkan).
- **v12 (SESI INI — sumbu terkunci sekarang BISA terkikis angkanya kalau jawaban kartu berulang kali berlawanan; REVERSAL dari aturan "kebal kalibrasi SELAMANYA" sejak v6):** founder melaporkan lock terasa "gak mau gerak" meski jawaban 6 kartunya berulang kali berlawanan arah dari sumbu yang dikunci — sebelumnya kontradiksi cuma jadi `lockTension` narasi (pertanyaan reflektif di Chapter Analysis), TIDAK PERNAH mengubah angka (aturan v6/v7 eksplisit: "angka sumbu terkunci kebal dari kalibrasi SELAMANYA"). Founder eksplisit mengizinkan pembalikan ini ("saya ijinkan" turun dari 10 ke 9/8/7). Ditanyakan 2 keputusan mekanisme ke founder: **(1) trigger — founder pilih SETIAP kontradiksi** (dari pilihan ke-1, bukan nunggu ambang ≥2 seperti `flaggedTensionAxes` yang tetap dipakai HANYA untuk narasi teks); **(2) besaran & status — founder pilih ±2 per kontradiksi dengan cap ±3 total (persis magnitude+cap kalibrasi biasa v11), sumbu TETAP berstatus "terkunci"** (kebal dari redistribusi sumbu LAIN, tetap hitung ke `MAX_LOCKS`, tetap ikon 🔒) sepanjang proses — cuma sinyal kontradiksi dari kartu yang bisa menggerakkannya.

  **Definisi "kontradiksi" (`isContraryPick`, `public/app.js`) — MIRROR persis dari `computeLockTension` (server, sejak v7)**: sumbu dikunci TINGGI (>5) tapi dipilih paling-tidak-disukai, atau dikunci RENDAH (<5) tapi dipilih favorit. Sumbu dikunci PERSIS di 5 tidak pernah kontrer (tidak ada klaim arah) — tidak pernah terkikis. Pilihan yang MENDUKUNG lock (favorit pada locked-tinggi, atau paling-tidak-disukai pada locked-rendah) TIDAK melakukan apa-apa — tidak ada informasi baru di situ.

  **Krusial — dinilai terhadap `lockedOriginalValue` (nilai SAAT DIKUNCI), BUKAN nilai radar yang live/sudah terkikis.** Kalau pakai nilai live, sumbu yang sudah terkikis lewat titik tengah 5 akan MEMBALIK arah "kontrer"-nya di tengah sesi yang sama — bug yang ditemukan lewat unit test (mis. Body dikunci 10 lalu terkikis ke 4 karena 2 kontradiksi; pilihan favorit KETIGA seharusnya justru MENDUKUNG deklarasi awal "locked tinggi", tapi kalau dinilai dari nilai live 4, malah salah diklasifikasikan sebagai kontrer lagi). Fix: `onboardForm.lockedOriginalValue[axis]` direkam SEKALI di momen tap-untuk-mengunci (`toggleLock`, `public/app.js`), dipakai sepanjang sesi kalibrasi itu untuk menentukan arah, dihapus lagi kalau axis dibuka-kunci (supaya kunci ulang di nilai baru merekam ulang). Field yang sama dikirim ke server (`POST /api/onboarding/chapter-analysis` body tambah `lockedOriginalValue`) supaya `computeLockTension` (narasi Chapter Analysis) juga menilai dari nilai ASLI, bukan `radarSnapshot` final — bug yang sama persis kalau tidak diperbaiki di dua sisi (client DAN server punya rumus "kontrer" masing-masing, harus konsisten).

  **Mekanisme erosi (`applyCalibrationCard`, `public/app.js`)**: pakai ULANG mesin `clampCalibrationDelta`/`applySynergyDrag` yang sama persis dengan kalibrasi sumbu unlocked — bukan sistem terpisah. Bedanya cuma dua: (a) `lockedKeys` yang dikirim ke `applySynergyDrag` untuk drag INI SAJA mengecualikan axis yang sedang dikikis (`onboardForm.locked.filter(k => k !== axis)`) supaya dia bisa jadi target drag sendiri, sementara sumbu terkunci LAIN tetap kebal total dari redistribusi ini; (b) untuk axis unlocked, filter itu no-op (axis tidak pernah ada di `locked`), jadi baris kode yang sama melayani dua kasus tanpa percabangan kondisi terpisah.

  **Prompt Chapter Analysis diperbarui** untuk berhenti mengklaim "angkanya TIDAK PERNAH berubah" (sudah tidak akurat) — sekarang menjelaskan sumbu locked kebal dari pilihan yang MENDUKUNG tapi bisa terkikis dari pilihan yang BERLAWANAN, dan kalau memang terkikis (`erodedLocks`, dihitung dari `lockedOriginalValue` vs `radarSnapshot` final), WAJIB disebut konkret di "insight" (mis. "kamu kunci Body di 10, tapi sekarang turun ke 7") — bukan cuma isyarat samar seperti prompt lama. Fallback (tanpa API key) dapat perlakuan sama, `tensionText` sekarang menyebut angka before→after kalau ada.

  Diverifikasi: unit test locked-tinggi terkikis turun dari kontradiksi favorit-least, locked-rendah terkikis naik dari kontradiksi favorit, cap ±3 sama seperti kalibrasi biasa, pilihan yang mendukung lock TIDAK bergerak sama sekali, sumbu locked LAIN yang tidak dikontradiksi tetap 100% kebal, `computeLockTension` versi naif (tanpa `lockedOriginalValue`) TERBUKTI salah klasifikasi (baik gagal menandai kontradiksi asli maupun salah menandai dukungan sebagai kontradiksi) — versi v12 benar di kedua kasus. Live browser end-to-end terhadap kode `public/app.js` asli: Body dikunci 10 → 2 kartu kontrer berturut-turut → turun ke 8 lalu 7 (persis cap −3), total tetap 35, status masih terkunci, narasi "Ketegangan kunci" muncul di Chapter Analysis. Regresi API (crisis-detection, isolasi 2-akun) diulang — tidak ada yang rusak.
- **v13 (SESI INI — Goal capture MAKSIMAL 3, jembatan ke First Trial; dari PRD founder revisi terbaru, section 7 baru):** satu-satunya konten substantif baru di PDF revisi 22-halaman (sisanya identik dengan revisi sebelumnya; catatan: bahasa lock-immunity lama di PDF itu SUDAH di-supersede instruksi eksplisit founder di chat yang melahirkan v12 — v12 TIDAK di-revert). **Goal capture BUKAN bagian onboarding** (onboarding baru saja dipangkas habis, jangan ditambah step lagi) **tapi jembatan ke First Trial**: setelah user konfirmasi Pathway (kartu carousel ATAU override manual), SEBELUM Quest hari pertama digenerate, tampil layar *"Apa yang mau kamu capai selama masa ini?"* — 1-3 goal free-text (REVISI dari draf lama yang cuma 1), contoh nyata founder: "Punya badan sehat", "IELTS band 6.5", "Dapat kerja remote sebagai data analyst" — tiga area beda sekaligus itu wajar, bukan kasus tepi. **PRINSIP INTI (WAJIB, paling gampang salah): Goal menentukan APA, Pathway menentukan BAGAIMANA — dua sumbu independen.** Pathway KONSTAN sepanjang First Trial, tidak ikut berubah saat goal yang digarap berganti; radar boleh dominan di area yang berbeda dari area goal — bukan kontradiksi, radar = fondasi/kondisi, goal = target (boleh di luar area kuat radar), Pathway = gaya konstan mengejar goal apa pun. Contoh acuan prompt (dari PRD, dimasukkan verbatim ke `MENTOR_SYSTEM`): goal IELTS + Pilgrim → "coba 3 metode belajar berbeda minggu ini sebelum komit ke satu silabus", BUKAN "ikuti jadwal belajar terstruktur 2 jam/hari" (itu Architect); pasangan contoh serupa untuk goal Livelihood dan Body. **Rotasi antar-goal DETERMINISTIK DI KODE** (`pickActiveGoalIndex`, `server/index.js`) — bukan user milih tiap pagi (friksi), bukan AI yang mutusin (tidak dipercaya untuk fairness), bukan random: prioritas ke goal yang PALING LAMA belum disentuh Quest (prinsip cakupan yang sama dengan seleksi sumbu kartu skenario), `activeGoal` disuntikkan ke ctx `generateQuest` + `quest.goalIndex` distempel server-side setelah respons AI (juga di mode fallback, supaya rotasi tetap adil dan teruji tanpa API key). **Penyimpanan: array of string** di kolom baru `character_state.goals jsonb` (WAJIB `JSON.stringify` eksplisit — pelajaran `growth_focus` v2 berulang), bukan field radar/stat terpisah; tiap goal secara alami mengarah ke stat lewat `statFocus` yang SUDAH ada di skema Quest — tidak ada skema tambahan. Akun pre-v13 (goals null) tetap jalan normal: rotasi mati (null), prompt tidak berubah, quest tanpa stempel. Bug NYATA yang ketemu saat verifikasi: `recentDays` (db.js) meratakan `quest` jadi cuma judulnya, jadi `goalIndex` tidak pernah sampai ke rotasi — rotasi diam-diam macet di goal #1 selamanya; ketahuan lewat simulasi multi-hari nyata (bukan unit test), diperbaiki dengan mengangkat `goalIndex` jadi field sebelah di hasil `recentDays`. Diverifikasi: unit rotasi (prioritas belum-pernah, siklus penuh 0→1→2→0→1→2, tie-break deterministik, hari pre-v13 tanpa stempel diabaikan, index out-of-range diabaikan), browser end-to-end (layar goals muncul setelah konfirmasi Pathway, tombol nonaktif tanpa goal, tombol balik ke carousel jalan, 3 goal founder tersimpan utuh di Postgres), simulasi 6 hari nyata lewat mundur-tanggal Postgres di server hidup: stempel `goalIndex` per hari = 0,1,2,0,1,2 — ketiga goal kebagian giliran persis rata. Konsistensi gaya-Pathway-lintas-goal adalah perilaku AI (prompt) — perlu spot-check founder di production, tidak bisa diverifikasi otomatis keyless.
- **v14 (SESI INI — perbaikan bug placeholder goal + kerangka WOOP implisit untuk Quest; dari PRD founder revisi 23-halaman, satu-satunya section yang berubah dari revisi v13 adalah section 7):** founder sendiri melaporkan lewat testing: **placeholder input goal v13 (`"Punya badan sehat"`, `"IELTS band 6.5"`, `"Dapat kerja remote sebagai data analyst"`) ternyata literal contoh pribadi founder** — tiga contoh itu di draft PRD sebelumnya cuma ILUSTRASI KONSEP untuk komunikasi dokumen, BUKAN teks yang dimaksud jadi hardcode literal ke aplikasi produksi (kesalahpahaman implementasi saya sendiri sesi sebelumnya, diakui eksplisit di PRD sebagai bug). Akibatnya SEMUA pengguna melihat placeholder yang sama persis — goal pribadi founder, bukan arahan generik. **Fix: placeholder per-Pathway, statis/hardcoded, TIDAK butuh panggilan API baru** (`PATHWAY_GOAL_PLACEHOLDER`, `public/app.js`) — Architect: "Selesaikan portofolio 5 proyek dalam 3 bulan"; Warden: "Konsisten olahraga 4x seminggu tanpa putus"; Weaver: "Bangun ulang koneksi dengan 5 teman lama"; Pilgrim: "Coba 3 jalur karier berbeda sebelum memutuskan satu" — keempatnya persis dari PRD, dipetakan 1:1 ke nama Pathway. **Specialist (kasus khusus, karena domainnya memang didefinisikan user sendiri):** placeholder diturunkan DINAMIS dari teks custom yang sudah diisi user saat memilih Pathway (jalur override) atau `pathwayNoun` yang AI turunkan (jalur kartu carousel) — dicoba dulu terhadap tabel kecil hint statis (`SPECIALIST_GOAL_HINTS`, contoh acuan PRD sendiri: "Sales" → "Closing 5 klien baru bulan ini"), kalau tidak cocok fallback ke template netral yang tetap memakai kata-kata pengguna sendiri ("mis. {teks mereka}, target konkret 14 hari ke depan") — bukan fallback generik tanpa konteks, dan bukan panggilan AI baru (PRD eksplisit: "statis/hardcoded, TIDAK butuh panggilan API baru"). Ketiga baris input goal berbagi SATU placeholder yang sama (bukan 3 placeholder berbeda seperti v13 — PRD cuma kasih satu contoh per Pathway, bukan tiga).

  **Kerangka WOOP (Wish-Outcome-Obstacle-Plan, Oettingen) — implisit di layer AI, BUKAN kolom input baru.** Input goal TETAP 1-3 baris free-text seperti v13 (PRD eksplisit menolak dipecah jadi 4 kolom Wish/Outcome/Obstacle/Plan terpisah — itu nambah friksi yang baru saja susah payah dihindari). `ctx.activeGoal` sudah mewakili Wish+Outcome; yang baru: instruksi eksplisit di `MENTOR_SYSTEM` supaya AI (secara internal, TIDAK ditulis eksplisit berformat "Obstacle: ... Plan: ..." ke pengguna) memikirkan Obstacle paling mungkin bikin goal ITU gagal buat ORANG INI SPESIFIK (diinfer dari radar+profile, mis. Autonomy rendah + goal ambisius → "gampang menyerah tanpa struktur eksternal"), lalu merancang Quest/Acting hari itu supaya SECARA DESAIN mengantisipasi obstacle spesifik itu (Plan) — bukan instruksi generik yang mengabaikan risiko gagalnya; "why" boleh menyinggung alasan ini secara natural tapi tetap kalimat mentor biasa, bukan template berlabel. Alasan founder pilih WOOP di atas SMART: bukti meta-analisis lebih kuat untuk konteks ini (Wang, Wang & Gai 2021, 21 studi, n=15.907, g=0.336) karena eksplisit mewajibkan identifikasi Obstacle duluan, bukan cuma goal yang jelas/terukur. Prompt `generateQuest` diperbarui untuk merujuk kerangka ini eksplisit tiap kali `ctx.activeGoal` ada. Catatan jujur: efek WOOP di kualitas output nyata adalah perilaku AI murni — tidak bisa diverifikasi otomatis tanpa API key sungguhan (fallback deterministik tidak dan tidak perlu meniru WOOP, itu di luar cakupan heuristik sederhananya), perlu spot-check founder di production terhadap beberapa output nyata.

  Diverifikasi: unit test placeholder (7 skenario termasuk regresi eksplisit "3 contoh literal founder TIDAK PERNAH muncul lagi", 4 pathway tetap dapat placeholder persis PRD, override "Sales" cocok hint table PRD, domain custom tak dikenal jatuh ke template netral berisi kata pengguna sendiri, kartu Specialist dengan/tanpa `pathwayNoun` berbeda ditangani benar), browser end-to-end DUA jalur (kartu carousel DAN override manual "Sales") terhadap kode `public/app.js` asli — placeholder yang tampil persis sesuai prediksi, ketiga baris input berbagi placeholder sama, tidak ada teks placeholder yang cocok tiga contoh founder yang lama. Regresi (crisis-detection, rotasi goal tetap benar) diulang — tidak ada yang rusak.
- **v15 (SESI INI — Task 7 growth-gate kespesifikan + Task 7b input terstruktur quest fisik + Task 8 icon bantuan kontekstual + Bible v1.9; dari PRD founder revisi 26-halaman + zip yang membawa PRD.md founder SATU commit lebih baru dari PDF-nya — Task 8 cuma ada di zip, jadi zip yang dipakai sebagai sumber otoritatif):**

  **Task 7 — growth-gate diperkuat: KESPESIFIKAN, bukan cuma panjang (Bible v1.8).** Ditemukan founder lewat testing nyata: gate lama cuma cek ≥12 kata, jadi refleksi generik panjang ("udah olahraga tadi, capek tapi enak, seneng bisa konsisten") lolos tanpa bukti keterlibatan nyata — Goodhart's Law masuk lewat celah itu. Fix di prompt `processReflection` (`server/claude.js`): sertakan deskripsi quest hari itu sebagai pembanding; kalau quest eksplisit meminta detail konkret (angka/ukuran/observasi — contoh nyata founder: "catat repetisi, jarak, dan titik menyerah"), refleksi yang tidak menyebut SATU PUN detail itu WAJIB ditolak growth-nya berapa pun panjangnya (mentorReply menyebut hangat detail apa yang kurang), refleksi SINGKAT tapi spesifik justru sah, dan quest kualitatif/emosional yang tidak minta angka TIDAK dipaksakan standar angka (gate soal kespesifikan YANG DIMINTA). Gate 12-kata deterministik tetap berdiri sebagai lapisan murah di bawahnya — dua lapis, bukan pengganti. Catatan jujur: efek semantiknya perilaku AI, tidak bisa diverifikasi keyless (fallback tanpa key tetap growth-0 seperti sebelumnya) — DoD butir "generik-panjang ditolak / singkat-spesifik diterima / kualitatif tidak over-strict" perlu spot-check founder di production.

  **Task 7b — input TERSTRUKTUR untuk quest fisik (Bible v1.9, keputusan founder 9 Agustus).** Quest fisik/terukur tidak lagi diselesaikan lewat kotak refleksi bebas: angka di kolom terpisah jauh lebih sulit dikarang daripada paragraf — bentuk terkuat anti-Goodhart tanpa GPS/native app. Mekanisme: `generateQuest` sekarang menyertakan `completionType` (`"structured-physical"` untuk fisik/terukur, `"reflective"` untuk kualitatif — dimensi TERPISAH dari `mode` quest/acting) + `structuredKind` (`"cardio"`/`"gym"`), DINORMALISASI DI KODE (`normalizeCompletionType`): pasangan malformed otomatis turun ke `reflective` (defense-in-depth, form rusak tidak pernah dirender); quest fallback keyless selalu `reflective` by construction. Field cardio: jenis aktivitas (Lari/Jalan cepat/Sepeda/Lompat tali/Lainnya+teks, wajib), durasi menit (wajib), jarak km (opsional — skip untuk non-jarak), RPE 1-10 (wajib), titik mulai berat (wajib). Field gym: gerakan, set×repetisi, beban kg (opsional untuk bodyweight), RPE, titik gagal/berat. **Validasi kelengkapan+kewajaran DETERMINISTIK DI KODE** — modul baru `server/structured.js` (dipisah supaya unit-testable tanpa boot server): field wajib, RPE integer 1-10, batas absolut (durasi ≤600mnt, jarak ≤200km, set ≤50, rep ≤500, beban ≤500kg), dan **cek kecepatan per-aktivitas** (Lari ≤25 km/jam, Jalan cepat ≤12, Sepeda ≤60 — contoh mustahil PRD sendiri "20 menit / 15 km lari" = 45 km/jam DITOLAK dengan pesan yang menyebut kombinasi konkretnya, bukan error generik); AI tidak pernah melihat data yang belum lolos validasi ini. Quest terstruktur yang lolos = growth-eligible TANPA gate 12-kata (refleksi naratif jadi OPSIONAL, ditandai eksplisit di UI; status "Nggak sempat" tidak memvalidasi apa-apa dan tetap tanpa growth). **Penyimpanan: di dalam `reflection` jsonb yang SUDAH ADA** (opsi "perubahan skema terkecil" dari PRD — nol kolom baru), otomatis ikut mengalir ke `recentDays` → jadi **baseline progresif** untuk quest berikutnya (prompt `generateQuest` eksplisit: "minggu lalu push-up 15, sekarang coba 18" dari angka nyata; `processReflection` structured-mode juga menerima `recentDays` untuk komentar baseline di mentorReply). UI dashboard: form field terstruktur menggantikan textarea utama (grid 2 kolom, select RPE, kolom "Lainnya" kondisional), error validasi tampil inline dengan nilai yang sudah diketik TETAP UTUH (bukan layar error buntu), ringkasan angka tampil di kartu quest selesai dan riwayat.

  **Task 8 — icon bantuan kontekstual (murni UI, statis).** App berat istilah RPG — icon "?" kecil konsisten di pojok kanan atas pada TEPAT layar yang PRD sebut: radar, Adaptive Cards, Pathway carousel, goal capture, dashboard (layar nama TIDAK dapat — tidak masuk daftar). Tap → sheet ringan dari bawah berisi penjelasan LAYAR ITU SAJA, bahasa awam tanpa jargon, teks STATIS per layar (bukan AI-generated — mekaniknya sama untuk semua orang, lebih murah & terkontrol; empat teks memakai contoh PRD verbatim, teks goal capture ditulis mengikuti gaya yang sama karena PRD tidak memberi contohnya). Tutup lewat tombol ATAU tap backdrop — non-blocking. Satu listener ter-delegasi di `document` (bertahan lintas re-render innerHTML, tanpa wiring per layar).

  **Bible v1.7 → v1.9 di `reference/`** (v1.8 = filosofi gate kespesifikan; v1.9 = input terstruktur + roadmap Phase 4 dikonkretkan ke Capacitor). File lama dihapus, bukan disimpan berdampingan (pola v8).

  Diverifikasi: unit (modul `server/structured.js` ASLI di-require langsung — 26 kasus: happy path cardio/gym, contoh mustahil PRD ditolak dengan pesan menyebut "45.0 km/jam", jalan cepat 20 km/jam ditolak, sepeda 25 km/jam SAH, batas absolut, semua field wajib, RPE non-integer, kind tak dikenal, null tidak crash; + string-level wiring lengkap), browser end-to-end (icon bantuan hadir & sheet buka/tutup dengan teks berbeda di KELIMA layar + absen di layar nama; quest cardio diinjeksi via SQL → field terstruktur tampil menggantikan textarea, naratif berlabel opsional tanpa counter 12-kata, kombinasi mustahil PRD ditolak inline dengan nilai terjaga, kombinasi wajar tersimpan → ringkasan tampil di kartu + `structuredData` terverifikasi di jsonb lewat SQL), regresi (quest reflective fallback ber-`completionType` reflective, refleksi pendek tetap kena gate 12-kata, crisis tetap ke 119 tanpa growth, `goalIndex` tetap terstempel).

**Alur onboarding final v8 (identik v7 di luar daftar nama Pathway — lihat riwayat v8 di atas):**
1. **Nama + Privacy Promise digabung jadi SATU layar** (v2 punya ini sebagai dua step terpisah) — field nama, di bawahnya langsung teks *"Semua yang kamu ceritakan di sini hanya untuk kamu dan Eleva."* + checkbox konfirmasi ("Aku mengerti dan siap mulai").
2. **Radar self-assessment — 7 SUMBU MECE** (Body, Growth, Livelihood, Emotional Stability, Social, Purpose, Autonomy — lihat riwayat v5 di atas untuk asal-usul merger & Autonomy), skala **1-10**, default **5 tiap sumbu**, total **SELALU TEPAT 35** (7×5), zero-sum. Angka nilai tampil di dalam tiap titik; tap titik = kunci/buka kunci (maksimal 3 terkunci, ikon 🔒 + caption "N/3 terkunci"); titik terkunci tidak bisa digeser dan tidak tersentuh redistribusi titik lain. Redistribusi memakai **re-normalisasi satu pool** (riwayat v5): semua sumbu unlocked lain berubah total persis −delta sebanding kekuatan tariknya (partner: bobot evidence; non-partner: nilai saat ini), sumbu yang sudah mentok dikecualikan dari pool, constraint-solving skala-turun seragam, largest-remainder menjaga total presisi 35, feedback getar+teks saat mentok. Interaksi tap-vs-drag dibedakan lewat ambang gerak ~8px (keputusan implementasi, PRD cuma bilang "lewat ikon/tap di titik"). Info lock TIDAK dipersist ke Postgres — dipakai sebagai konteks selama onboarding, dan maknanya terserap ke `originStory` naratif. **Migrasi 8→7: tidak ada** — akun lama reset lalu onboarding ulang (keputusan PRD). Saat step ini ditinggalkan, radar dibekukan sebagai `radarRaw` (client-side) sebelum kalibrasi (langkah 3) mulai menggesernya.
3-N. **Adaptive Scenario Cards, jumlah FLEKSIBEL 2-6** (v6 MENGGANTIKAN TOTAL Adaptive Statement Cards v4/v5 — bukan penambahan, format berbeda sepenuhnya). Tiap kartu: **satu skenario situasional + TEPAT 4 opsi**, masing-masing opsi mewakili SATU sumbu radar (bukan lagi satu pernyataan/satu sumbu per kartu seperti v4/v5 — satu kartu sekarang menutup 4 sumbu sekaligus, jauh lebih efisien: 2-6 kartu cukup untuk cakupan yang dulu perlu 4-10). Interaksi WAJIB dua-duanya, tidak ada yang opsional: user pilih 1 opsi **paling disukai** (favorit, 👍) DAN WAJIB juga pilih 1 dari 3 sisanya sebagai **paling tidak disukai** (👎) — tombol "Lanjut" tidak aktif sebelum dua-duanya terpilih; 2 opsi yang tidak dipilih sama sekali dianggap netral untuk kartu itu. **Pemilihan 4 sumbu per kartu** ditegakkan di kode (`selectCardAxes` di `server/claude.js`), bukan diserahkan ke AI: **sejak v7, opsi diambil dari SEMUA 7 sumbu (locked maupun unlocked — dibalik dari v6 yang cuma unlocked; lihat riwayat v7 di atas)**, diprioritaskan yang PALING SEDIKIT/BELUM pernah jadi opsi di kartu sebelumnya, supaya SEMUA 7 sumbu kebagian giliran sebelum ada yang diuji berulang — tiap kartu selalu punya persis 4 opsi. AI berhenti (minimal 2 kartu) begitu SEMUA 7 sumbu sudah pernah muncul sebagai opsi minimal sekali DAN pola pilihan cukup konsisten; maksimal 6 kartu dipaksa berhenti — **kedua syarat (minimal-2 DAN cakupan-penuh) ditegakkan di kode**, bukan dipercaya dari flag `confident` model sendiri, sama prinsip defense-in-depth dengan growth-gate/crisis-detection. Tiap kartu 1x panggilan API dengan context penuh sejauh ini (radar + semua kartu+pilihan sebelumnya). Data tiap kartu disimpan sebagai `{scenario, options: [{axis, text}], mostPreferred: axis, leastPreferred: axis}` (client-state sampai commit akhir — struktur ini yang dikirim ke Chapter Analysis).

   **Kalibrasi radar dari Adaptive Scenario Cards (mekanisme baru v6, WAJIB — bukan opsional).** Alasan: self-report skala 1-10 di langkah 2 rawan bias (masalah psikometri umum) — radar hasil drag manual adalah HIPOTESIS awal, bukan kebenaran final; reaksi konkret ke skenario menguji ulang hipotesis itu, dan radar dikoreksi HALUS berdasarkan hasilnya, bukan diganti total. Dua pengaman keras (non-negotiable, langsung dari diskusi founder): **(1) angka sumbu terkunci kebal dari kalibrasi SELAMANYA** (tidak pernah bergeser numerik, apa pun yang dipilih terkait sumbu itu — dijamin di `applyCalibrationCard`, `public/app.js`; **sejak v7, sumbu locked BOLEH tetap jadi opsi kartu** — kalau dipilih, dicatat sebagai `lockTension` narasi-saja, lihat riwayat v7 di atas, bukan diabaikan seperti perlakuan v6); **(2) pergeseran kalibrasi harus TERLIHAT, bukan diam-diam** — kalau ada sumbu bergeser net ≥2 dari nilai `radarRaw`-nya, Chapter Analysis WAJIB menyebutnya eksplisit ke pengguna (bukan cuma diam-diam pakai radar terkalibrasi), dan user tetap punya jalur override yang sudah ada (langkah N+3). **Mekanisme memakai ULANG mesin redistribusi yang sudah ada** (`applySynergyDrag`), TIDAK ada sistem kalibrasi terpisah: tiap kartu menghasilkan TEPAT DUA sinyal — sumbu favorit → delta +1, sumbu paling-tidak-disukai → delta −1 (2 sumbu netral → delta 0) — untuk sumbu UNLOCKED, diterapkan berurutan sebagai "drag otomatis" lewat mesin zero-sum+sinergi yang sama persis dengan drag manual (favorit dulu baru paling-tidak-disukai; urutan sebaliknya hasilnya sama, cuma jalur pembulatan sementara beda, tidak signifikan); untuk sumbu LOCKED, delta numerik SAMA SEKALI tidak diterapkan (lihat lockTension di atas). **Batas kumulatif per sumbu unlocked: total sinyal LANGSUNG (bukan pergerakan kolateral dari redistribusi sumbu lain) dari SEMUA kartu untuk satu sumbu dibatasi maksimal ±3 net** (bukan per-kartu — akumulasi, dan bisa saling meng-offset kalau sumbu yang sama pernah jadi favorit lalu jadi paling-tidak-disukai) — begitu limitnya kena, sinyal langsung berikutnya untuk sumbu itu diabaikan/di-clamp ke 0, tapi sumbu itu tetap boleh muncul sebagai opsi di kartu berikutnya (demi konsistensi cakupan). Total radar tetap PERSIS 35 di tiap tahap kalibrasi (delta diterapkan lewat mesin redistribusi yang sama, bukan penjumlahan sederhana yang bisa merusak zero-sum). Radar yang dipakai untuk Chapter Analysis & Pathway Recommendation adalah radar SETELAH kalibrasi; radar mentah (sebelum kalibrasi) disimpan terpisah untuk audit/transparency.
N+1. AI Thinking (layar loading singkat: *"Aku sedang mencoba memahami ceritamu..."*)
N+2. Chapter Analysis — mekanisme dasar tidak berubah dari v2/v3: insight naratif + Pathway Recommendation + Secondary Trait opsional. Inputnya sekarang radar chart TERKALIBRASI FINAL **termasuk sumbu mana yang dikunci pengguna** + semua kartu skenario+pilihan. Kalau ada 2-3 titik terkunci di nilai tinggi (kasus ekstrem), prompt eksplisit meminta AI mengaitkan kombinasi itu ke pola pilihan aktual pengguna, bukan mengarang generalisasi sendiri. **Sejak v6:** kalau ada sumbu bergeser net ≥2 akibat kalibrasi, prompt eksplisit mewajibkan AI menyebutnya di `insight` (server pra-hitung `significantShifts`, tidak dipercaya murni dari AI). **Sejak v7:** (a) layar ini WAJIB menampilkan visual radar mentah vs terkalibrasi bertumpuk, bukan cuma teks (lihat riwayat v7); (b) kalau ada `lockTension` yang cukup berulang, prompt eksplisit mewajibkan AI memunculkannya sebagai pertanyaan reflektif; (c) **Pathway Recommendation sekarang 3 KARTU yang bisa digeser** (kalibrasi/radar-mentah/wildcard — lihat riwayat v7), bukan lagi satu rekomendasi tetap; single panggilan API yang sama juga balikin `pathwayBlurb` + `rawPathwayTop2` untuk mengisi kartu 1 & 2.
N+3. Konfirmasi — tombol utama "Mulai First Trial (14 hari)" (aktif setelah user memilih salah satu dari 3 kartu Pathway) ATAU link sekunder "Bukan ini — aku tahu persis mau melatih apa" (override bebas teks, tidak berubah dari v2/v3). Override tetap masuk First Trial 14 hari yang sama.
N+4. **(v13) Goal capture — jembatan ke First Trial, BUKAN step onboarding:** setelah Pathway dikonfirmasi (dari kartu MAUPUN override), sebelum apa pun dipersist dan sebelum Quest hari pertama digenerate, layar *"Apa yang mau kamu capai selama masa ini?"* — 1-3 goal free-text (minimal 1 wajib, placeholder pakai contoh nyata founder), tombol balik ke pemilihan Pathway tersedia. Goal = APA yang dikejar; Pathway = BAGAIMANA gayanya (konstan) — lihat riwayat v13.

**First Trial (14 hari) — tidak berubah dari v2:** Pathway baru (rekomendasi AI ATAU override) mulai `pathway_status='trial'`. Growth-gate 12-kata tetap berlaku penuh. Resonance-check lazy di `GET /api/state`: `≥14 hari` DAN `≥5 growth session` → `'active'` permanen. Belum cukup → tetap `'trial'`. Traceable ke data refleksi nyata.

**Perubahan schema Postgres** (di atas kolom `pathway`/`pathway_noun`/`pathway_status`/`pathway_trial_started_at`/`secondary_trait` dari v2 yang tetap dipakai): `character_state` nambah `radar_snapshot jsonb` — **sejak v6 eksplisit berarti radar TERKALIBRASI**, dipakai downstream (nilai sumbu 1-10; baris era 8-sumbu tetap membawa 8 kunci lamanya sampai akunnya reset) — dan `radar_raw jsonb` (hasil drag manual SEBELUM kalibrasi, terpisah dari `radar_snapshot`, untuk audit/transparency; akun v3-v5 punya `radar_snapshot` tapi tidak punya `radar_raw` — itu wajar, bukan error, kalibrasi baru ada di v6). **Sejak v13 nambah `goals jsonb`** (array of string 1-3 goal First Trial, di-`JSON.stringify` eksplisit saat insert — array ke jsonb tidak auto-encode benar oleh `pg`; akun pre-v13 bernilai null, dibaca sebagai `[]`); stempel `goalIndex` per hari masuk ke `days.quest` (field di dalam jsonb yang sudah ada, bukan kolom baru). Keduanya (radar) disimpan sebagai object bukan array (bug `growth_focus` v2). Kolom `growth_focus` dari v2 TIDAK dihapus (aman ke baris v2 yang sudah ada), cuma berhenti ditulis untuk user baru.

**Kolom `stats` (0-100, digrow refleksi harian) vs `radar_snapshot` (1-10, terkalibrasi):** dua kolom terpisah, skala beda. `stats` diisi dari `radar_snapshot × 10` sekali saat `POST /api/profile` (server-side). `radar_snapshot` disimpan sebagai rekaman beku dari radar SETELAH kalibrasi Adaptive Scenario Cards (bukan cuma yang digambar manual — lihat riwayat v6 di atas).

**Konteks quest harian pengganti Situasi/Values/Fear yang dihapus:** `profile` server-side jadi `{name, createdAt, originStory}` — `originStory` diisi dari `insight` hasil Chapter Analysis, dipakai gantiin `profile.situation` v2 di context AI harian. Akun v2 yang sudah ada (termasuk akun founder) tidak retroaktif dapat `radar_snapshot`/`originStory` — fallback eksplisit di semua titik pembuatan context AI (`radarSnapshot: state.radarSnapshot || undefined` bersanding `growthFocus: state.growthFocus || undefined`, `originStory: profile.originStory || profile.situation`) supaya akun lama tidak mendadak kehilangan "kompas"-nya begitu v3 di-deploy.

**Route:** `POST /api/onboarding/scenario-card` (ganti nama dari `statement-card` v4/v5, body `{profile, radarSnapshot, lockedAxes, previousCards}`), `POST /api/onboarding/chapter-analysis` (body `{profile, radarSnapshot, radarRaw, lockedAxes, cards}`). `POST /api/profile` tetap satu-satunya titik commit ke DB — kartu-kartunya sendiri TIDAK dipersist ke Postgres (yang dipersist: `originStory`, `radar_snapshot` terkalibrasi, `radar_raw` mentah).

**`server/claude.js`:** `generateScenarioCard(ctx)` (ganti dari `generateStatementCard` v4/v5) balas `{scenario: string|null, options: [{axis,text}]|null, confident: boolean}` — batas minimal 2/maksimal 6 kartu DAN syarat cakupan-penuh-SEMUA-7-sumbu (v7: termasuk locked, lihat riwayat v7) DITEGAKKAN DI KODE (`selectCardAxes`/`coverageComplete`), bukan dipercaya penuh dari `confident` model — sama prinsip defense-in-depth dengan crisis-detection dan growth-gate. Prompt melarang skenario/opsi generik yang sama untuk semua orang dan mengulang skenario sebelumnya; semantik pilihan (favorit = gali lebih dalam, paling-tidak-disukai = area yang dihindari, penolakan juga informasi) tertulis di prompt kartu DAN prompt Chapter Analysis. `generateChapterAnalysis(ctx)` pra-hitung `significantShifts` (sumbu yang bergeser net ≥2 dari `radarRaw`) DAN (v7, baru) `computeLockTension`/`flaggedTensionAxes` (sumbu locked dengan ≥2 pilihan berlawanan dari nilai kuncinya) lewat kode, disuntikkan eksplisit ke prompt kalau ada, dan dikembalikan di response supaya UI bisa menampilkannya sebagai catatan terpisah juga (bukan cuma tergantung teks `insight`). Response juga tambah (v7) `pathwayBlurb` (1 kalimat label kartu 1) dan `rawPathwayTop2` (2 kandidat + blurb dari radar mentah SAJA, diminta di prompt yang sama demi efisiensi — bukan panggilan API terpisah). `MENTOR_SYSTEM` netral soal mekanisme onboarding sejak v3, tidak berubah lagi sampai v8 di mana ditambah SATU aturan baru: **"Pathway ≠ Chapter"** (lihat riwayat v8) — dengan contoh benar/salah eksplisit, berlaku untuk SEMUA panggilan API (harian maupun onboarding) karena system prompt-nya sama persis di keduanya. `PATHWAY_NAMES`/`RADAR_AXIS_TO_PATHWAY` juga di-remap ke 5 nama v1.7 di v8 (lihat riwayat v8). Fallback (tanpa API key): 2 skenario deterministik (cukup untuk cakupan penuh via `selectCardAxes` yang sama), personalisasi murah dari sumbu radar tertinggi (`RADAR_AXIS_TO_PATHWAY` tetap buat `fallbackChapterAnalysis`, juga menyisipkan catatan kalibrasi + ketegangan-lock deterministik kalau relevan, dan menurunkan `rawPathwayTop2` dari top-2 sumbu `radarRaw` lewat heuristik yang sama).

**Dashboard:** tidak berubah dari v2 — badge `{tier} {pathwayNoun}` + indikator "(hipotesis — First Trial)" selama trial.

**Definition of done Task 5 v6 (Adaptive Scenario Cards + kalibrasi radar — dari PRD founder revisi terbaru; menggantikan DoD statement-card v4/v5):**
- [x] Kartu format skenario + TEPAT 4 opsi (BUKAN thumbs up/down satu pernyataan) — dites di browser, tiap kartu selalu punya 4 opsi dari 4 sumbu berbeda
- [x] **(DIBALIK DI v7 — lihat DoD v7 di bawah)** ~~Sumbu terkunci TIDAK PERNAH muncul sebagai opsi di kartu manapun~~ — v6: dites eksplisit (lock Autonomy → dites lewat seluruh sesi kalibrasi, tidak pernah ditawarkan). v7 sengaja membalik aturan ini (locked BOLEH jadi opsi, angkanya tetap kebal) — butir ini historis, dipertahankan apa adanya sebagai catatan perilaku v6.
- [x] Interaksi wajib dua-duanya: tombol Lanjut nonaktif sebelum favorit DAN paling-tidak-disukai dua-duanya dipilih; tap ulang bisa ganti pilihan — dites eksplisit tiap state (sebelum pilih, setelah 1 pilih, setelah 2 pilih)
- [x] Pemilihan 4 sumbu per kartu memprioritaskan sumbu paling sedikit/belum muncul di kartu sebelumnya (dites unit: `selectCardAxes` selalu pilih axis ber-seenCount terendah dulu, tie-break urutan tetap)
- [x] Semua sumbu unlocked tersentuh minimal sekali sebagai opsi sebelum sesi boleh berhenti (dites unit `coverageComplete` + dites browser: 6 sumbu unlocked tercakup penuh dalam 2 kartu)
- [x] AI/kode berhenti minimal 2 kartu, maksimal 6 — DAN baru boleh berhenti kalau cakupan penuh (dua syarat ditegakkan di kode, bukan dipercaya dari `confident` model sendiri)
- [x] Kalibrasi memakai ULANG mesin redistribusi (`applySynergyDrag`) — bukan sistem terpisah; tiap kartu = 2 sinyal (favorit +1, paling-tidak-disukai −1) diterapkan berurutan sebagai drag otomatis
- [x] Batas kumulatif ±3 net per sumbu untuk sinyal LANGSUNG (bukan pergerakan kolateral) — dites unit: 5× sinyal +1 berturut-turut pada satu sumbu → hanya 3 pertama diterapkan penuh, sisanya clamp ke 0; sinyal berlawanan arah bisa meng-offset dan membuka ruang lagi (bukan floor/ceiling independen per arah)
- [x] Total radar tetap PERSIS 35 di setiap tahap kalibrasi (dites browser: sebelum kartu, setelah tiap kartu, setelah kartu terakhir — selalu 35)
- [x] Sumbu terkunci TIDAK PERNAH ikut kalibrasi (dijamin struktural — tidak pernah jadi opsi, jadi tidak pernah jadi favorit/paling-tidak-disukai)
- [x] `radar_raw` (sebelum kalibrasi) dan `radar_snapshot` (setelah kalibrasi) tersimpan terpisah di Postgres, dua-duanya bisa diaudit — dites lewat query SQL langsung setelah `POST /api/profile`
- [x] Pergeseran ≥2 poin net dari kalibrasi WAJIB disebutkan eksplisit di Chapter Analysis (bukan diam-diam) — dites unit `significantShifts` (deteksi threshold tepat, exclude <2) + dites browser end-to-end (drag berulang ke satu sumbu → shift ≥2 → teks insight DAN elemen UI terpisah dua-duanya menyebutkan pergeseran itu dengan angka before→after)
- [x] Pergeseran <2 poin TIDAK disebutkan (tidak ada noise untuk pergeseran kecil yang wajar) — dites unit eksplisit
- [x] Growth-gate 12-kata + crisis-detection + isolasi 2-akun dites ulang setelah v6 — tidak regresi
- [x] Radar 7-sumbu MECE, lock, dan algoritma redistribusi dari v5 tidak berubah/regresi — hanya CARA memicu drag (manual vs kalibrasi otomatis) yang baru
- [x] Radar chart **7 sumbu MECE** (Body, Growth, Livelihood, Emotional Stability, Social, Purpose, Autonomy), default 5 semua, **total selalu 35**, redistribusi PROPORSIONAL/terbobot ke sumbu unlocked lain (bukan rata/equal-split)
- [x] Verifikasi variasi: dua drag berturut-turut ke arah berbeda → titik unlocked lain menunjukkan nilai berbeda-beda, bukan identik
- [x] Total tetap 35 setelah pembulatan (largest-remainder ke 7 titik sekaligus; fuzz 3000 sekuens drag acak + lock acak: selalu tepat 35, semua integer 1-10)
- [x] Reproduksi bug founder edisi 7-elemen: Body dikunci 10, Social ditekan ke 1, Growth dinaikkan dari titik rendah → BISA naik (sampai ceiling 10 sekalipun) selama Livelihood/Emotional/Purpose masih di atas floor; Social yang sudah floor dikecualikan dari pool, TIDAK memblokir gesekan — dites unit + pointer event asli di browser
- [x] Geser Autonomy sendirian ke maksimal (kasus paling terhubung: 5 partner, cuma Social non-partner murni) — mencapai 10, total tetap persis 35, Social tidak dibebani berlebihan (re-normalisasi satu pool, bukan 2×delta ke non-partner saja)
- [x] Geser Livelihood sendirian ke maksimal — total tetap PERSIS 35; tidak ada lagi partner negatif (dua pasangan negatif lama terserap jadi campuran saat merge), batas maksimal murni dari zero-sum
- [x] Kunci 2-3 titik di nilai tinggi, dorong titik lain — pergerakan dibatasi wajar begitu budget 35 mendekati habis (dites: 2 lock @10 → Growth mentok 8; 3 lock @10 → sisa 5 poin, gerakan nyaris nol), dengan feedback getar+teks, bukan macet diam-diam
- [x] Elemen ber-partner banyak (Emotional: 4 partner) digeser +3 → total dampak ke non-partner ≈ 1.4 poin (sebanding delta, BUKAN dijumlah independen per-partner yang bakal minta ~12)
- [x] Angka (1-10) tampil di tiap titik; kunci maksimal 3 berfungsi; kombinasi ekstrem tetap mungkin dan masuk context Chapter Analysis tanpa rumus makna tetap di kode (semua tidak berubah dari v4.x, diverifikasi ulang di heptagon)
- [x] `reference/Eleva_Correlation_Matrix.html` + `ELEVA_Constitution_Product_Bible_v1.5.docx` ikut di-commit ke `reference/` (akhirnya disertakan founder) — **v1.5 docx digantikan `ELEVA_Constitution_Product_Bible_v1.7.docx` di v8** (lihat riwayat v8), file lama dihapus dari repo bukan disimpan berdampingan
- [x] Akun era 8-sumbu: TIDAK dimigrasikan (keputusan PRD) — dashboard menampilkan stats lama dengan label legacy tanpa NaN/undefined (dites lewat injeksi SQL akun 8-kunci + browser), jalur resmi = reset → onboarding ulang
- [x] (butir radar 7-sumbu di atas diwarisi apa adanya dari DoD v5, radar itu sendiri tidak berubah di v6 — lihat butir kalibrasi baru di atas untuk yang berubah)

**Definition of done Task 5 v7 (pembalikan locked-axis-as-option + lockTension + visual radar mentah-vs-terkalibrasi wajib + Pathway 3-kartu — dari PRD founder revisi terbaru; superset DoD v6, butir yang direvisi ditandai di atas):**
- [x] Sumbu terkunci SEKARANG BOLEH muncul sebagai opsi kartu skenario (dibalik dari v6) — dites unit `selectCardAxes` (locked axis tidak difilter dari pool) + dites browser (Body dikunci 10, tetap ditawarkan sebagai opsi berulang kali di sesi kartu)
- [x] Nilai numerik sumbu terkunci TIDAK PERNAH berubah walau dipilih favorit/paling-tidak-disukai — dijamin di `applyCalibrationCard` (early-return sebelum `clampCalibrationDelta`/`applySynergyDrag` untuk axis locked) — dites unit + browser (Body tetap 10 persis setelah seluruh sesi kalibrasi, total radar tetap 35)
- [x] Cakupan-penuh (`coverageComplete`) sekarang mencakup SEMUA 7 sumbu termasuk locked (bukan cuma unlocked seperti v6) — dites unit; invarian lama v6 "3 lock → 1 kartu cukup untuk 4 unlocked" sudah OBSOLETE dan diganti "pool tetap 7 sumbu, batas kartu 2-6 sama seperti sebelumnya"
- [x] `computeLockTension(cards, lockedAxes, radarSnapshot)` merekonstruksi penuh dari data yang sudah dikirim (`ctx.cards`+`ctx.lockedAxes`+`ctx.radarSnapshot`, tanpa bookkeeping baru di client) — dites unit: locked tinggi (>5) dipilih paling-tidak-disukai → `contrary:true`; locked rendah (<5) dipilih favorit → `contrary:true`; locked netral (=5) TIDAK PERNAH contrary apa pun pilihannya; sumbu unlocked tidak pernah menghasilkan entry tension
- [x] `flaggedTensionAxes` memicu di ambang **≥2 pilihan contrary** pada sumbu yang sama (keputusan implementasi saya untuk "berulang kali" yang PRD tidak beri angka pasti, didokumentasikan di komentar kode) — dites unit: 2× contrary → flagged, 1× contrary → belum flagged
- [x] Sumbu ter-flag WAJIB memicu pertanyaan REFLEKTIF (bukan pernyataan) di Chapter Analysis, baik lewat AI (prompt `tensionNote` eksplisit) maupun fallback deterministik — dan TIDAK PERNAH menyarankan user mengubah nilai kunci sendiri (cuma jalur override yang sudah ada yang boleh) — dites browser end-to-end: teks "Ketegangan kunci" muncul di layar analysis persis saat threshold ≥2 tercapai, tidak muncul kalau tidak
- [x] Perbandingan visual radar mentah-vs-terkalibrasi (`renderRadarComparisonSVG`) SELALU dirender di layar Chapter Analysis, bukan opsional/kondisional — dites browser: elemen `#polyCompareSvg`, `#polyShapeRaw` (dashed/muted), `#polyShapeCalibrated` (solid/accent) semua hadir setiap kali; dipakai ulang helper geometri murni (`polyPoint`/`POLY_ORDER`) tanpa drag handle (read-only)
- [x] Pathway Recommendation jadi 3 KARTU yang bisa digeser (carousel horizontal, scroll-snap) — Kartu 1 = pathway terkalibrasi (field lama `pathway`/`pathwayNoun` dipertahankan + `pathwayBlurb` baru), Kartu 2 = pathway dari radar mentah SAJA (`rawPathwayTop2`, AI diminta abaikan `ctx.cards` sepenuhnya untuk field ini, di panggilan API yang SAMA demi efisiensi — bukan request terpisah), Kartu 3 = wildcard acak client-side (`Math.random`, dihitung SEKALI saat analysis termuat, tidak berubah tiap render) — dites unit `buildPathwayOptions` + browser
- [x] Dedup 3 kartu Pathway: Kartu 2 jatuh ke kandidat top-2 raw kalau top-1 sama dengan Kartu 1; Kartu 3 (wildcard) selalu dipilih dari sisa pathway yang belum dipakai Kartu 1/2 — dites unit eksplisit 2 skenario (collision & no-collision) — ketiga kartu SELALU pathway berbeda-beda
- [x] Tombol konfirmasi ("Mulai First Trial") nonaktif sampai user memilih salah satu dari 3 kartu (tap-to-select, bukan default ke Kartu 1) — dites browser: disabled sebelum pilih, enabled setelah pilih
- [x] Memilih kartu NON-DEFAULT (Kartu 2, bukan Kartu 1) mengalir dengan benar sampai ke `pathway`/`pathwayNoun` yang di-submit dan tersimpan — dites browser end-to-end: pilih Kartu 2 secara eksplisit di UI test, verifikasi badge dashboard mencerminkan pathway Kartu 2, bukan selalu Kartu 1
- [x] `rawPathwayTop2` divalidasi ketat di server sebelum dipakai (array persis 2 elemen, tiap elemen punya `pathway` valid dari `PATHWAY_NAMES` + `blurb` string) — response AI malformed jatuh ke `fallbackChapterAnalysis` (defense-in-depth, sama prinsip dengan validasi kartu skenario)
- [x] Growth-gate 12-kata + crisis-detection + isolasi 2-akun + persistensi `radar_raw`/`radar_snapshot` dites ulang setelah v7 — tidak regresi
- [x] Radar 7-sumbu MECE, lock (maks 3), algoritma redistribusi `applySynergyDrag`, dan mesin kalibrasi dari v5/v6 tidak berubah/regresi — v7 hanya mengubah CAKUPAN sumbu yang boleh jadi opsi kartu + menambah lapisan narasi/visual/pilihan di atasnya

**Definition of done Task 5 v8 (rename+reframe Pathway per Bible v1.7 Bab 13 — dari PRD founder revisi terbaru; radar/kartu/kalibrasi/lockTension/carousel dari v5-v7 tidak berubah selain daftar nama):**
- [x] `PATHWAY_NAMES`/`PATHWAY_DESC` (client `public/app.js`) dan server (`server/claude.js`) tepat 5 nama: Architect, Warden, Weaver, Pilgrim, Specialist — dites unit, tidak ada sisa referensi ke Builder/Guardian/Explorer(pathway)/Connector/Seeker di kode (`grep` bersih, cuma tersisa di komentar riwayat)
- [x] Deskripsi tiap Pathway murni gaya perilaku/interaksi, TIDAK menyebut fase/masalah hidup — dites unit dengan daftar frasa terlarang ("belum tahu arah", "stabil secara emosi", dsb — pattern lama v1.2-v1.6 yang direvisi)
- [x] `MENTOR_SYSTEM` (`server/claude.js`) memuat aturan eksplisit "Pathway ≠ Chapter" dengan contoh benar/salah persis dari Bible — berlaku untuk SEMUA teks AI yang menyebut Pathway (quest harian, Chapter Analysis), bukan cuma satu prompt
- [x] `generateChapterAnalysis`: enum literal `pathway` di prompt JSON diperbarui ke 5 nama baru, instruksi `pathwayBlurb`/`rawPathwayTop2` eksplisit mewajibkan framing gaya-perilaku (bukan mengulang tugas "insight" yang boleh bicara fase hidup)
- [x] `RADAR_AXIS_TO_PATHWAY` (fallback heuristik) di-remap lengkap: semua 7 sumbu (plus kunci legacy 8-sumbu) mengarah ke salah satu dari 5 nama baru — dites unit, tidak ada axis yang jatuh ke nama yang sudah dihapus
- [x] `buildPathwayOptions` (dedup 3-kartu, `public/app.js`) tetap menghasilkan 3 pathway berbeda dengan pool yang lebih sempit (5, bukan 6 lagi) — dites unit kasus collision & no-collision, dan dites bahwa SETIAP pasangan (Opsi1, Opsi2) dari 5 nama itu masih menyisakan minimal 1 kandidat wildcard
- [x] Flow onboarding end-to-end (fallback mode, tanpa API key) dites lewat browser: `PATHWAY_DESC` di client persis 5 nama baru, 3 kartu Chapter Analysis semuanya nama baru (tidak ada nama lama bocor), badge dashboard akhir menampilkan pathway baru + indikator trial
- [x] Growth-gate 12-kata, crisis-detection (frasa risiko tinggi → `mentorReply` diganti, `statDeltas` kosong), dan isolasi 2-akun dites ulang lewat API langsung setelah v8 — tidak regresi
- [x] Migrasi data: TIDAK ADA (konsisten pola v5-v7) — akun lama dengan `pathway`/`pathwayNoun` bernilai nama lama (mis. "Builder") tetap tampil apa adanya di badge, bukan error; jalur resminya reset → onboarding ulang

**Definition of done Task 5 v9 (perkuat skala bobot `SYNERGY` 2x per laporan founder; formula/struktur redistribusi tidak berubah):**
- [x] Verifikasi manual: hasil drag SEBELUM perubahan (dari screenshot founder) dihitung ulang persis lewat formula re-normalisasi satu pool yang ada — cocok 1:1, membuktikan bukan regresi ke equal-split, murni efek skala bobot terlalu kecil dibanding pull non-partner
- [x] Bobot digandakan (lemah 1→2, sedang 2→4, kuat 3→6), rasio antar-partner dipertahankan persis, pull non-partner (nilai saat ini) tidak disentuh — dites unit: repro screenshot founder (Body 5→10) dengan bobot baru menghasilkan Emotional (partner terkuat) bergeser SAMA ATAU LEBIH BANYAK dari Livelihood/Social (non-partner) — sebelumnya terbalik
- [x] Dites lewat live browser (bukan cuma fungsi murni di Node) memakai `applySynergyDrag` yang SAMA persis dari `public/app.js` — hasil cocok dengan prediksi unit test
- [x] Semua invarian/DoD lama (v4.2/v4.3/v5) diulang dengan bobot baru — total selalu 35, semua nilai integer 1-10, lock tak tersentuh, Autonomy/Livelihood masih bisa capai 10 sendirian, kasus 3-lock/floor-exclusion masih benar — fuzz 3000 gesekan+lock acak, nol gagal
- [x] Dicatat jujur di PRD: pada delta kecil dari radar default, diferensiasi tetap samar akibat pembulatan integer ke 6 sumbu sekaligus — perbaikan ini mengatasi arah yang terbalik (non-partner > partner kuat), bukan menghilangkan pembulatan itu sendiri

**Definition of done Task 5 v10 (redam pull non-partner 0.5x + variasi ranah skenario — dari testing founder di production):**
- [x] `NONPARTNER_DAMP = 0.5` di `applySynergyDrag` (`public/app.js`) — pull non-partner = 0.5×nilai saat ini, partner tidak berubah dari v9 (6/4/2), normalisasi/saturasi/constraint-solving/largest-remainder tidak disentuh
- [x] Drag tunggal Body 5→10 menghasilkan gradien sinergi TERBACA: partner terkuat (Emotional, bobot 6) bergerak PALING BANYAK secara strict (−2), partner sedang & non-partner −1, partner lemah 0 — dites unit DAN live browser terhadap kode `public/app.js` asli (bukan salinan), hasil identik
- [x] Simulasi sesi kalibrasi 6 kartu dengan pola konsisten (satu sumbu di-favorit 3×, satu di-paling-tidak-disukai 3×): total pergeseran before/after ≥ perilaku pre-v10 (terukur 18 vs 14 poin), sumbu favorit berakhir ≥7, sumbu dihindari ≤3 — akar "before/after nyaris sama" (favored axis jadi penyerap kolateral terbesar sinyal berikutnya) teratasi
- [x] Batas ±3 sinyal langsung per sumbu TIDAK diubah (aturan founder v6 dipertahankan; dicatat sebagai knob terpisah kalau mau swing lebih besar)
- [x] Prompt `generateScenarioCard` mewajibkan rotasi ranah kehidupan antar kartu + melarang asumsi karier/proyek + menyertakan skenario terpakai — dicatat jujur: efeknya di kualitas output AI, tidak bisa diverifikasi otomatis keyless, perlu spot-check founder di production
- [x] Semua invarian lama diulang dengan damp baru: total selalu 35, integer 1-10, lock kebal, Autonomy/Livelihood tetap bisa 10 sendirian, repro floor-exclusion founder tetap benar, fuzz 3000 gesekan+lock acak nol gagal, flow onboarding penuh sampai dashboard lewat browser tanpa error JS

**Definition of done Task 5 v11 (perbesar magnitude per-pilihan kalibrasi 1→2 + larangan sebut nama sumbu radar harfiah + paksa Bahasa Indonesia — dari feedback founder, keputusan magnitude dikonfirmasi eksplisit dari 4 opsi):**
- [x] `CALIBRATION_PICK_MAGNITUDE = 2` (`public/app.js`) — tiap favorit/paling-tidak-disukai per kartu sekarang ±2 (dulu ±1); cap kumulatif ±3 per sumbu (aturan v6) TIDAK diubah — dites unit: 1 pilihan → efektif +2 (belum capped), 2 pilihan konsisten → efektif +1 tambahan lalu mentok tepat di +3, pilihan ke-3 kontribusi 0 (masih di cap)
- [x] **(DIBALIK DI v12 — lihat DoD v12 di bawah)** ~~Axis terkunci tetap TIDAK PERNAH bergerak numerik meski magnitude naik~~ — v11: guard early-return di `applyCalibrationCard` diverifikasi masih utuh. v12 sengaja membalik ini untuk kasus kontradiksi (locked axis BOLEH terkikis) — butir ini historis, dipertahankan apa adanya sebagai catatan perilaku v11.
- [x] Flow onboarding penuh lewat browser (mode fallback, 2 kartu): total radar tetap 35, `calibrationSum` per sumbu tidak pernah melebihi ±3, before/after menunjukkan pergerakan nyata (14 poin net dari 2 kartu saja, vs skenario serupa v10 yang perlu lebih banyak kartu untuk pergerakan sebanding)
- [x] `MENTOR_SYSTEM` (`server/claude.js`) dapat 2 bullet baru: larangan sebut nama 7 sumbu radar secara harfiah di teks bebas (WAJIB diterjemahkan jadi kecenderungan perilaku nyata), dan kewajiban Bahasa Indonesia natural di semua field teks bebas kecuali istilah produk (nama Pathway) — dites string-level bahwa aturan ada persis di source, dan dipastikan aturan Pathway≠Chapter v8 yang sudah ada tidak ikut terhapus
- [x] Dicatat jujur: dua aturan `MENTOR_SYSTEM` baru murni perubahan prompt AI, efeknya di kualitas output nyata tidak bisa diverifikasi otomatis tanpa API key sungguhan — perlu spot-check founder di production (mode fallback keyless memang sudah netral-bahasa/nama-sumbu by construction, jadi tidak representatif untuk kasus yang dilaporkan)

**Definition of done Task 5 v12 (sumbu terkunci BOLEH terkikis dari kontradiksi berulang — reversal eksplisit dari aturan "kebal SELAMANYA" v6/v7/v11, dua keputusan mekanisme dikonfirmasi eksplisit dari founder):**
- [x] `isContraryPick` (`public/app.js`) mirror persis definisi "kontrer" dari `computeLockTension` server (locked-tinggi+least, atau locked-rendah+favorit; locked-di-5 tidak pernah kontrer) — dites unit semua 4 kombinasi arah
- [x] Trigger erosi: SETIAP pilihan kontrer (dari pilihan ke-1), bukan menunggu ambang ≥2 — dites unit: satu pilihan kontrer saja sudah menggerakkan angka locked axis
- [x] Besaran & cap erosi identik dengan kalibrasi biasa (`CALIBRATION_PICK_MAGNITUDE=2`, cap kumulatif ±3, mesin `clampCalibrationDelta` yang SAMA, bukan sistem terpisah) — dites unit: 1 pilihan kontrer → efektif ±2 penuh, 2 pilihan kontrer konsisten → mentok tepat di cap ±3, pilihan ke-3 tidak berkontribusi lagi
- [x] Pilihan yang MENDUKUNG lock (bukan kontrer) tidak menggerakkan angka sama sekali dan tidak membuat entri `calibrationSum` — dites unit eksplisit
- [x] Sumbu locked LAIN (yang tidak dikontradiksi) tetap 100% kebal dari redistribusi saat SATU sumbu locked lain sedang terkikis — dites unit multi-lock
- [x] Sumbu yang sedang terkikis TETAP berstatus "terkunci" penuh sepanjang proses (kebal dari geseran sumbu lain, tetap terhitung `MAX_LOCKS`, tetap ikon 🔒 di UI) — hanya sinyal kontradiksi kartu yang bisa menggerakkannya, bukan drag manual atau redistribusi sumbu lain
- [x] `lockedOriginalValue` (nilai saat dikunci, direkam di `toggleLock`) dipakai untuk menentukan arah "kontrer" SEPANJANG sesi, BUKAN nilai radar live yang mungkin sudah terkikis — dites unit dengan skenario yang secara eksplisit membuktikan bug ini nyata: versi naif (pakai nilai live) salah mengklasifikasi baik pilihan kontrer asli (gagal ditandai) MAUPUN pilihan yang mendukung lock asli (salah ditandai kontrer) begitu nilai sudah melewati titik tengah 5; versi `lockedOriginalValue` benar di kedua kasus
- [x] `lockedOriginalValue` dikirim client→server (`POST /api/onboarding/chapter-analysis` body) dan `computeLockTension` (server) memakainya juga (bukan cuma client) — same-bug-two-places dicegah dengan cara yang sama di kedua sisi; fallback ke `radarSnapshot` kalau field tidak dikirim (client lama, defensif)
- [x] Prompt Chapter Analysis diperbarui: berhenti mengklaim angka locked "TIDAK PERNAH berubah" (sudah tidak akurat), WAJIB menyebut nilai before→after konkret kalau axis memang terkikis (`erodedLocks`) — dites string-level pola prompt; fallback (`fallbackChapterAnalysis`) dapat perlakuan sama
- [x] Live browser end-to-end terhadap kode `public/app.js`+`server/claude.js` ASLI (bukan salinan): kunci Body=10 → dua kartu kontrer berturut-turut → turun ke 8 lalu 7 (persis cap −3 dari 10), total tetap 35 di setiap langkah, status masih terkunci sepanjang proses, narasi "Ketegangan kunci" tampil di Chapter Analysis
- [x] Regresi diulang setelah v12: crisis-detection (frasa risiko tinggi → `mentorReply` diganti, `statDeltas` kosong) dan isolasi 2-akun via API langsung — tidak ada yang rusak

**Definition of done Task 5 v13 (Goal capture maks 3 + rotasi goal deterministik — dari PRD founder section 7 baru):**
- [x] Input goal muncul TEPAT setelah konfirmasi Pathway (kartu carousel MAUPUN jalur override), SEBELUM Quest hari pertama digenerate/dipersist — mendukung 1-3 goal, minimal 1 wajib (tombol nonaktif tanpa goal), tombol balik ke pemilihan Pathway berfungsi — dites browser end-to-end dengan tiga contoh goal nyata founder
- [x] Jawaban tersimpan sebagai array di `character_state.goals jsonb` (di-`JSON.stringify` eksplisit — pelajaran bug `growth_focus` v2), disanitasi server-side (string, trim, buang kosong, maks 3 × 200 char), BUKAN field radar/stat terpisah — diverifikasi lewat query SQL langsung setelah submit
- [x] Quest generation harian menyertakan SEMUA goal sebagai konteks (`ctx.goals`) tapi fokus SATU per hari (`ctx.activeGoal`) berdasarkan goal yang paling lama belum digarap — rotasi deterministik di kode (`pickActiveGoalIndex`, prinsip cakupan yang sama dengan seleksi sumbu kartu), BUKAN random dan BUKAN selalu goal #1; `quest.goalIndex` distempel server-side (tidak dipercaya dari model), juga di mode fallback
- [x] Simulasi lintas hari NYATA (mundur-tanggal Postgres + server hidup, bukan cuma unit test): 6 hari berturut-turut menghasilkan stempel goal 0→1→2→0→1→2 — ketiga goal kebagian giliran persis rata, siklus berulang benar
- [x] Unit test rotasi: prioritas goal belum-pernah-digarap, siklus penuh kembali ke goal tertua, tie-break deterministik ke urutan input, hari pre-v13 (quest tanpa `goalIndex`) tidak dihitung menyentuh goal apa pun, `goalIndex` out-of-range diabaikan, 2-goal bergantian bersih, goals kosong → rotasi mati (null) tanpa menyentuh prompt
- [x] `MENTOR_SYSTEM` dapat aturan inti Goal-vs-Pathway (Goal = APA, Pathway = BAGAIMANA, dua sumbu independen; gaya Pathway konsisten LINTAS goal, bukan cuma lintas hari) LENGKAP dengan contoh acuan wajib dari PRD (IELTS×Pilgrim vs Architect, Livelihood, Body) dan aturan radar-boleh-beda-dari-goal BUKAN kontradiksi yang perlu dikomentari — prompt `generateQuest` menyebut `activeGoal` eksplisit sebagai target hari itu
- [x] Bug nyata ditemukan & diperbaiki saat verifikasi: `recentDays` meratakan quest jadi judul saja sehingga `goalIndex` tak pernah sampai ke rotasi (macet diam-diam di goal #1) — ketahuan lewat simulasi multi-hari, bukan unit test; `goalIndex` sekarang diangkat jadi field sebelah di hasil `recentDays`
- [x] Akun pre-v13 (tanpa goals): quest harian tetap jalan normal, tanpa stempel `goalIndex`, prompt tidak berubah — dites lewat akun regresi lama
- [x] Konsistensi gaya-Pathway-lintas-goal dicatat jujur sebagai perilaku prompt AI — tidak bisa diverifikasi otomatis keyless, perlu spot-check founder di production
- [x] Regresi diulang setelah v13: crisis-detection + isolasi 2-akun — tidak ada yang rusak

**Definition of done Task 5 v14 (bug fix placeholder goal (literal contoh founder bocor ke semua user) + kerangka WOOP implisit untuk Quest — dari PRD founder revisi 23-halaman, bug dilaporkan founder sendiri lewat testing):**
- [x] Placeholder goal TIDAK PERNAH lagi menampilkan tiga contoh literal founder ("Punya badan sehat"/"IELTS band 6.5"/"Dapat kerja remote sebagai data analyst") — dites unit regresi eksplisit di 7 skenario pathway berbeda, dan browser end-to-end dua jalur (kartu carousel + override manual)
- [x] Empat Pathway bernama tetap (Architect/Warden/Weaver/Pilgrim) dapat placeholder statis persis dari PRD, dipetakan 1:1, tanpa panggilan API tambahan
- [x] Specialist (domain didefinisikan user) dapat placeholder DINAMIS dari teks custom (`pathway` untuk jalur override, `pathwayNoun` untuk jalur kartu) — dicoba dulu ke tabel hint statis kecil (cocok contoh acuan PRD "Sales"→"Closing 5 klien baru bulan ini"), fallback ke template netral yang tetap memakai kata pengguna sendiri kalau tidak cocok — dites unit untuk match, no-match, dan kasus tanpa noun berbeda (fallback mode)
- [x] Ketiga baris input goal berbagi SATU placeholder yang sama (bukan 3 berbeda seperti v13 — koreksi dari kesalahpahaman implementasi sebelumnya) — dites browser
- [x] `MENTOR_SYSTEM` dapat aturan WOOP implisit: AI memikirkan Obstacle spesifik-per-orang sebelum menulis Quest yang menyasar goal, merancang instruksi supaya mengantisipasi obstacle itu (Plan) — TANPA menulis "Obstacle:/Plan:" eksplisit ke pengguna, TANPA kolom input baru (input goal tetap 1-3 baris free-text, PRD eksplisit menolak dipecah 4 kolom) — dites string-level bahwa instruksi ada di prompt `generateQuest` saat `ctx.activeGoal` terisi
- [x] Dicatat jujur: efek WOOP di kualitas output nyata adalah perilaku AI, tidak bisa diverifikasi otomatis tanpa API key sungguhan — perlu spot-check founder di production
- [x] Regresi diulang setelah v14: crisis-detection, rotasi goal (`goalIndex` tetap distempel benar) — tidak ada yang rusak

**Definition of done Task 7 v15 (growth-gate kespesifikan — dari PRD founder section 11, Bible v1.8):**
- [x] Prompt `processReflection` menyertakan quest hari itu sebagai pembanding + instruksi eksplisit: tolak growth kalau refleksi tidak menyebut satu pun detail konkret yang DIMINTA quest, berapa pun panjangnya; terima refleksi singkat-tapi-spesifik; jangan over-strict untuk quest kualitatif yang tidak minta angka — dites string-level ketiga aturan ada persis di source
- [x] mentorReply penolakan diinstruksikan menyebut detail apa yang kurang (hangat, actionable) — bukan penolakan buta
- [x] Gate 12-kata deterministik TETAP berdiri sebagai lapisan bawah (dites API: refleksi pendek masih ditolak dengan pesan gate) — dua lapis, bukan diganti
- [ ] Butir semantik DoD founder (generik-panjang ditolak / singkat-spesifik diterima / kualitatif tidak over-strict pada quest lama) — perilaku AI, TIDAK BISA diverifikasi keyless; perlu spot-check founder di production dengan beberapa refleksi nyata

**Definition of done Task 7b v15 (input terstruktur quest fisik — dari PRD founder, Bible v1.9, keputusan 9 Agustus):**
- [x] Quest fisik (`completionType: "structured-physical"`) menampilkan field terstruktur menggantikan kotak refleksi bebas — dites browser dengan quest cardio hasil injeksi SQL
- [x] `completionType`/`structuredKind` dari AI dinormalisasi di kode (`normalizeCompletionType`): malformed → turun aman ke `reflective`; quest fallback keyless selalu `reflective` — dites string-level + API (fallback quest membawa `completionType: "reflective"`)
- [x] Validasi kelengkapan + kewajaran DETERMINISTIK di `server/structured.js` (modul terpisah, unit-testable): field wajib per-kind, RPE integer 1-10, batas absolut, cek kecepatan per-aktivitas — 26 kasus unit lolos termasuk contoh mustahil PRD (20 menit / 15 km lari = 45 km/jam DITOLAK dengan pesan menyebut kombinasi konkretnya) dan pembanding wajarnya (sepeda 25 km/jam SAH)
- [x] Kombinasi mustahil ditolak dengan pesan jelas INLINE di form, nilai yang sudah diketik tetap utuh (bukan layar error buntu) — dites browser
- [x] Quest kualitatif/emosional tetap pakai refleksi + gate (12-kata + kespesifikan Task 7) — tidak regresi, dites API
- [x] Refleksi naratif untuk quest terstruktur OPSIONAL eksplisit (label "opsional", tanpa counter 12-kata, boleh kosong) — dites browser
- [x] Data terstruktur tersimpan DI DALAM `reflection` jsonb yang sudah ada (nol kolom baru — opsi perubahan-terkecil PRD), terverifikasi lewat SQL; otomatis mengalir ke `recentDays` → prompt `generateQuest` eksplisit memakai sebagai baseline progresif ("minggu lalu 15, sekarang 18") dan `processReflection` structured-mode menerima `recentDays` untuk komentar baseline
- [x] Ringkasan angka tampil di kartu quest selesai + riwayat (baseline tetap terlihat pengguna)
- [x] Status "Nggak sempat" tidak memvalidasi field apa pun dan tetap tanpa growth
- [ ] Kualitas komentar baseline progresif di mentorReply/quest hari berikutnya — perilaku AI, perlu spot-check founder di production

**Definition of done Task 8 v15 (icon bantuan kontekstual — dari PRD founder di zip, section 12; commit founder `081cdfb`, tidak ada di PDF):**
- [x] Icon "?" posisi konsisten pojok kanan atas di TEPAT layar yang PRD sebut: radar, Adaptive Cards, Pathway carousel, goal capture, dashboard — dites browser kelimanya; layar nama TIDAK dapat icon (tidak masuk daftar PRD) — dites eksplisit absen
- [x] Isi penjelasan kontekstual PER LAYAR (5 teks berbeda, 4 memakai contoh PRD verbatim; teks goal capture ditulis segaya karena PRD tidak memberi contohnya), statis/hardcoded — bukan AI-generated, tanpa panggilan API
- [x] Bahasa awam tanpa jargon RPG yang belum dijelaskan
- [x] Sheet ringan dari bawah, non-blocking, tutup lewat tombol ATAU tap backdrop — dites browser buka+tutup di kelima layar; satu listener ter-delegasi (bertahan lintas re-render innerHTML)

**Definition of done khusus bug fix v4.3 (HISTORIS — era 8 sumbu; angka-angka spesifiknya digantikan oleh DoD v5 di atas):**
- [x] Reproduksi persis dari laporan founder (`body:10 locked, mind:6, career:4, finance:6, emotional:6, purpose:6, explorer:1, social:1`) — geser Explorer +1 TIDAK lagi ditolak total, sekarang berhasil (dites lewat pointer event asli di browser, plus fungsi murni)
- [x] Sumbu yang sudah di floor/ceiling (seperti Social=1 di atas) TIDAK memblokir pergerakan sumbu lain sama sekali — dikecualikan dari pool, sisanya tetap menyerap dan delta tetap jalan
- [x] Setelah fix, semua test case constraint-solving v4.2 lain diulang — tidak ada regresi: Body max ≈9-10, Career max ≈7-8 (tidak pernah 10), Purpose+3 dibagi terbobot bukan dijumlah independen, lock flow, feedback getar+teks saat benar-benar mentok
- [x] Kasus saturasi berantai (dua sumbu non-partner sudah di floor sekaligus) diuji lewat fuzz — tidak macet, total tetap 40
- [x] Fuzz 2000 gesekan acak dari state awal acak (termasuk lock 1-3 sumbu) — total selalu tepat 40, semua integer 1-10, tidak pernah gagal
- [x] Tidak ada regresi di growth-gate, crisis-detection, isolasi 2-akun (tidak disentuh sesi ini, di luar scope perubahan)

**Definition of done Task 5 v4.2 (HISTORIS — era 8 sumbu; butir radar/angka spesifik digantikan DoD v5, butir kartu/analysis/trial tetap berlaku):**
- [x] Step 1 (Nama + Privacy Promise) satu layar dengan checkbox, bukan dua step terpisah
- [x] Radar chart 8 sumbu, default 5/5/5..., total selalu 40, perubahan satu sumbu mendistribusi ulang sumbu lain yang BELUM terkunci secara PROPORSIONAL/terbobot (bukan rata/equal-split — bug versi production sebelumnya, sudah diganti)
- [x] Verifikasi eksplisit variasi: setelah geser, titik-titik unlocked lain menunjukkan nilai berbeda-beda, BUKAN selalu identik satu sama lain (dites: 4 nilai distinct setelah satu drag Career)
- [x] Total tetap selalu 40 meski setelah pembulatan (largest-remainder ke 8 titik sekaligus, bukan pembulatan naif per-titik — fuzz 1000 drag acak: selalu tepat 40, semua integer 1-10)
- [x] Geser Body sendirian ke maksimal — berhenti di ≈9.9 (2 non-partner: Explorer, Social) — dites unit + browser
- [x] Geser Career sendirian ke maksimal — berhenti di ≈7.8 (dibulatkan 8), TIDAK PERNAH mencapai 10 (cuma 1 non-partner: Finance) — dites unit + browser, cocok persis dengan hitungan manual PRD
- [x] Kunci sumbu lalu dorong sumbu ber-evidence negatif ke tinggi — sistem menahan lewat constraint-solving (dites: Body dikunci 10, Career mentok di 7), bukan diam-diam membiarkan kombinasi mustahil
- [x] Saat titik mentok, ada feedback (getar kalau didukung + teks) — bukan berhenti diam-diam atau macet
- [x] Purpose (4 partner) digeser +3 — total dampak ke non-partner = 2×delta = 6 poin (dibagi terbobot, BUKAN dijumlah independen per-partner) — dites unit, cocok dengan contoh PRD
- [x] Angka (1-10) tampil di tiap titik radar, bukan cuma posisi visual
- [x] Fitur kunci maksimal 3 titik berfungsi: titik terkunci tidak berubah nilainya akibat redistribusi titik lain
- [x] Kombinasi ekstrem (2-3 titik terkunci tinggi sekaligus) tetap memungkinkan secara matematis, dan masuk sebagai context ke Chapter Analysis — TIDAK ada rumus/lookup table tetap yang otomatis menyimpulkan makna kombinasi tertentu di kode
- [x] Growth Focus (multi-select kategori) TIDAK ada — digantikan radar chart
- [x] Step statis Situasi/Values/Fear TIDAK ada — digantikan Adaptive Statement Cards
- [x] Adaptive Statement Cards minimal 4, maksimal 10, format kartu + thumbs up/down (BUKAN input teks bebas)
- [x] Tiap pernyataan kartu di-generate AI per pengguna dari radar chart + histori swipe — bukan bank pernyataan statis/hardcoded (fallback tanpa-API-key satu-satunya pengecualian, deterministik dan jujur soal keterbatasannya, sama seperti fallback quest/reflection yang sudah ada)
- [x] AI berhenti lebih awal kalau pola swipe sudah cukup konsisten (batas 4/10 ditegakkan di kode)
- [x] Chapter Analysis menampilkan insight + Pathway Recommendation + Secondary Trait (opsional)
- [x] Tombol "Mulai First Trial" DAN link override "Bukan ini" dua-duanya ada dan berfungsi
- [x] Override tetap masuk status `trial`, bukan langsung `active`
- [x] Growth-gate 12-kata tidak regresi
- [x] Secondary Trait tidak dapat Identity Maturity ladder atau alokasi quest sendiri
- [x] Setelah 14 hari, ada logic resonance-check yang mengaktifkan Pathway atau memperpanjang trial (tidak berubah dari v2/v3, sudah diverifikasi lewat simulasi tanggal)
- [x] Akun v2/v3 yang sudah ada tetap dapat quest harian yang berfungsi (kontrak server yang dipakai akun existing tidak berubah di v4; fallback kompas v2 dari putaran v3 tetap berlaku)
- [x] Growth-gate, crisis-detection, isolasi 2-akun dites ulang eksplisit setelah perubahan v4

## 10. Task 6 — Prompt caching (setelah Task 5, tidak diblokir gate lain)

**Alasan:** system prompt Eleva (`MENTOR_SYSTEM` — instruksi nada mentor, aturan Goodhart's Law, dsb) SAMA di setiap panggilan API, baik onboarding maupun harian. Tanpa caching, bagian yang sama ini dibayar penuh berulang-ulang di setiap panggilan.

**Implementasi:** `system: MENTOR_SYSTEM` (string) di `callClaude` (`server/claude.js`) diganti `system: [{type:"text", text: MENTOR_SYSTEM, cache_control:{type:"ephemeral"}}]`. Bagian yang beda-beda tiap panggilan (konteks personal pengguna, riwayat refleksi) TETAP dikirim fresh di `messages`, tidak ikut di-cache.

**Catatan penting soal ambang minimum cache:** model `claude-sonnet-4-6` yang dipakai butuh **minimum 1024 token** di prefix yang di-cache supaya benar-benar tersimpan — di bawah itu, `cache_control` TIDAK error, cuma diam-diam tidak pernah menghasilkan cache hit (`cache_read_input_tokens` tetap 0 selamanya). `MENTOR_SYSTEM` versi v3 (setelah ditulis ulang buat Task 5) diperkirakan ~650-700 token — MASIH DI BAWAH ambang 1024. Kode `cache_control` tetap dipasang (tidak ada biaya tambahan kalau tidak ke-cache), tapi cache hit belum akan muncul sampai system prompt bertambah panjang secara natural — BUKAN ditambal artifisial cuma buat lewat ambang. Ini dilaporkan jujur, bukan diklaim "selesai" begitu saja.

**Definition of done:**
- [x] System prompt di `server/claude.js` pakai cache control Anthropic (`cache_control: {type:"ephemeral"}`)
- [x] `callClaude` expose `data.usage` (sebelumnya dibuang sama sekali) supaya `cache_read_input_tokens`/`cache_creation_input_tokens` bisa dicek — di-log tiap panggilan
- [ ] Verifikasi lewat response API bahwa cache hit benar-benar terjadi di panggilan kedua dst — BELUM bisa diverifikasi di sesi ini (sandbox lokal tidak punya `ANTHROPIC_API_KEY` aktif, dan estimasi token system prompt saat ini di bawah ambang minimum cache 1024 token). Perlu dicek ulang begitu jalan di production dengan API key nyata, dan/atau setelah system prompt bertambah panjang.
- [x] Tidak ada perubahan perilaku/kualitas output AI akibat caching — ini murni optimasi biaya, bukan perubahan konten prompt

## 11. Eksplisit DI LUAR scope sesi ini

- **Payment/billing** (Midtrans/Xendit dsb) — beta ini gratis, monetisasi baru dipikirkan setelah pilot selesai. Model bisnisnya SUDAH diputuskan (lihat bagian 12) tapi implementasinya jangan dikerjakan sekarang.
- Magic link / OAuth login — email+password dulu cukup.
- Flow lupa password.
- Side Quest, Social Quest, Career Quest, Exploration Quest (masih Main Quest saja).
- Redesign visual besar-besaran.
- Kebijakan privasi hukum formal (cukup notice minimal di Task 4).
- **Ide founder (10 Agustus, belum keputusan final, sengaja ditunda "sampai tahap pengembangan lanjutan"):** field record cardio/gym yang TIDAK bisa disajikan alat tracking manapun (RPE, titik mulai/gagal berat) diusulkan supaya AI yang menghasilkan laporannya sendiri dari data objektif (jenis/durasi/jarak — yang MEMANG bisa disalin dari Strava), bukan pengguna yang mengetik penilaian subjektif manual. Alasan founder: Strava enak karena laporannya sudah jadi tinggal disalin; Eleva saat ini membebani pengguna jadi "penulis laporan" untuk bagian yang device manapun nggak bisa ukur. Konteks penting yang belum terselesaikan sebelum ini bisa dieksekusi: RPE & titik-berat itu SENGAJA subjektif+spesifik (persis prinsip anti-Goodhart yang sama dengan gate refleksi teks) — kalau AI yang "menebak"-nya dari angka objektif semata, sinyal susah-dipalsukan yang jadi alasan field ini ada dari awal bisa hilang total (AI tinggal mengarang RPE plausibel dari jarak+durasi, tanpa pengguna benar-benar menilai dirinya sendiri). Perlu didesain ulang dengan hati-hati (misal: AI mengajukan estimasi sebagai draft yang WAJIB dikonfirmasi/dikoreksi pengguna, bukan auto-isi diam-diam) sebelum dibangun — jangan diimplementasikan tanpa diskusi ulang dengan founder soal cara menjaga anti-fabrication-nya.

Kalau merasa salah satu di atas "sekalian aja dikerjakan", tahan dulu — tanyakan ke pengguna dulu.

## 12. Model monetisasi — SUDAH DIPUTUSKAN, BELUM DIBANGUN (referensi untuk nanti, bukan task sesi ini)

Dicatat sekarang supaya keputusannya nggak hilang, TAPI implementasinya (payment gateway, dsb) tetap di luar scope sesi ini (lihat bagian 11). Baca sebelum ada yang mulai membangun fitur monetisasi kapan pun nanti.

**Struktur: Freemium "asli tapi terbatas"** — BUKAN "gratis = template, bayar = personal". Ide awal yang DITOLAK: versi gratis pakai respons template/generik, versi bayar baru dapat AI personal asli — ditolak karena momen "AI ini beneran ngerti aku" adalah yang bikin orang mau upgrade; kalau ditahan di balik paywall, pengguna gratis nggak akan pernah merasakan alasan untuk upgrade.

Struktur yang benar — beda di KEDALAMAN/FITUR, bukan di KEASLIAN personalisasi:

| | Gratis | Berlangganan |
|---|---|---|
| Quest/Acting Method | Asli, AI beneran generate (dengan prompt caching aktif) | Asli, sama |
| Frekuensi | 1/hari, tetap | Sama |
| Pathway & First Trial | Terkunci atau versi ringan | Penuh (Identity Maturity, Secondary Trait) |
| Riwayat & Chapter Analysis | Terbatas (mis. 7 hari terakhir) | Penuh, tersimpan selamanya |

**Harga acuan yang sedang dipertimbangkan founder:** ~Rp50.000/bulan/user. Berdasarkan kalkulasi biaya Sonnet (lihat bagian 1), ini punya margin sehat bahkan tanpa Haiku.

**Catatan platform:** Apple/Google App Store memotong komisi dari transaksi in-app (umumnya 30%, bisa lebih rendah untuk developer kecil tergantung kebijakan yang berlaku saat itu — cek App Store Connect / Google Play Console mendekati waktu launch, kebijakan ini berubah dari waktu ke waktu).

## 13. Definition of done untuk sesi ini (Task 0-4; Task 5 & 6 punya DoD sendiri di atas dan statusnya terpisah)

- [ ] Repo ke-push ke `github.com/hizkiarigel/Eleva`, branch `main`
- [ ] Backup SQLite lama tersimpan aman di luar Railway sebelum migrasi
- [ ] Postgres aktif, `DATABASE_URL` terhubung, data lama termigrasi ke akun founder
- [ ] `eleva.db` keluar dari git tracking, `.gitignore` & `README.md` diperbarui
- [ ] Signup/login/logout jalan, dilindungi `BETA_CODE`
- [ ] 2 akun uji dites eksplisit: tidak ada kebocoran data antar-user, termasuk lewat manipulasi request API langsung
- [ ] Semua route API ada di belakang auth middleware, di-scope by `userId` dari session (bukan dari body/param client)
- [ ] Reflection dengan frasa risiko tinggi → arahkan ke 119 ext. 8, tanpa growth, tervalidasi jalan
- [ ] Reflection normal tidak regresi (growth-gate 12-kata tetap berlaku)
- [ ] Privacy notice tampil sebelum signup selesai
- [ ] README diperbarui: cara jalanin lokal dengan Postgres + auth, dan catatan jelas bahwa payment BELUM ada

## 14. Task 9 — Fokus 1: model quest per-goal, menggantikan Rotasi antar-goal v13 (SELESAI)

Dari `UPDATE_PROMPT.pdf` founder (10 Agustus), prioritas tertinggi sesi ini, dilaporkan sebagai bug regresi: quest yang belum dikerjakan masih bisa hilang diganti quest baru, walau perbaikan 24h-rolling-window (v13 lampiran, section 9) sudah jalan. Diagnosis: masalahnya bukan KAPAN quest kedaluwarsa, tapi MODEL rotasinya sendiri — `pickActiveGoalIndex` (deterministik, prioritas goal paling-lama-belum-disentuh) tetap berarti hanya SATU goal yang aktif di satu waktu, jadi goal lain bisa "menang giliran" dan mendorong keluar quest goal yang belum sempat diselesaikan. **`pickActiveGoalIndex` dan seluruh mekanisme rotasi kompetitif di section 9 (v13) resmi digantikan** oleh spec di bawah — bukan disempurnakan, diganti modelnya.

**Aturan inti**: satu goal boleh punya paling banyak SATU quest terbuka (`reflection IS NULL`) di satu waktu. Quest baru untuk goal itu HANYA digenerate begitu quest sebelumnya beres (`done`/`partial`/`skipped` — apa pun status refleksinya, bukan cuma "done"). Dengan hingga 3 goal (goal capture v13 tidak berubah, tetap 1-3 goal), bisa ada hingga 3 quest terbuka bersamaan. Tidak ada lagi "goal yang menang giliran" — semua goal yang butuh quest baru dapat sekaligus dalam satu pass `GET /api/state`.

**Skema**: `days.id` (serial) jadi primary key, bukan lagi komposit `(user_id, date)` — satu goal generate banyak baris `days` dari waktu ke waktu, tidak ada lagi konsep "baris hari ini" per user. Kolom `issued_at` dan seluruh konsep kedaluwarsa dihapus total — sesuai aturan inti, satu-satunya syarat generate ulang adalah refleksi sudah terisi, tidak pernah soal waktu. `goal_index` jadi kolom asli (sebelumnya cuma field di dalam jsonb `quest`).

**Dashboard**: `GET /api/state` mengembalikan `openQuests` (array semua quest terbuka lintas goal, bukan satu `today`) — ditampilkan sebagai `.quest-carousel` yang sama polanya dengan Pathway carousel v7, satu kartu per goal, label nama goal di tiap kartu supaya jelas kartu mana untuk goal yang mana. Akun pra-goal-capture (goals kosong/null) tetap dapat pengalaman quest tunggal normal — 1 slot dengan `goalIndex: null`, bukan error atau kartu kosong.

**Penyelesaian quest**: `POST /api/reflection` dialamatkan lewat `questId` (bukan `date` seperti model lama) — tiap kartu carousel punya tombol sendiri yang menunjuk `id` quest itu spesifik, tidak ada lagi satu "quest hari ini" implisit. Baseline progresif untuk quest fisik (`recentDays`) di-scope per-goal (`goalIndex`) supaya angka goal lari tidak pernah bocor jadi baseline goal gym.

**Konsekuensi UX yang wajib ditangani** (bukan opsional — tanpa ini fitur ini justru regresi baru): karena goal yang baru selesai LANGSUNG generate quest baru tanpa jeda, balasan mentor + `statDeltas` bisa lenyap sebelum sempat dibaca kalau dashboard langsung refresh ke carousel berikutnya. `POST /api/reflection` membalas `mentorReply`/`deltas`/`structuredData` (bukan cuma `{ok:true}`), klien menahannya di kartu pengakuan (`completedResult`, pola sama seperti kartu Pathway) dengan tombol "Lanjut" eksplisit sebelum carousel di-refresh.

**Riwayat**: karena tidak ada lagi kuota harian yang bisa "kelewat" (goal bisa dikerjakan kapan pun, tidak ada jatah harian yang hangus), `allHistory` disederhanakan jadi murni "yang sudah direfleksikan", konsep terlewat di Riwayat (section sebelumnya, bug 10 Agustus pertama) sudah tidak relevan lagi di bawah model ini.

**DoD** (diverifikasi lewat API test langsung ke Postgres lokal + Playwright browser test, mode fallback tanpa API key):
- [x] 3 goal → 3 quest terbuka simultan setelah onboarding, satu per `goalIndex`
- [x] Quest ID stabil lintas `GET /api/state` berulang tanpa aksi apa pun (tidak pernah diam-diam diganti)
- [x] Selesaikan quest goal A → goal B dan C TIDAK terpengaruh (id tetap sama), goal A dapat quest baru dengan id berbeda
- [x] Akun lawas tanpa goals (`goals: []`) tetap dapat 1 quest, `goalIndex: null`
- [x] Submit quest yang sama dua kali (id sudah punya refleksi) ditolak 400
- [x] Riwayat bertambah tepat 1 entri per refleksi tersimpan
- [x] UI: carousel render 3 kartu dengan label goal berbeda-beda, submit → kartu "Lanjut" tampil `mentorReply`, dismiss → carousel 3 kartu lagi (termasuk quest baru goal yang baru selesai)

## 15. Task 10 — Fokus 2.1 & 2.2/2.3: Pace otomatis + "Target Berikutnya" (SELESAI)

Dari `UPDATE_PROMPT.pdf` founder (10 Agustus), Fokus 2 — dua sub-item dikerjakan bersamaan karena saling terkait (keduanya di area form record cardio/gym).

**2.1 — Pace (min/km) otomatis**: murni tampilan turunan di bawah field Durasi+Jarak form cardio, dihitung `Durasi ÷ Jarak` (`paceLabel`, `public/app.js`) — BUKAN kolom input baru, tidak ada skema tambahan. Ditulis langsung ke DOM tiap keystroke (pola sama seperti hitungan kata refleksi), bukan lewat re-render penuh, supaya fokus input tidak hilang saat mengetik.

**2.2/2.3 — "Target Berikutnya" (revisi jadi 3 opsi A/B/C)**: setelah quest fisik tersimpan (structured-physical, ada angka yang bisa ditarget — `targets.canTarget`, server/targets.js modul baru), layar hasil (kartu `completedResult` yang sama dengan Fokus 1) menampilkan ringkasan angka dulu, baru picker "Target Berikutnya" di bawahnya — reuse `.pathway-carousel`/`.pathway-card` yang sama persis dengan carousel Pathway onboarding, sesuai instruksi eksplisit founder ("pakai ulang pola yang sama dengan Pathway carousel").

- **Opsi A & B**: dua ARAH progresi AI yang benar-benar beda (`generateTargetOptions`, server/claude.js) — cardio: A "kejar kecepatan" (jarak serupa, pace lebih cepat), B "kejar jarak" (pace serupa, jarak nambah); gym: A tambah repetisi, B tambah beban (atau tambah set untuk bodyweight). Fallback tanpa API key dihitung deterministik dari angka hari ini (bukan placeholder) — hasil ujinya persis mendekati contoh founder di PRD ("3.21km @ 6:13/km" → fallback A "3.2km @ 5:47/km", B "3.9km @ 6:25/km"). Tiap opsi disertai "approach" 1 kalimat pendekatan, bukan cuma angka kosong.
- **Opsi C**: tulis sendiri, tapi field-nya numerik (durasi+jarak untuk cardio — sama gaya dengan form record, bukan input pace langsung; set/repetisi/beban untuk gym) supaya "tercapai belum" tetap bisa dicek deterministik seperti A/B, bukan cuma label teks bebas. Ini deviasi kecil dari "tulis sendiri" murni (yang secara harfiah berarti teks bebas) — sengaja, karena tanpa angka nyata sistem tidak bisa pernah tahu kapan harus menawarkan target baru lagi.
- **Target itu ANGKA, bukan cuma label** (`server/targets.js`): tiap target tersimpan sebagai `metrics` numerik (`{jarakKm, paceMinPerKm}` cardio, `{set, repetisi, bebanKg?}` gym) supaya `targetReached` bisa mengecek DETERMINISTIK di kode (toleransi pace 2% buat pembulatan), bukan tanya AI ulang tiap kali.
- **Target PERSISTEN per goal** (`character_state.goal_targets JSONB`, keyed goalIndex — kolom baru, `db.setGoalTarget` merge tanpa menimpa goal lain): quest berikutnya untuk goal itu digenerate sebagai LANGKAH MENUJU target (`ctx.currentTarget` masuk prompt `generateQuest`), layar hasil quest berikutnya HANYA tampil progres 1 baris ("Target: 3.2km @ 5:47/km — masih menuju ke sana") — **picker A/B/C TIDAK muncul lagi sampai target tercapai/terlampaui**. Endpoint baru `POST /api/goal-target` menyimpan pilihan; `GET /api/state` ikut balikin `goalTargets`.

**Definition of done:**
- [x] Pace tampil live saat Durasi+Jarak diisi, hilang lagi kalau salah satu kosong, tidak mengganggu fokus input saat mengetik
- [x] Layar hasil muncul SETELAH quest disimpan (bukan sebelum) — ringkasan angka dulu, baru 3 opsi
- [x] Opsi A & B benar-benar beda arah secara numerik (bukan dua angka mirip)
- [x] Tiap opsi A/B disertai pendekatan/latihan singkat, bukan cuma angka
- [x] Opsi C (tulis sendiri) berfungsi dan tersimpan sama seperti A/B, termasuk validasi inline kalau kosong
- [x] Layar A/B/C HANYA muncul saat goal belum punya `current_target` ATAU target lama baru tercapai — TIDAK muncul tiap kali quest selesai kalau target masih dalam proses dikejar
- [x] Quest untuk goal dengan `current_target` aktif digenerate sebagai langkah menuju target, disertai tampilan progres — bukan asumsi target langsung tercapai
- [x] Simulasi 3 quest berturut-turut untuk goal yang sama dengan target belum tercapai — target TIDAK berubah/ke-reset di tengah jalan
- [x] Target yang dipilih muncul sebagai konteks nyata di prompt quest berikutnya untuk goal yang sama
- [x] Refleksi tetap terpisah, tidak digabung/dihapus
- [x] Berlaku untuk kedua tipe goal terstruktur (cardio dan gym), bukan cuma lari

## 16. Task 11 — Fokus 0: bug tampilan form fisik + revisi field (SELESAI)

Dari `UPDATE_PROMPT.pdf` susulan founder (10 Agustus, prioritas tertinggi sesi ini), dua bagian:

**Bug tampilan (diverifikasi, TIDAK butuh perubahan kode)**: founder melampirkan `reference/screenshots/body-quest-completion-target.png` sebagai bukti quest `completionType="structured-physical"` masih menampilkan kotak refleksi generik dulu saat tap "Mulai", dengan form terstruktur disembunyikan di link sekunder "Aktivitas fisik? Catat sebagai record →". Diverifikasi lewat quest yang di-tag `structured-physical` langsung via SQL + Playwright: kode HASIL Fokus 1 sesi ini (`recordMode = quest?.completionType === "structured-physical"` di handler `[data-reflect-id]`, `public/app.js`) SUDAH membuat field terstruktur tampil langsung sebagai tampilan utama — toggle "Balik ke refleksi teks aja" hanya render kalau `!mustRecord`, jadi tidak pernah muncul untuk quest bertag ini. Screenshot founder kemungkinan diambil dari build produksi sebelum Fokus 1 di-deploy. Tidak ada perubahan kode untuk bagian ini.

**Revisi field (perubahan nyata)**:
1. **Durasi format MM:SS** — input `durasiMenit` di form cardio jadi `type="text"` (placeholder "20:01"), di-parse client-side (`parseDurasiMenit`, `public/app.js`) jadi menit desimal tepat SEBELUM dikirim ke server — satu titik konversi di edge klien, representasi internal (pace calc, target metrics `server/targets.js`, validasi `server/structured.js`) tidak berubah sama sekali, semua tetap menerima angka polos seperti sebelumnya. Toleran menerima angka polos tanpa titik dua juga (kompatibilitas/kemudahan).
2. **RPE 1-10 dihapus total** dari form cardio DAN gym — field UI, validasi wajib di `server/structured.js`, referensi di prompt `processReflection` (`server/claude.js`), dan `structSummary` (`public/app.js`) semua dibersihkan bersamaan, tidak ada RPE tersisa di manapun di codebase.
3. **"Titik mulai berat" cardio** (sebelumnya teks bebas) diganti picker 3 pilihan **Ringan / Cukup / Berat** (tombol `[data-tberat]`, styling sama dengan status picker yang sudah ada). Field refleksi kondisional **"Apa yang bikin berat?"** (`titikBeratDetail`) HANYA muncul dan HANYA wajib diisi kalau pilih "Berat" — Ringan/Cukup tidak memaksa penjelasan lanjutan. **Sengaja cardio-only**: revisi founder menyebut nama field ini secara eksplisit ("Titik mulai berat"), sementara field gym yang mirip punya nama & konsep berbeda ("Titik gagal/berat" — set/rep mana yang gagal, bukan kapan mulai terasa berat) dan tidak disebut di revisi, jadi tetap teks bebas seperti semula.

**DoD**:
- [x] Tidak ada field/label RPE di form cardio maupun gym (dites eksplisit: 0 kemunculan teks "RPE")
- [x] Input durasi cardio `type="text"`, placeholder MM:SS, pace terhitung benar dari nilai MM:SS yang diparse
- [x] Picker Ringan/Cukup/Berat menggantikan input teks lama untuk cardio; input teks lama (`data-sf="titikBerat"`) sudah tidak ada di DOM
- [x] Pilih Ringan atau Cukup — TIDAK memunculkan field detail
- [x] Pilih Berat — field detail muncul; submit dengan field itu kosong ditolak inline (bukan crash/silent fail); submit dengan field terisi tersimpan dan tampil di kartu hasil + Riwayat
- [x] Form gym: RPE hilang, "Titik gagal/berat" tetap teks bebas seperti semula
- [x] Regresi penuh: carousel per-goal (Fokus 1), pace display (Fokus 2.1), Target Berikutnya A/B/C + manual override (Fokus 2.2/2.3) semua tetap hijau dengan payload yang disesuaikan skema field baru

## 17. Feedback langsung founder dari produksi (10 Agustus, setelah Task 11) — dua item kecil

**1. Record picker tidak auto-terbuka untuk quest Body yang salah tag (SELESAI).** Screenshot produksi: quest "Audit Titik Mula Tubuhmu" (jelas minta angka) masih kebuka ke kotak refleksi dulu — tag AI `completionType` ternyata `reflective`, bukan `structured-physical`, walau isinya jelas fisik/terukur. `recordMode` (`public/app.js`) sekarang default nyala juga kalau `quest.statFocus === "body"`, sebagai lapisan cadangan deterministik di samping tag AI yang terbukti tidak selalu benar — tetap cuma DEFAULT (toggle balik ke refleksi teks tetap tersedia), bukan requirement baru yang memaksa. Sekalian ditambah label stat di kartu quest (`qlabel`, mis. "QUEST · nama goal · Body") sesuai permintaan founder.

**2. Countdown 24 jam diminta balik, TAPI cuma versi visual+lockout (SELESAI, dengan gap yang diketahui).** Diklarifikasi dulu lewat pertanyaan eksplisit (potensi konflik langsung dengan hasil Fokus 1 hari yang sama — countdown lama punya efek FUNGSIONAL yaitu quest diam-diam diganti, persis regresi yang baru diperbaiki): founder menjawab mau versi baru yang beda — tombol "Mulai" jadi disabled/hilang begitu lewat 24 jam, TAPI quest-nya sendiri TIDAK diganti/dihilangkan (invariant Fokus 1 tetap utuh), dan solusi jangka panjang ("terus gimana kalau kejadian ini beneran ke goal-nya") sengaja ditunda ("next solusi kita pikirkan nanti" — kata founder sendiri). Implementasi: `days.created_at` (kolom baru, `TIMESTAMPTZ DEFAULT now()`) murni timestamp tampilan, tidak pernah dibaca oleh logic penggantian quest mana pun. Client: countdown ⏳ HH:MM:SS live per kartu quest (satu `setInterval` bersama, nulis langsung ke DOM tiap kartu — bukan lewat `renderDashboard()`, biar draft refleksi di kartu lain tidak keganggu, pola sama dengan pace/word-count). Begitu lewat 24 jam: tombol jadi `disabled` + teks "Waktu habis", kartu tetap ada di carousel. **Gap yang diketahui, sengaja dibiarkan sesuai instruksi founder**: goal yang satu-satunya quest terbukanya locked-out begini untuk sementara tidak punya jalan maju otomatis — solusinya belum diputuskan. Server TIDAK menegakkan batas ini (submit lewat API langsung tetap diterima meski tombol client-nya disabled) — sengaja, supaya tidak menambah dead-end kedua di lapisan lain sebelum solusi jangka panjangnya jelas.

**DoD**:
- [x] Quest Body-focus yang salah tag tetap auto-buka record picker (dites lewat quest yang di-tag `reflective` + `statFocus: body` langsung via SQL)
- [x] Toggle balik-ke-refleksi tetap berfungsi (default baru, bukan paksaan)
- [x] Label stat tampil di kartu quest
- [x] Countdown quest baru mulai dari ~24:00:00, turun tiap detik secara live
- [x] Quest yang `created_at`-nya dimundurkan 25 jam menampilkan pesan lewat-waktu + tombol disabled
- [x] Quest yang locked-out TETAP ada di `openQuests` dengan id yang sama — tidak diganti/dihilangkan (invariant inti Fokus 1 terjaga)
- [x] Regresi penuh (Fokus 1 carousel, Fokus 0 form) tetap hijau

## 18. Task 9 — Sub-Pathway (15 arketipe tetap): AI memilih dari enum tetap, bukan generate bebas (SELESAI)

Dari PDF founder terpisah ("task9subpathway.pdf") — catatan penomoran: dokumen founder ini menyebutnya "Task 9", label yang KEBETULAN sudah dipakai section 14 PRD.md ini untuk topik lain (Fokus 1). Founder punya sistem penomoran Task sendiri per dokumen yang tidak selalu sinkron dengan urutan section repo ini — dicatat di sini supaya tidak rancu, bukan indikasi ada dua pekerjaan "Task 9" yang tumpang tindih.

**Kenapa**: `pathwayNoun` yang sudah live di produksi — digenerate bebas oleh AI untuk Kartu 1 (kalibrasi), sementara Kartu 2 (raw) dan Kartu 3 (wildcard) TIDAK PERNAH punya sub-label sama sekali (cuma mengulang nama Pathway-nya) — diganti total ke katalog 15 arketipe TETAP (5 Pathway × 3 sub masing-masing). Founder eksplisit memilih ini demi kualitas & konsistensi kesan pertama dibanding risiko AI mengarang teks janggal (analisis-lintas-cohort baru relevan di skala ratusan user, bukan pilot 10 orang — keputusan ini murni soal kualitas kesan pertama, sudah didiskusikan dan diputuskan sadar). Ini mengubah behavior yang sudah live, bukan penambahan fitur baru.

**Katalog tetap** (`SUB_PATHWAY_NAMES`, identik byte-for-byte di `server/claude.js` dan `public/app.js` — tidak ada shared-module system di codebase ini, pola hand-sync yang sama seperti `PATHWAY_NAMES`/`PATHWAY_DESC` yang sudah lebih dulu dipisah dua tempat):
```js
const SUB_PATHWAY_NAMES = {
  Architect: ["Engineer of Foundations", "Strategist of Blueprints", "Craftsman of Precision"],
  Warden: ["Sentinel of Discipline", "Keeper of Boundaries", "Guardian of Consistency"],
  Weaver: ["Connector of Circles", "Anchor of Belonging", "Bridge of Empathy"],
  Pilgrim: ["Wanderer of Meaning", "Seeker of Horizons", "Nomad of Discovery"],
  Specialist: ["Architect of Mastery", "Artisan of Depth", "Virtuoso of Precision"],
};
```
Nama Inggris SENGAJA (istilah produk, sama perlakuan dengan nama Pathway sendiri — aturan v11 MENTOR_SYSTEM soal wajib-Bahasa-Indonesia-kecuali-istilah-produk).

**Cakupan — cuma jalur kartu carousel, BUKAN jalur override**: override manual ("Bukan ini — aku tahu persis mau melatih apa") memakai teks bebas sebagai `pathway`/`pathwayNoun` (`pendingPathway = { pathway: v, pathwayNoun: v }`), sama sekali independen dari `buildPathwayOptions` — katalog 15 arketipe secara definisi tidak berlaku di jalur itu, tidak disentuh sama sekali.

**Mekanisme per kartu** (tiga perlakuan beda karena tiga tingkat konteks AI yang beda):
- **Kartu 1 (kalibrasi)**: AI **memilih** (bukan generate bebas) satu dari 3 arketipe milik `pathway` yang direkomendasikan, di panggilan `generateChapterAnalysis` yang SUDAH ADA (nol biaya API tambahan) — karena `pathway` dan `subPathway` diputuskan bersamaan dalam satu panggilan, prompt menyuntikkan SELURUH katalog 15 (dibangun dari `SUB_PATHWAY_NAMES` sendiri lewat `.map`/`.join`, bukan diketik dua kali supaya prompt tidak pernah bisa drift dari yang divalidasi) dan menginstruksikan: setelah `pathway` ditentukan, `subPathway` WAJIB salah satu dari 3 milik pathway itu saja. Field respons baru `subPathway` menggantikan `pathwayNoun` yang lama — field `pathwayNoun` DIHAPUS TOTAL dari kontrak JSON (baik di prompt maupun di objek yang dikembalikan), tidak ada lagi teks bebas untuk field ini.
- **Kartu 2 (raw)** dan **Kartu 3 (wildcard)**: tidak melalui penilaian AI berkonteks-penuh (raw sengaja abaikan kartu skenario; wildcard murni acak), jadi sub-nya juga dipilih **acak client-side** (`pickRandomSubPathway`, `public/app.js`) — pola yang SAMA PERSIS dengan pemilihan wildcard pathway itu sendiri (`Math.floor(Math.random() * ...)`), dihitung SEKALI saat `buildPathwayOptions` jalan (bukan re-roll tiap render — properti ini sudah terjamin struktural karena `buildPathwayOptions` cuma dipanggil sekali per fetch `chapterAnalysis`, tidak di dalam render loop). Konsisten dengan levels-of-curation yang sudah ada di kode (Kartu 1 = AI penuh, Kartu 2-3 = lebih ringan), bukan penyimpangan baru. Tidak perlu dedup tambahan antar-kartu untuk sub-pathway: dedup Pathway yang sudah ada menjamin ketiga kartu selalu ambil dari 3 grup arketipe berbeda, jadi collision string mustahil terjadi struktural.

**Validasi — BEDA dari pola tiga field lain (defense-in-depth, tapi lebih permisif)**: `pathway`/`pathwayBlurb`/`rawPathwayTop2` tetap all-or-nothing (satu field salah bentuk → `throw`, seluruh respons dibuang, jatuh ke `fallbackChapterAnalysis`). `subPathway` sengaja BEDA — di-**patch di tempat**, bukan menggagalkan seluruh respons: helper baru `normalizeSubPathway(pathway, subPathway)` (mirror pola `normalizeCompletionType` yang sudah ada di file yang sama) mengecek `SUB_PATHWAY_NAMES[pathway].includes(subPathway)` — kalau tidak (null/malformed/nama dari pathway lain/nama karangan) → di-patch ke arketipe pertama pathway itu (`SUB_PATHWAY_NAMES[pathway][0]`), TIDAK PERNAH error, TIDAK PERNAH kosong. Alasannya: Chapter Analysis yang sudah bagus (insight, rekomendasi pathway, blurb) terlalu berharga untuk dibuang cuma karena AI salah satu sub-field yang jauh lebih kecil taruhannya. `fallbackChapterAnalysis` (jalur tanpa API key, satu-satunya jalur yang bisa dites end-to-end lokal karena tidak ada key terkonfigurasi) reuse helper yang sama dengan `null` sebagai input kedua — otomatis jatuh ke arketipe pertama, nol duplikasi logika.

**Penyimpanan**: tidak ada kolom baru — `pathwayNoun` yang dikirim ke `POST /api/profile` tetap kolom yang sama (`character_state.pathway_noun`), cuma isinya sekarang dijamin salah satu dari 15 nilai tetap untuk akun non-override (teks bebas untuk akun override, tidak berubah). Mekanisme `generateQuest` menurunkan `pathwayNoun` dari `null` untuk akun lama pra-fitur ini (fallback legacy) tetap ada, tidak disentuh — sejak fitur ini, akun baru manapun (lewat carousel atau override) selalu punya `pathwayNoun` terisi saat profil dibuat, jadi jalur derivasi-null itu praktis tidak pernah lagi terpicu untuk akun baru.

**Diverifikasi**:
- Unit test (`require("./server/claude")`, tanpa perlu API key): `normalizeSubPathway` — 15 kombinasi nama valid passthrough tidak berubah; nama dari pathway yang SALAH (cross-pathway leak) → patch ke index 0 pathway yang diminta; `null`/`undefined`/string kosong/nama karangan → patch ke index 0; pathway tak dikenal → `null`. `fallbackChapterAnalysis`: `subPathway` selalu anggota katalog pathway-nya sendiri, persis index 0, `pathwayNoun` benar-benar `undefined` (bukan cuma kosong). `generateChapterAnalysis` tanpa key: rute otomatis ke fallback, shape sama. Sinkronisasi katalog: baca kedua file sebagai teks, `eval` object literal `SUB_PATHWAY_NAMES` di `public/app.js`, `JSON.stringify` dibandingkan byte-per-byte dengan versi `server/claude.js` — identik.
- Playwright end-to-end (onboarding penuh, mode fallback tanpa key): carousel akhir tepat 3 `.pathway-card`, ketiganya (bukan cuma Kartu 1) menampilkan format `"Pathway: Archetype"` — efek visual yang memang disengaja, dulu cuma Kartu 1 yang punya sub-label; tiap archetype adalah anggota katalog pathway kartu itu sendiri; klik kartu (memicu re-render lewat `renderAdaptive()`, TIDAK memanggil ulang `buildPathwayOptions`) → 3 teks archetype identik sebelum/sesudah klik (bukti tidak re-roll); jalur override diverifikasi terpisah — teks bebas ("Public Speaking Coach") mengalir verbatim ke badge dashboard, sama sekali tidak dipaksa masuk 15 katalog; selesaikan onboarding lewat kartu non-override → goal capture → dashboard: badge `{tier} {pathwayNoun}` menampilkan salah satu dari 15 nama tetap, verbatim.
- Regresi: ulang test carousel-per-goal Fokus 1 yang sudah ada — tetap hijau, tidak ada dampak silang.

**Gap yang diketahui, dicatat bukan ditutup**: pemilihan `subPathway` oleh AI SUNGGUHAN di Kartu 1 (bukan fallback), dan skenario nyata "AI mengembalikan `subPathway` di luar enum lalu di-patch" pada jalur asli — tidak bisa diverifikasi tanpa `ANTHROPIC_API_KEY` asli, yang tidak dikonfigurasi di lingkungan lokal ini. Unit test terhadap `normalizeSubPathway` jadi proxy yang cukup untuk logika patch-nya; verifikasi penuh jalur AI perlu smoke-check manual founder di produksi begitu key aktif — gap yang sama jenisnya dengan yang sudah dicatat untuk Task 7 growth-gate kespesifikan sebelumnya di sesi ini.

## 19. Task 12 — Redesign visual onboarding (design handoff, 10 Agustus): Radar · Pathway Tarot · Goal Capture (SELESAI)

Dari bundle `Eleva_Design_Token_Audit.zip` founder: README handoff high-fidelity + mockup `.dc.html` + salinan codebase yang sudah membawa serangkaian perbaikan usability ("Step 1-6 round") yang BELUM ada di repo. Dua lapis pekerjaan, keduanya visual/copy-only (constraint keras handoff: nol perubahan data model, alur, math radar, growth-gate, rotasi quest, atau panggilan API):

**Lapis 1 — adopsi perbaikan usability dari bundle** (diff bundle vs repo diverifikasi manual sebelum diadopsi):
- Placeholder goal per-Pathway + hint Specialist ditulis ulang jadi realistis-14-hari ("Rapikan 1 bagian portofolio yang paling ketinggalan", "Closing 2 klien baru dalam 14 hari ke depan") — copy lama diam-diam mengajari user menulis goal yang mustahil selesai dalam trial.
- Copy step radar diganti "Ke mana kamu mau fokus sekarang?" + sub baru berorientasi prioritas-ke-depan (bukan "gambarkan kondisimu") — hasil uji pengguna, verbatim.
- Checkbox privacy Step 1 DIHAPUS sebagai kontrol terpisah → caption "Dengan tap Lanjut, kamu setuju dengan pesan privasi di atas" di bawah tombol; validasi step jadi nama-saja. (Checkbox privacy di layar SIGNUP tidak disentuh — itu mekanisme Task 4 yang berbeda.)
- Copy pilihan kedua kartu skenario: "paling nggak sesuai sama kamu" (bukan "paling nggak kamu suka").
- `lockExplainer`: hint satu-kali saat pertama mengunci axis (transien, BUKAN badge persisten — pola badge "TERKUNCI" permanen dites dan ditolak).
- Tap kartu Pathway = konfirmasi langsung (tombol "Mulai First Trial" di layar carousel dihapus) — sekaligus menghilangkan bug lama scroll carousel ke-reset saat konfirmasi me-render ulang carousel di tempat (paling kelihatan saat memilih kartu wildcard paling kanan): sekarang layarnya berganti seluruhnya.

**Lapis 2 — redesign 3 layar per README handoff:**
- **Token warna baru** (`:root` styles.css): `--bg #0d0c12`, `--surface #131019`, `--surface2 #17151f`, `--text #ece7dc`, `--muted #948f9c` + token baru `--muted-dim #6f6b78`/`--accent-bright #e8b768`/`--hair-strong`, `--accent #d8a355`; `--growth`/`--rust` SENGAJA tidak berubah (hijau eksklusif "positif/selesai", rust eksklusif error). 11 kemunculan hex `#e8a33d` hardcoded dimigrasi ke padanan aksen baru. Elevation model: tanpa box-shadow, depth murni tone-shift + hairline. `.btn-primary` membesar app-wide (16px/52px min-height). `.scenario-opt` kontras naik (border `--hair-strong`, bg `--surface2`, radius dikoreksi ke 12px). Blok `prefers-reduced-motion` ditambahkan (README mengklaim sudah ada, nyatanya belum — sekarang ada).
- **Screen 1 Radar (instrument/bezel styling)**: bezel luar aksen-14%, 3 ring skala DASHED redup (36/68/100% radius — radius-space, bukan value-space), garis axis SOLID lebih terang (hierarki kunci: axis ≠ ring), tick circle di vertex luar, polygon data fill aksen-16% stroke 1.75. Titik handle: outline saat bebas, **SOLID aksen saat terkunci** (r 5→6.5) — glyph 🔒 dihapus total, dan TIDAK memakai hijau (eksklusif makna lain). Label nilai numerik 1-desimal (mono 7.5px, `paint-order: stroke` supaya terbaca di atas ring dashed; aksen saat locked). Label axis mono 8px (wrap 2 baris untuk "Emotional Stability", aksen saat locked). **Ikon (i) per-axis** (lingkaran aksen-75% + glyph, hit-circle transparan r12 supaya tetap gampang di-tap) men-toggle **panel definisi** di bawah chart (teks `AXIS_DEFINITIONS` yang sudah user-tested, verbatim) — menggantikan grid definisi statis DAN daftar definisi di sheet bantuan "?" layar radar (sheet dashboard tetap membawa daftar itu). Dua bullet di atas chart jadi SATU-SATUNYA copy penjelas yang selalu tampil; baris counter "N/3 terkunci" persisten dihapus. Hit-target drag tetap besar (circle transparan r14 terpisah dari dot visual) — logika drag/lock/redistribusi TIDAK disentuh. `POLY_VIEW_MIN/SIZE` dilebarkan (-46/392, center invariant terjaga) karena ikon Livelihood kepotong di bound lama — ketahuan dari screenshot Playwright.
- **Screen 2 Chapter Analysis + Pathway**: perbandingan radar raw-vs-terkalibrasi dipertegas (raw dashed `rgba(148,143,156,.55)` 3-3, kalibrasi solid aksen + fill 14%, legend dot outline-vs-filled "Radar awal"/"Terkalibrasi"). Copy intro baru dengan emphasis ("Pathway adalah CARA... Tujuannya (GOAL KAMU SENDIRI) dipilih di step berikutnya"). **Kartu tarot** (class BARU `.tarot-card`/`.tarot-carousel` — `.pathway-card` lama TIDAK diubah karena masih dipakai picker Target Berikutnya Fokus 2): aspect-ratio 2.27/4.75, kartu utama 150px border emas 2px + inset glow + wash radial-gradient emas/ungu + bintang-bintang kecil (placeholder CSS — ilustrasi tarot/celestial asli perlu menggantikan ini sebelum rilis luas, cukup untuk pilot), sekunder 130px opacity .8; numeral romawi I/II/III; footer = label sumber mono + **baris "kenapa cocok"** templated dari data yang sudah ada di client (kartu 1: top-2 axis radar terkalibrasi; kartu 2: top-2 radar mentah; kartu 3: frasa gaya per-Pathway statis `PATHWAY_ALT_PHRASE` — frasa Weaver verbatim dari mockup) — nama Pathway/arketipe TIDAK tampil di muka kartu (disengaja: reveal-nya di layar berikutnya), data `pendingPathway` tetap membawa arketipe Task 9 utuh ke badge dashboard.
- **Screen 3 Goal Capture**: eyebrow "PATHWAY TERPILIH" → heading "The {Pathway}" Fraunces 600 26px emas (payoff visual dari kartu yang di-tap; teks bebas override tampil apa adanya tanpa "The") → divider ornamen (garis gradien memudar ke tengah + berlian 5px) → recap HOW/WHAT satu baris → **entri goal gaya naskah bernomor romawi** (i./ii./iii., baris hairline atas-bawah, input transparan tanpa kotak, placeholder italic; numeral iii redup saat kosong & menyala saat diisi — via DOM langsung, tanpa re-render supaya fokus ketikan aman; placeholder iii = "Opsional") → CTA besar. **Jalur override PINDAH KE SINI** (keputusan founder atas pertanyaan ⚠️ "needs verification with product" di handoff — README menghapusnya dari layar carousel tanpa menentukan nasibnya): link "Bukan salah satu dari kartu tadi? Tulis pathway-mu sendiri" membuka input inline; konfirmasi mengganti `pendingPathway` jadi teks bebas verbatim (data flow end-state SAMA PERSIS dengan override lama, termasuk tidak pernah dipaksa masuk katalog 15 arketipe) lalu heading/recap/placeholder ikut menyesuaikan.
- **Bug interaksi Task 9 yang ketemu saat verifikasi visual**: `goalPlaceholder` menyelidiki `pathwayNoun` Specialist sebagai kata domain (pola era "Closer") — sejak Task 9 isinya nama arketipe, menghasilkan placeholder janggal "mis. Architect of Mastery, target konkret...". Fix: `pathwayNoun` yang merupakan anggota `SUB_PATHWAY_NAMES` langsung dapat placeholder netral; probe domain kini hanya berlaku untuk teks override bebas.

**Diverifikasi** (Playwright viewport 385px, 32 assertion + 2 run tambahan): Step 1 tanpa checkbox + caption + tombol aktif dari nama saja; radar copy/bullet/chrome baru lengkap (bezel 1, ring 3, axis 7, tick 7, dot 7, handle 7, nilai "5.0"), tanpa 🔒, tanpa counter; ikon (i) buka/tutup panel definisi Body verbatim; tap dot → solid locked + explainer sekali; kartu skenario copy baru; layar analysis 3 kartu tarot (1 primary), numeral I/II/III, why-line terisi ketiganya, TANPA tombol Mulai dan TANPA link override; **tap kartu wildcard (III) → langsung Goal Capture** (QA spesifik yang diminta handoff); goals: heading "The Specialist", divider, numeral i./ii./iii., "Opsional", recap, link override ada; override → heading jadi teks bebas verbatim → dashboard badge menampilkannya; jalur kartu non-override → badge menampilkan salah satu 15 arketipe (Task 9 utuh). Regresi: quest carousel per-goal (API) + Target Berikutnya picker (`.pathway-card` tidak berubah) tetap hijau. Screenshot ketiga layar dikirim ke founder.

## 20. Task 9 — Practice Test (Reading & Listening): completionType baru untuk goal belajar terukur (SELESAI)

**Catatan penomoran** (label "Task 9" founder dipakai untuk topik berbeda tiga kali sepanjang PRD ini — dicatat di prosa biar tidak bingung, bukan direnumber ulang: section 14 = Fokus 1 per-goal quest model, section 18 = Sub-Pathway 15 arketipe, section ini = fitur di bawah). Sumber: `PROMPT_PRACTICE_TEST.pdf` (dokumen actionable tunggal) + `Eleva_PRD.pdf` section 13 (konteks lengkap, dokumen master founder).

**Gate diverifikasi sebelum mulai**: fitur ini menyimpan progres belajar per-user (level kesulitan, riwayat skor) sehingga butuh Task 2 (auth + multi-tenant) dan Task 3 (crisis safety) sudah terverifikasi — beda dari Task 7/7b/8 yang bebas gate (logic backend murni). Dicek langsung dari PRD.md section 3 sebelum menyentuh kode: gate itu eksplisit mengharuskan konfirmasi founder sebelum Task 5 (Pathway onboarding) dimulai, dan Task 5 sudah SELESAI sejak awal sesi ini (section 9) — jadi gate yang sama sudah lama terlewati, tidak perlu ditanya ulang.

**Apa yang dibangun**: `completionType` KETIGA (`"practice-test"`, di samping `"structured-physical"`/`"reflective"`) — dipakai untuk goal belajar terukur (contoh founder: "IELTS band 6.5"), digeneralisasi bukan di-hardcode ke IELTS (IELTS cuma referensi format soal di prompt AI, bukan di skema/kode). Bukan refleksi teks, bukan record fisik — soal beneran yang dinilai otomatis benar/salah, evidence paling objektif dari semua completionType yang ada (lebih kuat dari growth-gate kespesifikan Task 7, karena bukan evaluasi kualitatif sama sekali).

**Alur pengguna**: tap "Mulai" pada quest `practice-test` → pilih Reading/Listening → pilih Academic/General Training → AI generate bacaan/skrip + soal (SATU panggilan API, termasuk kunci jawaban dan pembahasan singkat per soal, supaya penilaian tidak butuh panggilan tambahan) → user jawab semua soal (campuran multiple-choice, True/False/Not Given, isian singkat) → submit → skor + pembahasan soal yang salah tampil di kartu hasil yang sama dengan quest lain (`completedResultCardHTML`, bukan layar terpisah).

**Backend**:
- `server/practiceTest.js` (modul baru, pola sama dengan `structured.js`/`targets.js` — deterministik, testable tanpa boot server): `cleanPayload(kind, raw)` memvalidasi/membersihkan JSON dari AI (drop soal individual yang cacat daripada membuang seluruh bacaan yang sudah bagus, gagal total ke `null` cuma kalau kurang dari 3 soal tersisa — memaksa fallback); `stripAnswers(payload)` yang menghasilkan bentuk aman-dikirim-ke-client (tanpa `correctAnswer`/`explanation`); `gradeAnswers(questions, answers)` case/whitespace-insensitive, deterministik, tidak pernah tanya AI lagi soal benar/salah.
- `server/claude.js`: `generatePracticeTest(ctx)` + `fallbackPracticeTest` (konten statis keyless, jujur generik seperti fallback lain). `generateQuest`'s prompt diperluas: `completionType` enum tambah `"practice-test"`, dengan instruksi eksplisit HANYA pilih itu kalau goal secara eksplisit soal ujian/tes/sertifikasi terukur (jarang muncul, bukan default). `normalizeCompletionType` diperluas menangani `practice-test` (structuredKind selalu null). `processReflection`'s `evaluationRules` dapat cabang KETIGA (`ctx.practiceTestResult`) — statDeltas WAJIB diisi wajar berapa pun skornya (objektif, bukan kualitatif, jadi tidak butuh gate kespesifikan Task 7 di jalur ini).
- `server/db.js`: kolom baru `character_state.practice_test JSONB` (keyed goalIndex seperti `goal_targets`, tapi SENGAJA kolom terpisah — `goal_targets` itu "target yang dikejar lalu diganti", ini "level yang cuma naik, tidak pernah reset") berisi `{level, history: [{ts,testKind,track,score,total}]}` (history di-cap 20 entri); kolom baru `days.practice_test_payload JSONB` — **sengaja TERPISAH dari kolom `quest`** karena `quest` dikirim mentah ke client di setiap `GET /api/state` (lewat `rowToQuest`) — taruh kunci jawaban di situ akan bocor ke browser di antara generate dan submit. Kolom ini tidak pernah diseleksi `rowToQuest`, cuma dibaca langsung oleh route submit.
- `server/index.js`: dua route baru, `POST /api/practice-test/generate` (baca level/history goal dari `practice_test`, panggil AI, simpan payload lengkap ke `days.practice_test_payload`, balas versi `stripAnswers`) dan `POST /api/practice-test/submit` (baca payload tersimpan, grade deterministik, panggil `ai.processReflection` dengan `ctx.practiceTestResult` supaya mentorReply/statDeltas tetap natural seperti quest lain, growth SELALU eligible tanpa gate, simpan `reflection.practiceTestResult` ke `days` yang sama dengan quest lain — quest jadi selesai lewat mekanisme `saveReflection` yang persis sama, jadi Riwayat/chapter-advance/growth-sessions semuanya otomatis konsisten). Level goal naik +1 di SETIAP submit apa pun skornya ("otomatis lebih sulit sedikit, TIDAK reset ke level dasar" — permintaan founder eksplisit), history diappend. **Sengaja BUKAN lewat `POST /api/reflection`**: alurnya (pilih kind → pilih track → generate → jawab → submit) cukup beda dari flow refleksi teks/structured-physical sehingga menumpangkannya di situ akan menambah percabangan yang lebih mengaburkan daripada dua route baru yang fokus.

**Frontend** (`public/app.js`): `practiceTestFlow` state terpisah total dari `reflectOpen`/`recordMode` (tap "Mulai" pada quest `practice-test` cabang di awal handler, tidak pernah masuk form refleksi biasa). Step "kind" → "track" → generate (full-screen spinner, pola sama dengan `dismissCompleted`) → "test" (render bacaan/skrip + daftar soal per tipe: `mc`/`tf` = tombol pilihan reuse `.status-btn`, `fill` = input teks) → submit → hasil masuk ke `completedResult.practiceTest` dan dirender lewat `completedResultCardHTML` yang sudah ada (skor + pembahasan soal salah), lanjut "Lanjut" dismiss standar. `speakScript(text)` untuk Listening pakai Web Speech API browser murni (`SpeechSynthesisUtterance`, `rate:0.95`) — TIDAK ada TTS berbayar/aplikasi native, tombol "Putar" dibatasi maksimal 2x sesuai format IELTS asli lalu nonaktif. **Tidak ada CSS baru** — seluruh UI reuse class yang sudah ada (`.quest-card`, `.status-row`/`.status-btn`, `.field`, `.mono`, `.eyebrow`) supaya scope tetap sempit sesuai instruksi founder ("kerjakan CUMA fitur ini").

**Diverifikasi**:
- Unit test (`server/practiceTest.js` + `claude.js` langsung via `require`): `cleanPayload` menerima payload valid, default opsi tf ke True/False/Not Given, drop soal individual cacat sambil pertahankan sisanya (jatuh ke `null`/fallback kalau kurang dari 3 tersisa), tolak passage/script kosong; `stripAnswers` membuang `correctAnswer`/`explanation`; `gradeAnswers` case/whitespace-insensitive dan melaporkan detail soal salah dengan benar; `generatePracticeTest` fallback (reading & listening) menghasilkan payload yang lolos `cleanPayload`nya sendiri; `fallbackQuest` tetap TIDAK PERNAH menghasilkan `practice-test` (sama seperti `structured-physical` tidak pernah — practice-test cuma reachable lewat AI sungguhan atau injeksi DB langsung).
- Playwright (server lokal, quest `practice-test` diinjeksi lewat SQL langsung — sama teknik verifikasi Fokus 0, karena fallback keyless tidak pernah memilih tipe ini): alur Reading lengkap (kind→track→soal→jawab→submit→hasil skor+mentorReply tampil, quest hilang dari `openQuests`, masuk Riwayat, `reflection.practiceTestResult` tersimpan benar, `practice_test.level` naik 1→2 dengan history terisi); alur Listening (tombol Putar muncul tanpa kotak bacaan, dibatasi 2x lalu nonaktif); tombol Batal dari step manapun kembali bersih ke dashboard normal tanpa sisa state; validasi "jawab semua soal dulu" tampil inline tanpa submit prematur; **respons `/api/practice-test/generate` dicek eksplisit TIDAK pernah mengandung `correctAnswer`/`explanation`** (bukti kunci jawaban tidak bocor ke client sebelum submit).
- Regresi: quest carousel redesign (32 assertion), Task 9 Sub-Pathway card path, Fokus 2.1 pace live, Fokus 2.2/2.3 Target Berikutnya picker — semua tetap hijau, tidak ada dampak silang dari `completedResultCardHTML`/click-handler yang diperluas.

**Gap yang diketahui, dicatat bukan ditutup** (sama pola dengan gap Task 7/Task 9 Sub-Pathway sebelumnya): tanpa `ANTHROPIC_API_KEY` lokal, `processReflection` selalu jatuh ke `fallbackReflection()` yang me-return `statDeltas: {}` kosong TERLEPAS dari eligibility — jadi growth practice-test yang "selalu eligible" cuma bisa diverifikasi secara PROMPT (branch `ctx.practiceTestResult` di `evaluationRules`), bukan end-to-end lewat AI sungguhan, sampai key aktif di production untuk smoke-check founder. Kualitas suara Web Speech API robotic/bervariasi antar device — known limitation versi pilot per spesifikasi founder sendiri, bukan bug; upgrade ke TTS berbayar (mis. ElevenLabs) eksplisit Phase 3/4, bukan syarat sekarang.

## 21. Task 10 — Artifacts library + Job Match Analysis (SELESAI)

Sumber: `Eleva_PRD.pdf` section 14 (dokumen master founder, upload 11 Agustus). Sama seperti Task 9, di-gate di belakang Task 2/3 (data pribadi sensitif per-user — CV berisi identitas & riwayat kerja) — gate itu diverifikasi sudah lama terpenuhi dengan cara yang sama seperti Task 9 (Task 5 sudah SELESAI di awal sesi, dan Task 5 mensyaratkan gate yang sama persis sebelum boleh mulai).

**Task 10a — Artifacts library (infrastruktur umum, dibangun duluan sesuai urutan spek):**
- Tabel Postgres baru `artifacts` (`user_id`, `type`, `content` jsonb, `uploaded_at`, `updated_at`) — generik dari awal (bukan cuma "tabel CV"), `type` bebas string (`cv` yang dipakai nyata sesi ini, `portfolio`/`certificate`/`other` sudah didukung skema tanpa migrasi lagi kalau dipakai nanti).
- `content` dua bentuk: `{kind:"text", text}` (hasil ekstraksi DOCX — Claude API TIDAK menerima `.docx` mentah, jadi diekstrak SEKALI saat upload lewat `mammoth`, bukan diekstrak ulang tiap quest butuh) atau `{kind:"file", mimeType, dataBase64, filename}` (PDF/foto — dikirim apa adanya ke Claude, PDF didukung native sebagai content block `document`).
- Icon "🗎" baru (`public/styles.css` `.artifacts-btn`, posisi sebaris dengan icon bantuan "?" Task 8 tapi terpisah — spek eksplisit menyerahkan keputusan lokasi ke implementasi) → sheet reuse pola visual `.help-overlay`/`.help-sheet` yang sudah ada (bukan komponen baru): daftar artifact tersimpan (preview singkat + tombol "Ganti"), form tambah baru. Bisa dibuka kapan pun dari dashboard, tidak terkunci ke satu quest.
- **Endpoint**: `GET /api/artifacts` (listing metadata-only — `db.js` `stripArtifactPreview` sengaja membuang `dataBase64` dari respons list, cuma `filename`/`mimeType`/cuplikan teks; alasan sama dengan practice-test's kunci jawaban: tidak ada alasan mengirim byte file penuh di setiap pengecekan rutin "CV sudah ada belum"), `POST /api/artifacts` (tambah baru), `POST /api/artifacts/:id/replace` (ganti, discoped `WHERE user_id` jadi mustahil user lain menimpa artifact orang lain walau menebak id).
- **Alur integrasi ke quest**: quest yang butuh artifact tertentu cek dulu ke library (client-side, sebelum render form) — kalau BELUM ada, tawarkan upload (tersimpan permanen ke Artifacts, bukan cuma untuk quest itu); kalau SUDAH ada, langsung pakai tanpa nanya ulang. Diverifikasi eksplisit: quest job-match-analysis KEDUA untuk user yang sama langsung lompat ke upload lowongan, skip step upload CV sepenuhnya.

**Task 10b — Job Match Analysis (completionType baru, pakai Artifacts):**
- `completionType` KEEMPAT: `"job-match-analysis"` (di samping `structured-physical`/`reflective`/`practice-test`). `generateQuest`'s prompt diperluas lagi (enum + instruksi eligibility eksplisit-restraint sama gaya seperti practice-test — HANYA kalau goal eksplisit soal melamar/mencari kerja, kalau ragu jangan pilih). `normalizeCompletionType` diperluas menangani tipe ini (structuredKind selalu null, sama seperti practice-test).
- **Alur**: tap "Mulai" → cek Artifacts (CV ada?) → kalau belum, upload (PDF/DOCX/foto) → user upload SATU ATAU BEBERAPA screenshot lowongan sekaligus (spek eksplisit: posting nyata sering kepanjangan buat satu layar, contoh founder dari Upwork butuh 4 screenshot) → submit → AI baca CV + semua screenshot dalam SATU panggilan multimodal → hasil terstruktur.
- **`server/jobMatch.js`** (modul baru, pola sama `structured.js`/`practiceTest.js` — separasi deterministik/AI): `prepareArtifactContent` (validasi mimeType/ukuran + ekstraksi DOCX via mammoth saat upload), `buildCvContentBlocks` (artifact tersimpan → content block Claude yang benar: `text` untuk DOCX-terekstrak, `document` untuk PDF, `image` untuk foto CV), `buildImageBlocks` (screenshot lowongan → array block `image`, dibatasi maks 6, ditolak kalau kosong/format salah), `cleanJobMatchResult` (validasi output AI — drop baris `matchTable` yang cacat individual daripada buang seluruh analisis, gagal ke `null`/fallback kalau matchTable-nya kosong sama sekali).
- **`server/claude.js`**: `generateJobMatchAnalysis(ctx)` — SATU-SATUNYA fungsi `generate*` di file ini yang multimodal (content berupa ARRAY block, bukan string JSON.stringify seperti semua caller lain) — `callClaude` ternyata sudah cukup generik untuk ini tanpa perlu diubah (dia cuma meneruskan `content` apa adanya ke `messages[0].content`, entah string atau array). Prompt menegaskan prinsip **anti-sycophancy** eksplisit dari Bible: verdict harus jujur (3 kategori: siap apply / bisa apply tapi perkuat X / gap besar fokus bangun X dulu), dan WAJIB cek relevansi lowongan ke GOAL pengguna (bukan cuma ke CV) — kalau lowongan yang di-screenshot user TIDAK nyambung ke goal (contoh spesifik founder: goal "data analyst" tapi lowongan "data entry" — bertetangga tapi beda), AI wajib bilang jujur, bukan diam-diam dianggap sama atau asal "match, apply aja!".
- **`server/index.js`**: `POST /api/job-match/analyze` — cek CV artifact milik user (bukan dari body yang bisa dipalsukan, tapi `db.getArtifactById` yang discoped `user_id`), panggil AI, tandai quest selesai lewat mekanisme `saveReflection` yang SAMA dengan completionType lain (jadi Riwayat/growth-sessions otomatis konsisten). **Growth DETERMINISTIK, bukan panggilan AI kedua**: beda dari practice-test (yang reuse `processReflection` untuk komentar mentor natural), di sini `verdict`/`relevanceNote`/`nextStep` dari `generateJobMatchAnalysis` SENDIRI sudah teks naratif spesifik yang cukup jadi "komentar mentor" — panggilan `processReflection` kedua cuma akan jadi biaya API tambahan tanpa informasi baru, jadi delta stat dihitung langsung di kode (+3 ke `statFocus` quest, default `livelihood` kalau statFocus tidak ada/tidak valid). **Catatan jujur soal spesifikasi**: DoD founder untuk Task 10b tidak menyebut growth/statDeltas sama sekali (beda dari Task 9 yang eksplisit membahasnya) — keputusan menerapkan growth di sini adalah interpretasi saya sendiri, konsisten dengan SEMUA completionType lain di app ini yang menaikkan growth saat selesai, dicatat di sini biar transparan itu bukan requirement eksplisit founder.
- `express.json()` body limit dinaikkan dari default 100kb ke 20mb (`server/index.js`) — CV/screenshot base64 jauh lebih besar dari payload JSON biasa di app ini; satu-satunya perubahan yang mempengaruhi SEMUA route (tapi tanpa efek untuk route lain yang memang selalu kecil).

**Diverifikasi**:
- Unit test (`server/jobMatch.js` + `claude.js` via `require`, termasuk ekstraksi DOCX SUNGGUHAN pakai file fixture asli dari `node_modules/mammoth/test/test-data/` — bukan mock): `prepareArtifactContent` menerima DOCX asli dan benar-benar mengekstrak teksnya (bukan stub), menyimpan PDF sebagai `kind:file` apa adanya, menolak mimeType tak didukung & file kosong; `buildCvContentBlocks` menghasilkan block yang benar untuk ketiga bentuk (text/document/image); `buildImageBlocks` menerima 1-6 gambar, menolak 0/>6/mimeType salah; `cleanJobMatchResult` menerima hasil valid, drop baris matchTable cacat individual, tolak hasil tanpa verdict/matchTable kosong; `generateJobMatchAnalysis` fallback keyless menghasilkan struktur valid & jujur soal keterbatasannya; `fallbackQuest` tetap tidak pernah menghasilkan `job-match-analysis` (sama presedennya dengan `structured-physical`/`practice-test`).
- Playwright (server lokal, quest diinjeksi SQL jadi `job-match-analysis` — fallback keyless tidak pernah memilih tipe ini sendiri): upload CV (DOCX asli via `mammoth`'s sendiri test fixture) → langsung lanjut ke upload lowongan tanpa step tambahan; upload 3 screenshot sekaligus → counter benar, tombol submit aktif; submit → layar hasil tampil tabel kecocokan + langkah berikutnya; quest hilang dari `openQuests`, masuk Riwayat, `reflection.jobMatchResult` tersimpan, delta stat diterapkan; **quest KEDUA untuk goal yang sama langsung skip ke upload lowongan** (CV dari library dipakai ulang, bukti alur "cek dulu, jangan tanya ulang" bekerja); sheet Artifacts menampilkan CV yang baru diupload + tombol Ganti berfungsi; **respons `GET /api/artifacts` dicek eksplisit tidak mengandung byte file mentah** (bukti list metadata-only bekerja, bukan cuma diklaim).
- Regresi: practice-test (Reading+Listening penuh), redesign onboarding (32 assertion), Fokus 2.2/2.3 Target Berikutnya — semua tetap hijau; `express.json()` limit yang dinaikkan tidak berdampak ke route lain manapun.

**Gap yang diketahui, dicatat bukan ditutup** (pola sama dengan gap AI-dependent lain di sesi ini): analisis multimodal SUNGGUHAN (AI benar-benar membaca screenshot lowongan & CV, termasuk kasus "posting tidak relevan ke goal" dari contoh founder) tidak bisa diverifikasi tanpa `ANTHROPIC_API_KEY` asli — perlu smoke-check manual founder di production begitu key aktif, SAMA seperti gap yang sudah dicatat untuk Task 7/Task 9. Belum ada tombol hapus artifact (spek cuma minta "lihat, tambah, atau ganti", jadi tidak dibangun proaktif). Tipe `portfolio`/`certificate` didukung skema & label UI tapi belum ada quest completionType nyata yang memakainya — infrastruktur Task 10a memang dibangun generik duluan sesuai urutan spek, pemakaian nyatanya menyusul kalau ada task berikutnya yang butuh.

## 22. Redesign Homepage/Dashboard (design handoff, 11 Agustus): nav shell 4-layar + Eleva Observed + Kondisi Hari Ini + Character/Kisahmu terpisah dari Home (SELESAI)

Sumber: `Eleva_Design_Token_Audit_2.zip` founder ("Ada redesign ui/ux") — README handoff high-fidelity + prototype `.dc.html` + screenshot. Beda dari Task 12 (section 19) yang murni restyle 3 layar onboarding tanpa ubah data model: handoff ini secara eksplisit meminta restrukturisasi INFORMASI, bukan cuma visual — 7 stat pindah dari Home ke layar baru, riwayat chapter jadi autobiografi kronologis di layar baru, dan dua konsep baru (Eleva Observed, Kondisi Hari Ini) yang belum ada representasi datanya sama sekali di backend.

**Dua ambiguitas asli, diselesaikan lewat `AskUserQuestion` sebelum mulai coding** (bukan ditebak — keduanya menyentuh keputusan produk yang sudah pernah dikunci sebelumnya di PRD.md/README.md ini):
- **Side Quest** bertentangan langsung dengan keputusan out-of-scope eksplisit yang sudah ada ("Side/Social/Career/Exploration quest (baru Main Quest)" — README.md bagian "Belum ada"). Founder memilih **"Placeholder visual saja"**: baris "→ N Side Quest tersedia" murni kosmetik (`slots = max(0, 3 - jumlah goal aktif)`), tap membuka pesan eksplisit "belum tersedia", TIDAK ada generasi quest bonus sungguhan.
- **Decay** (banner "Decay dijeda" + tag "trending down" per stat) ditunjukkan di mockup tapi tidak pernah dispesifikasikan mekanismenya — dan app ini sejak awal punya prinsip anti-Goodhart keras "stat cuma naik lewat refleksi nyata, tidak pernah otomatis turun". Founder memilih **"Kosmetik dari data yang ada"**: trending down murni SINYAL TURUNAN dari histori nyata (dibandingkan jumlah growth-delta 7 hari terakhir vs 7 hari sebelumnya), TIDAK PERNAH ada pengurangan stat otomatis di kode manapun.

**Backend**:
- `server/db.js`: kolom baru `character_state.kondisi_status` (default `'Normal'`), `kondisi_updated_at`, `chapter_narrative` (paragraf naratif chapter AKTIF, terpisah dari `chapter_title` yang cuma judul pendek — di-seed teks default untuk akun lama), `observed JSONB` (nullable). Tabel baru `chapters` (arsip chapter yang SUDAH LEWAT — `chapter_number`/`chapter_title`/`narrative`/`created_at`) untuk layar Kisahmu. `updateState` diperluas dengan pola **COALESCE-preserve** untuk `chapter_narrative`/`observed`: route yang tidak punya data segar untuk dua field ini (mayoritas route) otomatis mempertahankan nilai lama alih-alih menimpanya dengan `null` — kebalikan dari pola `chapter_title`/`pathway_noun` yang harus selalu di-pass eksplisit. Fungsi baru `updateKondisi`, `resetKondisiToNormal`, `archiveChapter`, `listChapters`.
- `server/claude.js`: `generateQuest`'s kontrak JSON diperluas dengan field opsional `observed: {yesterday, noticed, today} | null` (3-kalimat rantai kausal "kemarin kamu X → Eleva notice Y → hari ini coba Z") — **ditumpangkan ke panggilan generate quest yang sudah ada, BUKAN panggilan API baru**, demi disiplin biaya yang sama seperti prinsip sesi-sesi sebelumnya. Validasi dua-lapis (pola sama dengan `normalizeCompletionType`): field yang malformed di-patch jadi `null`, bukan menggagalkan seluruh quest generation. `processReflection`'s kontrak diperluas dengan `newChapterNarrative: string | null` (paragraf 2-4 kalimat, WAJIB diisi kalau `chapterAdvance` true) — dipakai layar Kisahmu.
- `server/index.js`: route baru `POST /api/kondisi` (validasi terhadap 6 label tetap + "Normal", simpan). **Reset harian lazy** (pola sama dengan resonance-check pathway yang sudah ada, bukan cron job): `GET /api/state` membandingkan `todayKey(kondisiUpdatedAt)` vs `todayKey()` — begitu hari kalender berganti, kondisi otomatis balik ke "Normal" di request pertama hari itu. Perhitungan `statTrends` (tiap stat, jumlah delta 7 hari terakhir vs 7 hari sebelumnya dari `allHistory`, `true` kalau turun) — MURNI derivasi baca-saja, tidak menulis apa pun ke `stats`. Helper `archiveChapterIfAdvancing` dipanggil di KEDUA titik pemicu chapter-advance yang sudah ada (`/api/reflection`, `/api/practice-test/submit`) SEBELUM `character_state` ditimpa dengan chapter baru — mengarsipkan snapshot chapter yang OUTGOING ke tabel `chapters`. `/api/job-match/analyze` sengaja TIDAK jadi pemicu ketiga (konsisten dengan keputusan sesi sebelumnya: growth-nya deterministik, tidak ada evaluasi AI kedua yang bisa memutuskan chapter-advance).

**Frontend** (`public/app.js`, `public/styles.css`): shell navigasi baru — header persisten (tanggal + avatar + gear settings) dan tab bar bawah (Home/Kisahmu/Character/Settings, state `activeScreen`) di keempat layar. **Home** disusun ulang: chapter header jadi ringkas (bukan blok besar seperti sebelumnya), kartu "Eleva Observed" (muncul kalau `observed` ada), Primary Quest cards dengan **disclosure progresif** untuk alasan AI (`reasonOpenIds`, Set per-id — tombol "Kenapa Eleva kasih quest ini →" menyingkap `why` yang tadinya SELALU tampil), baris Side Quest placeholder, blok Kondisi Hari Ini (ringkasan tap-untuk-buka picker 6 label, commit sekali-tap langsung tanpa tombol konfirmasi terpisah — pola konsisten dengan keputusan checkbox-dihapus Task 12), Riwayat tidak berubah. **Character** (layar baru): 7 stat dipindah dari Home ke sini, ditampilkan sebagai `Lv.1-5 · {TierName}` (`statLevelInfo` — 5 tier 20 poin per level, nama tier "Vigil" set dari 3 draft yang diajukan founder: Initiate→Wanderer→Adept→Sentinel→Sovereign, progress bar mengisi relatif ke DALAM band tier, bukan skala mentah 0-100) menggantikan tampilan lama "% complete" mentah, dengan tag kosmetik "↘ menurun" per stat kalau `statTrends[k]` true, dan banner "Decay dijeda" saat `kondisiStatus !== "Normal"`. **Kisahmu** (layar baru): daftar kronologis satu entri per chapter (`s.chapters` — arsip dari tabel `chapters` + chapter aktif saat ini di akhir), tiap entri paragraf naratif penuh (bukan cuma judul pendek seperti dulu). **Settings** (layar baru, placeholder): Profil/Notifikasi/Privasi & Data berlabel "segera hadir" (disabled, tidak fake-navigable), plus tombol Reset dan Keluar yang dipindah ke sini dari lokasi lamanya.

**Bug visual ditemukan & diperbaiki lewat review screenshot langsung** (bukan dari test otomatis — assertion Playwright semua lolos duluan tanpa menangkap ini): icon bantuan "?" dan Artifacts "🗎" (`position:absolute` dengan offset vertikal tetap dari era pra-header) bertabrakan visual dengan `.banner-warn`/kartu Eleva Observed begitu header persisten baru mendorong konten ke bawah dengan tinggi yang bervariasi. Fix: kedua icon dipindah jadi elemen INLINE di dalam `.header-icons` (baris flex header, hanya dirender saat `activeScreen === "home"`), dengan override CSS terskop `.header-icons .help-btn { position: static; ... }` supaya fungsi `helpBtnHTML()` yang SAMA tetap dipakai apa adanya oleh layar onboarding (radar/kartu/pathway/goal-capture, yang masih floating absolute seperti semula, tidak disentuh sama sekali oleh redesign ini).

**Diverifikasi** (Playwright viewport 390px, 22 assertion + 10 unit test langsung ke `server/db.js`): header persisten (tanggal+avatar+gear) dan tab bar 4-item dengan Home aktif default; chapter header ringkas tanpa judul besar terpisah; label kartu "PRIMARY QUEST"; `why` tersembunyi default, tampil setelah tap toggle; baris Side Quest menampilkan hitungan yang benar (`3 - goalCount`) dan meluas ke pesan "belum tersedia" eksplisit saat ditap; Kondisi Hari Ini default "Normal", 6 chip pilihan, tap sekali langsung commit dan menutup kembali ke ringkasan; layar Character menampilkan 7 baris level format `Lv.N · TierName`, banner decay-paused muncul kalau kondisi ≠ Normal, TIDAK ada blok stat 0-100 mentah tersisa; layar Kisahmu menampilkan minimal 1 entri chapter dengan narasi asli (bukan kosong); layar Settings menampilkan 4 baris (3 placeholder "segera hadir" + Reset) dan `#armReset`; gear header mengarahkan ke Settings; kondisi lazy-reset ke Normal terverifikasi lewat backdate `kondisi_updated_at` + `GET /api/state` fresh. Unit test langsung `server/db.js`: akun baru ter-seed `chapter_narrative` default, `listChapters` kosong di awal, `archiveChapter` + `updateState` bareng menghasilkan tepat 1 entri arsip berisi chapter OUTGOING (bukan yang baru), `character_state` mencerminkan chapter baru setelahnya, round-trip `updateKondisi`/`resetKondisiToNormal`, dan `observed` PERSIST (tidak ter-clear) saat `updateState` dipanggil tanpa parameter itu (bukti pola COALESCE-preserve bekerja). Regresi: practice-test, job-match, Fokus 2.2/2.3 Target Berikutnya, redesign onboarding Task 12 (32 assertion) — semua tetap hijau, tidak ada dampak silang dari perubahan shell navigasi terhadap layar-layar lama.

**Gap yang diketahui, dicatat bukan ditutup**: kualitas isi "Eleva Observed" dan `newChapterNarrative` yang SUNGGUHAN (AI benar-benar menyusun rantai kausal yang masuk akal dari histori nyata pengguna) tidak bisa diverifikasi tanpa `ANTHROPIC_API_KEY` asli — jalur fallback keyless yang teruji cuma memvalidasi mekanisme patch/COALESCE-nya, bukan kualitas naratifnya; perlu smoke-check founder di production, pola gap yang sama dengan Task 7/9/10 sebelumnya. Side Quest masih murni placeholder visual (by design, dari jawaban AskUserQuestion) — belum ada mekanisme bonus-quest sungguhan. "Trending down" per stat baru dites lewat data sintetik (delta dimasukkan manual), belum lewat histori 7-hari-vs-7-hari pengguna asli yang benar-benar menunjukkan pola menurun.

**Catatan susulan (11 Agustus, setelah `Eleva_PRD.pdf` master di-upload ulang lebih lengkap): Side Quest placeholder dan Decay kosmetik di atas SUDAH DIGANTIKAN jadi fitur sungguhan — lihat section 23 di bawah.** Master PRD yang lebih detail ternyata sudah menspesifikasikan kedua mekanisme itu penuh (termasuk pengaman Decay) — placeholder/kosmetik di section ini adalah keputusan yang tepat SAAT ITU (zip design handoff tidak menjelaskan mekanismenya), bukan salah, tapi sekarang sudah tidak akurat menggambarkan kode.

## 23. Task 7c (Quest Flow revisi) + formalisasi Context Update (11f) + Decay sungguhan (11e) + Side Quest sungguhan (11c) — dari `Eleva_PRD.pdf` master + `UPDATE_PROMPT.pdf` (11 Agustus) (SELESAI P0+sebagian P1)

**Konteks & sumber**: dua dokumen founder di-upload bersamaan — `UPDATE_PROMPT.pdf` (peta cepat "apa yang perlu dikerjakan sesi ini", eksplisit BUKAN otoritatif kalau bertentangan sama master) dan `Eleva_PRD.pdf` (master PRD, lebih detail & otoritatif). Cross-check: hampir semua "Fokus 1-5" di `UPDATE_PROMPT.pdf` ternyata SUDAH selesai di sesi-sesi sebelumnya (Fokus 1 = model quest per-goal section 14, Fokus 2 = pace+Target Berikutnya section 15, Fokus 3/4 = icon bantuan+growth-gate section 16/section "Task 7" v15, Fokus 5 = Practice Test section 20) — kecuali **"Fokus 0.5" (Task 7c, Quest Flow revisi)**, yang genuinely belum pernah dibangun. Membaca `Eleva_PRD.pdf` lebih lanjut juga mengungkap bahwa Task 11 (Homepage Redesign, section 22 di atas) yang sudah dikerjakan berdasarkan zip design handoff ternyata cuma sebagian dari spek master yang lebih lengkap — dua gap besar: Decay (11e) dan Side Quest (11c) dispesifikasikan penuh di master, bukan lagi placeholder/kosmetik seperti keputusan AskUserQuestion round sebelumnya.

**Keputusan Decay ditinjau ulang lewat AskUserQuestion kedua kalinya**: karena master PRD sekarang menjelaskan mekanisme Decay LENGKAP (termasuk pengaman pause) — beda dari round sebelumnya yang cuma tahu Decay disebut di mockup tanpa mekanisme — founder ditanya ulang apakah mau membangun Decay sungguhan sesuai spek baru, atau tetap kosmetik seperti keputusan round lalu. Founder memilih **Decay sungguhan**. Side Quest TIDAK ditanya ulang — section 16 master PRD sudah eksplisit mengklarifikasi bahwa "Side Quest" di sana cuma soal LABEL tampilan Primary/Side pada mekanisme Main Quest yang sudah ada (bukan tipe quest baru), jadi tidak bertentangan dengan keputusan out-of-scope "Side/Social/Career/Exploration quest sebagai tipe terpisah" yang sudah lama berdiri.

### Task 7c — Quest Flow: dari form ke evidence yang auto-dinilai

**Masalah inti**: alur "Mulai" pada quest fisik masih menanyakan ulang hal yang sudah jelas dari quest-nya sendiri ("Aktivitasnya jenis apa? Cardio/Gym...") padahal AI yang men-generate quest itu sudah tahu jawabannya, dan completion masih self-report (Selesai/Sebagian/Nggak sempat) padahal seharusnya bisa dihitung otomatis dari angka.

**P0 (wajib sebelum beta), semua selesai:**
1. **`evidenceSchema` ditentukan saat quest digenerate, bukan ditanya ulang.** `server/claude.js`'s `generateQuest` sekarang menyertakan `evidenceSchema: {activityType, hasWeight, metricType, target} | null` di response — untuk cardio: `activityType` salah satu dari 5 pilihan tetap + `metricType:"distance"` + `target` (km, diselaraskan ke `ctx.currentTarget` kalau goal itu sudah punya target persisten dari Task 7b); untuk gym: `hasWeight` boolean (AI membaca apakah quest menyebut alat) + `metricType:"reps"` + `target` (repetisi/set). Dinormalisasi server-side (`normalizeEvidenceSchema`, pola sama dengan `normalizeCompletionType` — malformed di-patch ke `null`, bukan gagal total). Frontend (`public/app.js`, handler `[data-reflect-id]`): begitu quest punya `evidenceSchema.metricType` yang valid, `structKind` di-auto-set (`structKindAuto=true`) dan picker "Aktivitasnya jenis apa?" (`kindPickerHTML`) di-skip total — form langsung ke field yang relevan, `jenisAktivitas` pre-filled dari `activityType`. Quest lama/fallback keyless (tidak punya `evidenceSchema`) tetap fallback ke picker lama — degradasi anggun, bukan pecah.
2. **Completion (selesai/sebagian) dihitung otomatis dari evidence vs target, bukan dipilih user.** `server/index.js`'s `computeEvidenceStatus(quest, structuredClean)`: `actual / evidenceSchema.target >= 0.95` → "done", di bawah itu → "partial"; tanpa target yang bisa dibandingkan (quest lama, atau AI tidak bisa menyimpulkan angka target yang wajar) → "done" tetap begitu evidence disubmit (perilaku pra-Task-7c, degradasi anggun). Picker "Gimana progressnya?" (Selesai/Sebagian/Nggak sempat) DIHAPUS dari form structured-physical (`mustRecord` quest) — tetap ada apa adanya untuk quest reflektif biasa (di luar scope P0 ini). **Jalur "Aku nggak bisa quest ini"** (context signal, BUKAN evidence): tombol baru di form structured-physical membuka `unableFormHTML()` — 5 chip Context Update (lihat 11f di bawah, minus "Energi lebih") + catatan opsional, submit lewat endpoint `/api/kondisi` yang SUDAH ADA (bukan endpoint baru) — quest TETAP terbuka, tidak ada growth, tidak ada evidence tersimpan, murni menyimpan sinyal konteks yang lalu dibaca `generateQuest` berikutnya (lihat 11f).
3. **"Eleva Response" (`interpretation`)** — pasangan simetris dari "Eleva Observed" (yang bicara soal KEMARIN sebelum quest; ini bicara soal HARI INI setelah evidence masuk). `processReflection`'s JSON contract diperluas dengan field `interpretation: string` — 1-2 kalimat yang eksplisit merujuk balik ke angka yang baru disubmit (bukan pujian generik). **Reuse panggilan evaluasi evidence yang SUDAH ADA** (tidak ada panggilan API baru) — field ini datang dari respons `processReflection` yang sama yang sudah menghasilkan `statDeltas`/`mentorReply`. Diteruskan lewat `POST /api/reflection` dan `POST /api/practice-test/submit` (keduanya sudah memanggil `processReflection`; `job-match-analyze` sengaja TIDAK dapat field ini karena growth-nya deterministik tanpa panggilan AI kedua, konsisten keputusan Task 10). Frontend: blok "ELEVA RESPONSE" baru muncul di `completedResultCardHTML`, di ATAS status SELESAI/SEBAGIAN — urutan tampil sesuai spek ("sebelum pindah ke 'quest selesai'").
4. **4 tingkat kesulitan + label diperbaiki.** "Titik mulai berat" (3 opsi: Ringan/Cukup/Berat, label salah — nanya DI MANA mulai berat padahal jawabannya general difficulty scale) → **"Rasanya gimana?"** dengan **4 opsi: Ringan/Cukup/Berat/Terlalu berat**. Refleksi kondisional (field "Apa yang bikin berat?") sekarang muncul untuk BERAT DAN TERLALU BERAT (dulu cuma Berat). `server/structured.js` validasi diperluas menerima 4 nilai; field detail wajib untuk 2 tingkat teratas. Gym's "titik gagal/berat" (konsep beda, sudah didokumentasikan sebelumnya) sengaja TIDAK disentuh — spek cuma menyebut field cardio secara eksplisit.

**P1 (penting, boleh nyusul) — 2 dari 4 item diselesaikan bersamaan karena murah & langsung relevan:**
- **Durasi jadi 2 field terpisah (MENIT/DETIK)** — bug NYATA ditemukan langsung dari testing founder sebelumnya (field MM:SS tunggal ambigu: user ketik "2211", tidak jelas 22 menit 11 detik atau 2211 menit). `structForm.durasiMenit` (string MM:SS) diganti `durasiMin`+`durasiSec` (dua input number terpisah), digabung jadi decimal minutes di SATU titik konversi (`durasiMenitFromFields`, client-side, tepat sebelum dikirim ke server) — semua konsumen lain (pace calc, server validation, target metrics) tidak berubah sama sekali.
- **Target vs Actual ditampilkan berdampingan** — baris "TARGET X km"/"TARGET X repetisi/set" (dari `evidenceSchema.target`) ditampilkan di atas field yang relevan, begitu quest punya evidenceSchema dengan target yang valid.
- **P1 yang SENGAJA belum dikerjakan (boleh nyusul, dicatat sebagai gap)**: "Quest Active" state (layar transisi antara tap "Mulai" dan form evidence — murni psikologis, tidak menambah fungsi baru) dan "Refleksi jadi chip cepat + expand opsional" (mengganti textarea besar dengan chip tap-cepat). Keduanya eksplisit ditandai "boleh nyusul setelah P0" di `UPDATE_PROMPT.pdf`, dan P2 (placeholder "Next Quest: Preparing...", GPS/live tracking beneran) eksplisit "boleh nyusul jauh, TIDAK harus selesai sebelum beta" — GPS sendiri sudah lama jadi roadmap Phase 4/Capacitor, bukan syarat sekarang.

### Task 11f — Context Update: formalisasi "Kondisi Hari Ini" jadi entry point resmi

Fitur "Kondisi Hari Ini" dari redesign sebelumnya (section 22) sudah punya mekanisme dasarnya; master PRD memformalkannya jadi "Context Update" dengan spek lengkap yang sebelumnya belum ada di mana pun:
- **Wording + emoji diselaraskan ke spek**: `KONDISI_LABELS` direvisi ("Capek" → "Capek/energi rendah", "Sibuk berat" → "Beban kerja tinggi", lainnya tetap) + `KONDISI_EMOJI` map (🥵😷💼✈️🧠🔥) ditambahkan di kedua sisi (`server/index.js`, `public/app.js`, hand-sync yang sama seperti sebelumnya). String lama yang sudah tersimpan di akun existing tetap tampil apa adanya sampai diupdate lagi — tidak ada migrasi data, sama presedennya dengan rename Pathway di Task 9.
- **Note opsional** — skema 11f `{status, note, timestamp}`: kolom baru `character_state.kondisi_note TEXT` (nullable), diisi lewat textarea "Ceritakan lebih (opsional)..." di picker yang sama, dikirim bareng status dalam satu request `POST /api/kondisi` (diperluas menerima `note`). Betul-betul opsional (tidak wajib, bisa diupdate kapan saja) sesuai DoD.
- **Feed ke `generateQuest` (WOOP-Obstacle tie-in)**: `GET /api/state`'s needySlots loop sekarang menyuntikkan `ctx.kondisiStatus`/`ctx.kondisiNote` ke context quest generation (hanya kalau bukan "Normal") — prompt AI diinstruksikan menurunkan intensitas/skala quest hari itu secara wajar, dianggap bagian dari Obstacle di kerangka WOOP yang sudah ada (bukan mekanisme terpisah).
- **Pengaman buat Decay** (lihat 11e di bawah) — fungsi kedua dari data yang sama, tanpa skema tambahan.

### Task 11e — Decay: mekanisme NYATA (bukan lagi kosmetik)

**Perubahan filosofi yang eksplisit ditinjau ulang lewat AskUserQuestion**: keputusan round sebelumnya ("kosmetik saja, stat tidak pernah turun otomatis") DIBALIK di sini — master PRD menjelaskan Decay lengkap termasuk pengamannya, dan founder mengonfirmasi mau versi sungguhan. Prinsip "growth cuma naik lewat refleksi nyata" TETAP berlaku untuk arah NAIK (tidak berubah sama sekali); Decay adalah mekanisme TERPISAH yang mengizinkan arah TURUN dari inaktivitas, dengan pengaman eksplisit yang sama pentingnya dengan mekanisme turunnya sendiri.

- **`character_state.stat_activity JSONB`** (baru): per-stat timestamp kapan stat itu TERAKHIR dapat delta positif nyata — ditulis lewat `db.touchStatActivity(userId, statKeys)`, dipanggil di SEMUA titik yang menerapkan `deltas` ke stats (`POST /api/reflection`, `/api/practice-test/submit`, `/api/job-match/analyze`). Stat yang belum pernah tersentuh jatuh balik ke `pathway_trial_started_at` sebagai anchor (bukan tanggal palsu).
- **`character_state.last_decay_check TIMESTAMPTZ`** (baru): gerbang evaluasi lazy, PALING BANYAK sekali per hari kalender — pola sama persis dengan reset harian Kondisi Hari Ini dan resonance-check pathway (tidak ada cron job).
- **`db.applyDecayIfDue(userId)`**, dipanggil dari `GET /api/state`: kalau sudah dievaluasi hari ini, no-op. Kalau `kondisiStatus !== "Normal"` → **PAUSE WAJIB, NON-NEGOTIABLE** (persis kata founder di spek) — gerbang tetap maju ke "hari ini sudah dicek" tapi TIDAK ADA stat yang berkurang sama sekali, diverifikasi eksplisit lewat unit test. Kalau Normal dan sudah due: tiap stat yang tidak tersentuh ≥4 hari (`DECAY_THRESHOLD_DAYS`) turun TEPAT 1 poin (`DECAY_RATE`) — **cuma satu hari senilai pengurangan per evaluasi, TIDAK PERNAH backlog** walau user absen berminggu-minggu (constraint desain eksplisit: absen lama tidak boleh jadi hukuman besar sekali balik, sesuai instruksi "LAMBAT dan BERTAHAP, bukan drop tiba-tiba, terasa sebagai konsekuensi wajar bukan hukuman"). Ambang (4 hari) dan laju (1 poin) adalah keputusan implementasi sendiri (spek eksplisit menyerahkan detail ini "perlu ditentukan lebih lanjut saat implementasi") — cukup konservatif untuk pilot, bisa disetel ulang tanpa migrasi skema kalau kerasa kurang/kelebihan tajam.
- **Penamaan netral**: label "Decay" dipertahankan (bukan istilah berkonotasi kegagalan moral seperti "Corruption") — sudah sesuai instruksi spek dari awal, tidak perlu istilah baru.
- Character screen (`characterScreenHTML`, sudah ada dari redesign sebelumnya) TIDAK berubah tampilannya — tag "↘ menurun" dan banner "Decay dijeda" yang sudah dibangun sebagai turunan kosmetik dari `statTrends` sekarang benar-benar mencerminkan mekanisme nyata di baliknya, bukan cuma sinyal derivasi baca-saja.

### Task 11c — Side Quest: bonus quest AI sungguhan (bukan lagi placeholder)

**Klarifikasi scope penting** (section 16 master PRD, dibaca eksplisit sebelum implementasi): "Side Quest" di sini murni LABEL tampilan pada mekanisme Main Quest yang sudah ada (satu pemanggilan AI generatif yang sama, cuma tidak terikat goal) — BUKAN izin membangun tipe quest baru (Social/Career/Exploration Quest tetap eksplisit di luar scope, keputusan lama tidak berubah).

- **`server/claude.js`**: `generateSideQuest(ctx)` + `fallbackSideQuest()` — reuse `MENTOR_SYSTEM`/`callClaude`, TAPI prompt sengaja lebih ringan dari `generateQuest` (tidak ada framing WOOP/Goal-vs-Pathway karena tidak ada goal untuk dijangkarkan), SELALU `completionType:"reflective"` (bonus rendah-tekanan, tidak pernah structured-physical/practice-test/job-match-analysis), dan tidak menyentuh chapter/pathwayNoun/observed sama sekali.
- **`server/db.js`**: kolom baru `days.is_side_quest BOOLEAN DEFAULT false` — membedakan Side Quest (goalIndex null, sengaja) dari akun lawas pra-goal-capture yang JUGA punya goalIndex null tapi itu satu-satunya quest mereka (Primary, bukan Side). Tanpa flag ini keduanya tidak bisa dibedakan dan akun lawas akan salah muncul di seksi Side Quest.
- **`server/index.js`**: `GET /api/state` menambah loop kedua setelah loop goal quest yang sudah ada — `slots = max(0, 3 - goals.length)` (0 untuk akun lawas tanpa goal capture, sengaja tidak dapat Side Quest sama sekali), generate quest baru untuk tiap slot yang masih kosong (`openSideCount < slots`) sampai penuh, pola identik dengan needySlots yang sudah ada untuk goal quest.
- **Frontend** (`public/app.js`): `openQuests` dari server dipecah jadi `openQuests` (Primary saja, dipakai carousel/reflect-target seperti sebelumnya) dan `sideQuests` (dipakai `sideQuestRowHTML` yang BARU) — `questSummaryCard` diperluas menerima label "SIDE QUEST" saat `q.isSideQuest`. `sideQuestRowHTML` sekarang merender kartu quest SUNGGUHAN (reuse `questSummaryCard`, tombol "Mulai" berfungsi penuh — completion lewat `/api/reflection` yang sama, tidak ada endpoint baru) menggantikan pesan placeholder "belum tersedia" dari round sebelumnya. Quest yang sedang direfleksikan (kalau kebetulan Side Quest) di-exclude dari daftar collapsed supaya tidak duplikat tampilan dengan kartu aktif di atasnya.

**Diverifikasi** (10 unit test `server/db.js` langsung + 11 API-level test lewat `fetch` langsung ke route + 15 assertion Playwright, semua di luar API key nyata):
- Unit: kondisi note persist/clear bareng status; `touchStatActivity` cuma menyentuh key yang diberikan; Decay no-op di hari yang sama; Decay mengurangi TEPAT 1 poin untuk stat basi (≥4 hari) sambil stat yang baru disentuh TIDAK turun; **Decay PAUSE MUTLAK saat kondisi ≠ Normal** (assertion paling penting — stat benar-benar 0 perubahan); `createQuest` dengan `isSideQuest=true` menyimpan & mengembalikan flag itu lewat `getOpenQuests`.
- API-level: akun 2-goal menghasilkan tepat 1 Side Quest (formula `3-2=1`) + 2 Primary Quest, Side Quest goalIndex null & completionType reflective; quest structured-physical dengan `evidenceSchema.target=3.2` + evidence 3.24km → status "done" otomatis; evidence 1.87km (di bawah 95% target) → status "partial" otomatis (BUKAN dipilih user); response `POST /api/reflection` membawa field `interpretation`; `POST /api/kondisi` dengan note tersimpan & muncul balik di `GET /api/state` dengan wording baru.
- Playwright (viewport 390px): Side Quest row collapsed menunjukkan "2 Side Quest tersedia", expand menampilkan kartu SUNGGUHAN berlabel "SIDE QUEST" (bukan lagi pesan "belum tersedia"); quest structured-physical dengan evidenceSchema TIDAK menampilkan picker "Aktivitasnya jenis apa?" ataupun "Gimana progressnya?"; baris "TARGET 3.2 km" tampil; field Menit/Detik terpisah (bukan satu MM:SS box); tombol 4-tier "Rasanya gimana?" termasuk "Terlalu berat"; tombol "Aku nggak bisa quest ini" membuka picker 5-chip (tanpa "Energi lebih") dan tombol batal kembali ke form evidence tanpa mengganggu apa pun; submit evidence di atas target → "SELESAI" otomatis + blok "ELEVA RESPONSE" muncul; picker Context Update menampilkan 6 chip beremoji dengan wording baru, note tersimpan & muncul di ringkasan.
- Regresi (5 skrip Playwright lama, disesuaikan seperlunya untuk perubahan UI yang memang disengaja — bukan bug): Practice Test, Job Match Analysis, redesign onboarding Task 12, dan redesign homepage Task 22 (semua ASSERT lolos setelah 2 baris ekspektasi lama disesuaikan ke wording/perilaku baru yang memang disengaja: pesan Side Quest placeholder → kartu sungguhan, label kondisi "Capek" → "Capek/energi rendah"). Skrip Fokus 0 lama (durasi MM:SS satu field, titik-berat 3-opsi) diupdate ke field Menit/Detik terpisah dan 4-opsi — semua ASSERT lolos setelah penyesuaian, termasuk pace calculation dari dua field terpisah tetap akurat.

**Gap yang diketahui, dicatat bukan ditutup**: P1 items "Quest Active state" dan "Refleksi jadi chip cepat" sengaja belum dikerjakan (eksplisit "boleh nyusul" di spek, prioritas P0 dulu). Ambang & laju Decay (4 hari / 1 poin) adalah pilihan implementasi sendiri, belum divalidasi lewat pengguna asli yang benar-benar absen berhari-hari — perlu smoke-check founder di production. Kualitas `interpretation`/`evidenceSchema.activityType`/target derivation dari AI SUNGGUHAN (bukan fallback keyless) tidak bisa diverifikasi tanpa `ANTHROPIC_API_KEY` asli, pola gap yang sama dengan semua fitur AI-dependent sebelumnya di sesi ini.

## 24. Task 7d — Formalisasi hierarki Quest + hapus poin arbitrer + generalisasi evidence schema (SELESAI P0)

**Konteks & sumber**: `Eleva_PRD.pdf` di-upload ulang lebih lengkap (46 halaman, dari 43 sebelumnya) — satu-satunya konten baru adalah section "Task 7d", dilabeli eksplisit P0, "ditemukan lewat testing lanjutan" founder SENDIRI setelah Task 7c (section 23) live di produksi. Sebagian ditemukan langsung dari screenshot produksi founder (quest "Latihan Ritme: 3.2km dengan Pace Terkontrol" masih menampilkan picker manual Cardio/Gym lama) — didiagnosis sebagai lag deploy biasa (quest itu dibuat sebelum kode Task 7c naik, dan quest terbuka memang sengaja tidak pernah di-retag diam-diam begitu sudah ada — invarian yang sama yang sudah didiagnosis dua kali sebelumnya di sesi ini), diselesaikan dengan SQL patch terskop sempit (match judul persis + `reflection IS NULL`) yang founder jalankan sendiri di Railway, BUKAN perubahan kode.

**Enam item, semua P0:**
1. **Hierarki resmi: Primary Quest ≠ Milestone ≠ Today's Trial.** Sebelumnya istilah "Quest" dipakai longgar untuk tiga konsep berbeda, bikin pergeseran topik harian (mis. dari lari ke istirahat pemulihan) terbaca seolah Eleva "lupa" goal-nya. Sekarang dipisah tegas: **Primary Quest** = goal yang dinyatakan user sendiri dari Task 5 (`ctx.activeGoal`, tidak pernah berubah harian), **Milestone** = `ctx.currentTarget` dari Task 7b/Fokus 2.2 (angka konkret yang sedang dikejar, cuma berubah saat tercapai), **Today's Trial** = instruksi harian yang di-generate sekarang (label kartu quest, dulu "PRIMARY QUEST"/"QUEST" → "TODAY'S TRIAL", "ACTING METHOD" → "TODAY'S ACTING METHOD") — boleh ganti topik total hari ke hari tanpa itu berarti Primary Quest/Milestone-nya berubah. Ditegakkan di DUA sisi: `MENTOR_SYSTEM` (`server/claude.js`) dapat bullet kontrak istilah baru supaya teks bebas AI (insight, why, Eleva Response) tidak pernah mencampur ketiganya; `questSummaryCard` (`public/app.js`) dapat blok context header baru (`.quest-context`, di atas tiap kartu, cuma render kalau ada goalLabel & bukan Side Quest) menampilkan "◆ Primary Quest: {goal}" + "→ Milestone: {label}" (kalau sudah ada target tersimpan) — goalLabel yang dulu numpuk di baris `qlabel` sekarang cuma di context header ini, `qlabel` murni label+statFocus.
2. **Poin arbitrer dihapus dari tampilan.** Setelah evidence submit, chip delta tidak lagi menunjukkan angka spesifik ("Body +3") — diganti indikator bukti-tercatat ("Body · Evidence tercatat"). **Sengaja cuma perubahan tampilan** — perhitungan/penerapan delta ke `stats` (termasuk `touchStatActivity`/Decay wiring dari 11e) SAMA SEKALI tidak disentuh, karena growth nyata tetap datang dari evaluasi pola longitudinal (Bible bab 11), bukan satu sesi — menampilkan angka mentah per-sesi cuma mengundang fiksasi pada satu titik data yang justru bukan sinyal growth sesungguhnya.
3. **Eleva Response jadi 3 lapis eksplisit: Observed → Hypothesis → Decision** (dulu satu kalimat bebas). `processReflection`'s kontrak JSON: `interpretation: {observed, hypothesis, decision}` — Observed = fakta mentah dari evidence yang baru disubmit, Hypothesis = interpretasi tentatif yang WAJIB di-hedge ("mungkin"/"bisa jadi", eksplisit di prompt), Decision = langkah konkret berikutnya, BOLEH tegas meski hypothesis-nya tentatif. Prinsipnya: **yakin soal AKSI, tidak overconfident soal DIAGNOSIS** dari data terbatas (satu sesi tidak cukup buat klaim pola pasti, tapi tetap cukup buat rekomendasi langkah). Divalidasi/di-patch server-side (fallback ke shape `fallbackReflection()` kalau AI balikin bentuk cacat, pola sama dengan validasi lain di file ini) — `public/app.js`'s `elevaResponseHTML` merender 3 baris bertag (`.er-tag`: "Diamati"/"Kemungkinan"/"Keputusan"), dengan fallback baca-turun-derajat kalau ketemu shape lama (string polos, dari quest yang selesai di tengah masa deploy).
4. **Catatan keamanan untuk sinyal nyeri/cedera** (baru). Kalau teks refleksi/context menyebut nyeri signifikan, kram, pincang, dsb., `processReflection` menyertakan `safetyNote: string|null` — satu kalimat hati-hati yang lembut (BUKAN diagnosis, BUKAN alarmis): jangan dipaksakan kalau menetap, pertimbangkan bantuan profesional kalau parah/berkelanjutan. Tampil independen dari eligibility growth (tetap muncul walau growth ditahan gate kespesifikan — ini soal keamanan, bukan reward) lewat blok `.safety-note` (warna amber, sengaja beda dari `--rust` yang eksklusif untuk error) di `completedResultCardHTML`.
5. **Evidence schema digeneralisasi ke SEMUA jenis Trial fisik/tubuh, bukan cuma yang aktif.** Menutup regresi nyata yang founder temukan: quest bertema pemulihan pasca-cedera ("Audit Fondasi Pemulihan") jatuh balik ke jurnal teks bebas ("buat catatan jujur tentang apa yang kamu makan..."). `structuredKind` diperluas jadi tiga: `"cardio"|"gym"|"recovery"` — recovery punya field sendiri (durasi tidur jam, asupan air gelas, jumlah makan berprotein, level nyeri 4-chip; `server/structured.js` validasi rentang wajar untuk semuanya), `target` SELALU null (tidak ada satu angka tunggal dikejar, 4 field dikumpulkan bareng, `targets.canTarget` sudah otomatis skip kind tak dikenal jadi Target Berikutnya A/B/C tidak pernah muncul salah tempat). **`normalizeEvidenceSchema` (`server/claude.js`) diubah dari "null-kan kalau cacat" jadi "SELALU kembalikan objek valid"** untuk quest `structured-physical` apa pun — inilah mekanisme yang menutup DoD "picker manual dihapus total": klien sekarang selalu bisa percaya `evidenceSchema.metricType` untuk memilih form yang benar. Untuk quest yang bahkan tidak punya `evidenceSchema` sama sekali (pra-Task-7c), klien jatuh balik ke tag `structuredKind` lama (Task 7b), lalu ke default diam-diam `"cardio"` sebagai upaya terakhir — tidak pernah ke picker manual.
   - **Catatan implementasi penting**: picker manual "Aktivitasnya jenis apa?" (`kindPickerHTML`) TIDAK dihapus total dari kode — DoD di atas cuma soal quest yang AI-nya SUDAH menyatakan maksud (`structured-physical` dengan evidence schema/tag apa pun). Jalur terpisah dari Fokus 0 (README, "Prinsip desain" — 10 Agustus) tetap hidup: opt-in manual "Aktivitas fisik? Catat sebagai record →" untuk quest REFLEKTIF biasa yang usernya sendiri mau catat sebagai record fisik, di mana memang tidak ada apa pun untuk di-auto-derive. Picker ini digerbangi `structKindAuto` (true di semua cabang auto-derive Task 7d.5 di atas, false hanya di jalur opt-in manual) — sempat terhapus tidak sengaja saat implementasi awal, ketahuan & diperbaiki lewat regresi Playwright (`ui-test-focus0-form.js`/`ui-test-focus0-gym.js`) sebelum push.
6. **Pemicu alasan-shortfall, independen dari rating "Rasanya gimana?"** (baru). Evidence bisa jatuh jauh di bawah Milestone walau user menilai usahanya "Ringan" (contoh founder: hujan tiba-tiba motong lari 3.2km jadi cuma 1.8km). Ambang `SHORTFALL_THRESHOLD = 0.8` — evidence di bawah 80% target memicu picker chip TERPISAH (Cuaca · Cedera · Gangguan/diinterupsi · Kehabisan waktu · Lainnya, `computeShortfallPrompt` di `server/index.js`, murni dari rasio evidence/target, TIDAK PERNAH dari titik-berat/status). Alasan yang dipilih disimpan sebagai **CONTEXT, bukan evidence** — merge ke `reflection` jsonb yang sudah ada (`db.setShortfallReason`, pola sama dengan `kondisi_note`), dibaca balik oleh `generateQuest` lewat `recentDays` yang sudah ada (tidak ada plumbing baru) dengan instruksi eksplisit: JANGAN anggap ini pola stamina menurun, cuma faktor sekali kejadian (beda dari `ctx.kondisiStatus` yang memang menandakan kondisi masih berlangsung) — jangan turunkan target/intensitas cuma karena satu shortfall bercontext. Single-tap-commits (pola sama dengan Kondisi Hari Ini), tidak pernah memblokir tombol "Lanjut".

**Diverifikasi** (28 unit test langsung ke `server/claude.js`/`server/structured.js` + 18 API-level test lewat `fetch` ke route + 20 assertion Playwright, semua di luar API key nyata):
- Unit: `normalizeEvidenceSchema` tidak pernah null untuk quest structured-physical apa pun (termasuk input cacat/kosong), metricType benar per kind (distance/reps/recovery), target recovery selalu null walau AI coba isi, target implausible (5000km) di-drop; `normalizeCompletionType` menerima `recovery` sebagai structuredKind valid; `fallbackReflection` mengembalikan shape interpretation 3-lapis + `safetyNote: null`; `validateStructuredData("recovery", ...)` menolak rentang tidak wajar (tidur >24 jam, air negatif, protein non-integer, level nyeri di luar 4 enum) dan menerima batas nol semua.
- API-level: quest recovery selesai tanpa target untuk dibandingkan → "done" otomatis (murni "sudah dicatat"), evidence tidur 40 jam ditolak server meski klien tidak lagi punya picker manual untuk "menyaring" input aneh duluan; `interpretation` selalu object 3-field, `safetyNote` selalu ada sebagai key; evidence 1.8/3.2km (56%, dinilai "Ringan") memicu `shortfallPrompt`, evidence 2.9/3.2km (~91%) tidak; `POST /api/quest/shortfall-reason` menerima alasan valid & persist ke jsonb, menolak alasan di luar enum, menolak quest yang belum direfleksikan.
- Playwright: context header menampilkan "Primary Quest:"+"Milestone:" berbeda di tiap kartu goal, label kartu "TODAY'S TRIAL"; quest cardio & recovery evidence-schema-driven SAMA-SAMA tidak pernah menampilkan picker manual; delta chip (dicek via pemanggilan langsung fungsi render, karena fallback keyless lokal selalu `statDeltas:{}`) menampilkan "Evidence tercatat" tanpa angka; Eleva Response menampilkan ketiga tag Diamati/Kemungkinan/Keputusan; form recovery menampilkan 4 field termasuk chip nyeri yang benar-benar menulis ke form (class aktif berubah saat diklik) dan submit berhasil; shortfall picker muncul dengan 5 chip saat evidence jauh di bawah target, klik chip commit langsung dengan teks konfirmasi dan chip hilang, TIDAK muncul saat evidence dekat target atau untuk quest recovery (tidak ada target dibandingkan); safety-note render dicek lewat pemanggilan fungsi langsung (amber class + glyph ⚠ saat ada, blok sama sekali tidak render saat null).
- Regresi (15 skrip lama termasuk seluruh daftar wajib practicetest/jobmatch/targetpicker/redesign/redesign2/focus0-form/focus0-gym/focus0-verify-current/task7c, plus bodyfocus-autorecord/focus1/focus2-manual/axis-help-dashboard/subpathway-unit/chapter-archive): **satu regresi nyata ditemukan & diperbaiki** (lihat catatan item 5 di atas — picker manual opt-in Fokus 0 sempat ikut terhapus, dikembalikan). Dua skrip lama diupdate ekspektasinya untuk perilaku yang memang sengaja berubah oleh Task 7d.5 sendiri (bukan bug): `ui-test-focus0-verify-current.js` (quest bertag `structuredKind` tanpa `evidenceSchema` dulu HARUS munculkan picker manual, sekarang justru DoD-nya tidak boleh), `ui-test-focus1.js` (selector goal-label pindah dari `.qlabel` ke `.quest-context-primary` yang baru). Tiga skrip lain (`subpathway.js`/`subpathway-override.js`/`focus2-pace.js`) sudah basi dari TASK-TASK SEBELUM Task 7d (selector `#promiseCheck`/`durasiMenit` yang sudah tidak ada sejak redesign Task 12/Task 7c) — dicatat, tidak diperbaiki karena di luar blast radius perubahan sesi ini.

**Gap yang diketahui, dicatat bukan ditutup**: kualitas nyata `interpretation`/`safetyNote`/pemilihan `evidenceSchema.target` dari AI SUNGGUHAN (deteksi sinyal nyeri, hedging hypothesis yang natural, dsb.) tidak bisa diverifikasi tanpa `ANTHROPIC_API_KEY` asli — pola gap yang sama dengan semua fitur AI-dependent sebelumnya di sesi ini, cuma bisa di-smoke-check founder di production. Ambang shortfall 80% adalah pilihan implementasi sendiri (spek tidak memberi angka persis), sama seperti ambang Decay 4-hari sebelumnya — bisa disetel ulang tanpa migrasi skema kalau kerasa kurang/kelebihan sensitif.

## 25. Task 13 — Objective Assessment Engine (Band Range + Confidence + Skill Baseline per-track) + fix `practiceTestSchema` (SELESAI)

> Diagram alur lengkap (WAJIB dibaca sebelum menyentuh stack practice-test): [`docs/practice-test-flow.md`](docs/practice-test-flow.md)

**Konteks & sumber**: `Eleva_Draft_Task13_ObjectiveAssessmentEngine.pdf` (DISETUJUI founder 12 Agustus 2026) + `PROMPT_ClaudeCode_Task13_dan_PracticeTestFix.pdf`. Dua item kerja dalam satu sesi karena bersinggungan di file yang sama: Item 1 fix kecil (quest practice-test tidak meneruskan kind/track ke flow — user dipaksa milih ulang dari nol padahal quest-nya sendiri sudah bilang "Listening"), Item 2 Task 13 penuh. Nomor Task resmi: 13 — kebetulan sama dengan placeholder draft, karena nomor Task tertinggi yang sudah terpakai di dokumen ini memang 12.

**Apa yang dibangun**:
1. **Item 1 — `practiceTestSchema` (pasangan `evidenceSchema` untuk quest belajar).** Kontrak JSON `generateQuest` dapat field `practiceTestSchema: {kind: "reading"|"listening"|null, track: "academic"|"general"|null}` — diisi HANYA kalau quest eksplisit menyebutnya (instruksi prompt melarang mengarang), dinormalisasi `normalizePracticeTestSchema` (enum-atau-null, pola sama `normalizeEvidenceSchema`). Klien (`[data-reflect-id]` handler): kind+track terisi → langsung generate & layar soal (dua picker di-skip); kind saja → langsung picker track; kosong → perilaku lama persis. Jalur META tidak disentuh (sesi bebas tanpa quest konkret, tetap tanya dari awal).
2. **Skema per-track.** `character_state.practice_test[goalIndex]` dari flat `{level, history}` jadi `{tracks: {reading, listening, writing, speaking}}`, tiap track `{level, history (cap 20, tampilan), totalQuestions, totalCorrect}` — running counter TERPISAH dari history supaya band/confidence tidak kehilangan evidence lewat 20 sesi. Migrasi lazy `migrateState` (`server/practiceTest.js`): entri lama di-backfill per `history[i].testKind` (field bernama `track` di entri lama itu academic/general — sumbu berbeda), dipersist balik dalam bentuk baru pada submit pertama. Slot writing/speaking kosong siap ditempati nanti TANPA migrasi ulang (completionType rubric-nya sendiri tetap di luar scope, keputusan founder terpisah).
3. **`estimateBand` + confidence — deterministik, bukan AI.** Proyeksi `totalCorrect/totalQuestions × 40`, lookup titik referensi publik (15/40≈5, 23/40≈6, 30/40≈7, 35/40≈8), interpolasi linear + ekstrapolasi ter-clamp [1,9], mid dibulatkan ke 0.5 terdekat, range ±0.5. TIDAK PERNAH dirender sebagai "IELTS Band Score: X.X" — selalu range + confidence. Confidence dari `totalQuestions`: <20 Low, 20-99 Moderate, ≥100 High (konstanta standalone, retunable tanpa migrasi, pola ambang Decay/shortfall).
4. **Sprint 20 soal, kesulitan terdistribusi, tipe bervariasi.** Prompt `generatePracticeTest`: TEPAT 20 soal, Q1-7 accessible / Q8-14 moderate / Q15-20 harder, campuran mc/tf/fill/**matching** (baru di `cleanPayload` — bank pilihan bersama 3-8 item), `category` WAJIB per soal (label skill konsisten; yang hilang di-fallback ke label tipe, bukan drop). Ambang minimum `cleanPayload` naik proporsional 3→12 (drill→8). Fallback keyless diperluas ke set statis 20 soal per kind (kategori + semua tipe) supaya mode tanpa API key melewati pipeline band/breakdown yang sama.
5. **Reuse hierarki Task 7d.** `ctx.currentTarget` goal practice-test = `{track, targetBand}` + label/approach (bentuk kompatibel dengan target fisik), dipilih server-side **bottleneck-first** (`currentTargetFor`): track belum teruji = terlemah mutlak ("Establish {Track} baseline"), selain itu band terendah yang belum STABLE ("Stabilize {Track} at Band {X}"). `targetBand` diparse deterministik dari teks goal (angka 1-9 pertama, koma diterima), fallback 6.5.
6. **Status ladder per track** (EXPOSED→EMERGING→STABLE→MASTERED), murni baca dari history (pola statTrends, tidak ada state tersimpan): STABLE = ≥3 dari 4 attempt terakhir ≥ targetBand; MASTERED = 2 window STABLE berturut (window bergeser satu attempt); EMERGING = ada hit di window; EXPOSED = ada attempt tanpa hit. "Milestone achieved" saat transisi masuk STABLE/MASTERED → baris di result card + `currentTargetFor` otomatis memilih track terlemah berikutnya (tidak pernah menaikkan target dari satu sprint bagus).
7. **Drill terfokus (Next Trial).** Kategori terlemah attempt ini (akurasi ≤60%) tersimpan sebagai `tracks[track].nextDrill` — sesi berikutnya di track itu otomatis jadi DRILL 12 soal satu-kategori (`entryType: "drill"`): evidence MASUK `totalQuestions`/`totalCorrect`, TIDAK menaikkan nomor SPRINT # dan TIDAK menaikkan level ratchet, label UI "DRILL" (bukan "SPRINT #N"). Attempt bersih menghapus nextDrill → sprint penuh lagi.
8. **Result card dirombak** (`practiceTestResultHTML`): header "{TRACK} SPRINT #{n}"/"{TRACK} DRILL", skor+metadata, blok **ESTIMATED LEVEL** ("IELTS {low}–{high}", "Projected raw ≈ {x}/40", "Confidence: {level} · {n} questions observed"), blok **ELEVA OBSERVED** (Strong/Unstable per kategori — agregasi deterministik dari grading, bukan AI), blok **ELEVA DECISION** (Primary Quest / Current Target / Next Trial, label hierarki Task 7d). Interpretation 3-lapis generik TIDAK dirender untuk practice-test (blok-blok ini penggantinya). Delta chip tetap "Evidence tercatat", tidak disentuh.

**Backend**: `server/practiceTest.js` (engine deterministik: estimateBand/confidence/parseTargetBand/migrateState/trackStatus/currentTargetFor/categorySplit/weakestCategory + cleanPayload/gradeAnswers diperluas), `server/claude.js` (normalizePracticeTestSchema, kontrak generateQuest, prompt sprint/drill generatePracticeTest, fallback 20-soal), `server/index.js` (generate: migrasi + deteksi drill; submit: update tracks + assessment lengkap di response; GET /api/state: currentTarget bottleneck-first untuk goal practice-test).

**Frontend**: `public/app.js` (auto-skip picker per practiceTestSchema, result card baru, label DRILL di layar soal, suppress interpretation generik untuk practice-test), `public/styles.css` (blok `.pt-block`/`.pt-band`/`.pt-obs`).

**Diverifikasi** (`tests/practicetest.js` unit+API, `tests/practicetest.e2e.js` Playwright — keduanya keyless, Postgres scratch lokal):
- Unit (36): estimateBand di semua titik anchor + interpolasi + proyeksi 20→40 + clamp; ambang confidence; parseTargetBand; migrasi flat→tracks (backfill per testKind, totals terpisah dari history, slot writing/speaking kosong); ladder kelima status; bottleneck-first (untested menang, band terendah menang); breakdown kategori grading; cleanPayload matching/minimum 12/minimum drill 8/fallback category/cap 20.
- API-level (8): state mengirim practiceTestSchema ke klien; generate menghormati kind/track quest + sprint 20 soal + kunci jawaban tidak bocor + tipe matching hadir; submit mengembalikan band range + confidence + observed/decision; state per-track terpersist bentuk tracks; sprint jelek menjadwalkan DRILL (entryType drill, ≤12 soal, focusCategory); drill menambah totals TANPA menaikkan SPRINT #; sesi META tetap tanpa schema (picker penuh); quest flow reflektif tidak regresi.
- Playwright (4): full schema → langsung layar soal tanpa kedua picker; kind-only → skip picker kind, track tetap ditanya; tanpa schema → picker penuh (regresi, tidak berubah); submit sprint merender "LISTENING SPRINT #01" + ESTIMATED LEVEL + ELEVA OBSERVED + ELEVA DECISION + band sebagai RANGE.
- Catatan regresi: skrip regresi `practicetest.js` yang disebut prompt sesi TIDAK ADA di repo (sesi-sesi sebelumnya memverifikasi lewat skrip ephemeral yang tidak di-commit) — `tests/practicetest.js` ini jadi skrip kanoniknya mulai sekarang, dengan ekspektasi ukuran sprint 20 (perubahan disengaja dari 3, bukan bug).

**Keputusan default yang diambil (bisa di-override founder, sesuai instruksi prompt sesi)**:
- **Timer aktif 30 menit: TIDAK masuk sesi ini.** Bucket "Time" di Eleva Observed dilewati (tidak dirender) — dicatat sebagai gap diketahui, tidak ada infrastruktur timer setengah jadi.
- **Drill masuk `totalQuestions`/`totalCorrect` tapi TIDAK menaikkan SPRINT #** — label UI "DRILL", persis default yang diinstruksikan.
- Tambahan kecil yang spek biarkan terbuka: ambang Strong ≥80% / Unstable ≤60% akurasi per kategori; drill dipicu otomatis oleh `nextDrill` pending (bukan tombol terpisah di result card); level ratchet hanya naik dari sprint; `targetBand` fallback 6.5 kalau goal tidak menyebut angka; sesi META (tanpa goalIndex) mendapat assessment dari sesi itu sendiri saja (Low confidence, tidak ada akumulasi per-goal untuk diakumulasi).

**Gap yang diketahui, dicatat bukan ditutup**: kualitas soal AI sungguhan (distribusi kesulitan nyata Q1-7/8-14/15-20, konsistensi label kategori antar sesi, kualitas soal matching) tidak bisa diverifikasi keyless — butuh smoke-test founder di production, pola gap yang sama dengan semua fitur AI-dependent sebelumnya. Bucket "Time" menunggu timer aktif. Writing/Speaking menunggu keputusan scope rubric terpisah. Band anchor dari referensi publik IELTS — kalau goal belajar non-IELTS dipakai serius, label "IELTS {range}" di result card perlu di-generalisasi (sekarang mengikuti mockup founder apa adanya).

### 25.1 — Follow-up (12 Agustus, langsung dari production): picker practice-test dihapus total dari Today's Trial

Founder laporan dari screenshot production: quest practice-test yang di-tap "Mulai" masih menampilkan picker "Mau latihan apa dulu?" (Reading/Listening) — Item 1 di atas cuma skip picker kalau `practiceTestSchema` terisi eksplisit oleh AI, dan quest yang generik (paling umum) tetap nanya. Diminta langsung: tap "Mulai" pada practice-test APA PUN harus langsung masuk ke soal, tanpa terkecuali.

**Keputusan (dikonfirmasi founder)**: default sederhana, BUKAN AI yang menentukan. Kalau `practiceTestSchema.kind`/`.track` kosong, klien langsung pakai `reading`/`academic` tanpa nanya — tidak ada lagi jalur yang menampilkan picker di Today's Trial. Picker manual (`practiceTestFlowHTML` step `kind`/`track`) tidak dihapus dari kode — itu tetap satu-satunya jalur lewat tab **META**, di mana user memang sengaja memilih sendiri mau latihan apa (beda konteks dari Today's Trial yang sudah ditentukan Eleva).

**Kode**: `public/app.js`'s `[data-reflect-id]` handler untuk `completionType==="practice-test"` — hapus percabangan tiga arah, langsung `kind = pts.kind || "reading"`, `track = pts.track || "academic"`, generate. Step `"error"` baru ditambahkan di `practiceTestFlowHTML` + tombol "Coba lagi" (`#ptRetry`) untuk kasus langka generate gagal total (bukan fallback AI biasa, itu sudah ditangani server — ini cuma kalau server/network benar-benar error), karena sekarang tidak ada picker untuk "mundur" ke sana kalau generate gagal.

**Diverifikasi**: `tests/practicetest.e2e.js` diupdate — 3 skenario (schema penuh/sebagian/kosong) semuanya sekarang mengharapkan langsung ke layar soal tanpa picker apa pun; 1 skenario baru mengonfirmasi tab META tetap menampilkan picker manual seperti sebelumnya (perilaku yang sengaja dipertahankan).

**Gap yang diketahui**: default `reading`/`academic` bukan personalisasi — kalau goal user eksplisit soal Listening tapi AI gagal mengisi `practiceTestSchema.kind` (jarang, tapi mungkin), Today's Trial-nya akan mulai dari Reading padahal seharusnya Listening. Ini trade-off yang diterima demi zero-picker; kalau kerasa sering salah di production, opsi lanjutan adalah AI-inferred default (bukan fixed) — dicatat, belum diminta.

## 26. Task 14 — Livelihood Milestone: Qualified Applications + verdict deterministik Job Match (SELESAI)

> Diagram alur lengkap (WAJIB dibaca sebelum menyentuh stack Livelihood/Job Match): [`docs/livelihood-milestone-flow.md`](docs/livelihood-milestone-flow.md)

**Konteks & sumber**: sesi chat founder 12 Agustus, dua observasi langsung founder: (1) quest card Livelihood tidak pernah menampilkan baris "→ Milestone: …" — tidak pernah masuk mekanisme Target Berikutnya sama sekali; (2) hasil Job Match Analysis masih menampilkan badge poin instan "Livelihood +3", yang seharusnya sudah ditutup Task 7d item 2 tapi Job Match sengaja dikecualikan dari refactor evidence-schema saat itu (pertumbuhannya deterministik tanpa AI call kedua, lihat catatan section 21). Dua keputusan dikonfirmasi founder lewat AskUserQuestion SEBELUM implementasi dimulai (dicatat, bukan diasumsikan): (a) target "apply ke N perusahaan" dihitung dari jumlah Qualified, bukan raw apply count; (b) "lolos"-nya verdict ditentukan dari ambang `matchScore >= 70`, bukan penilaian naratif AI semata.

**Verifikasi kode sebelum implementasi (instruksi eksplisit sesi: "jangan asumsikan dari deskripsi, cek kode dulu")**: kedua observasi founder terkonfirmasi AKURAT terhadap kode saat itu — `questSummaryCard`'s baris Milestone (`.quest-context`) murni dibaca dari `state.goalTargets[goalIndex]`, kolom yang cuma pernah diisi `setGoalTarget` (dipanggil dari flow cardio/gym A/B/C); Livelihood/practice-test tidak pernah menulis ke situ. `/api/job-match/analyze` masih persis menerapkan `deltas = {[statFocus]: 3}` flat, tanpa `matchScore`/`qualified` di mana pun di `server/jobMatch.js`/`server/claude.js`.

**Apa yang dibangun**:
1. **Milestone Livelihood #1 — "10 Qualified Applications", count tetap, bukan pilihan A/B/C.** `server/targets.js` dapat kind baru `qualified-applications` — `{targetCount, currentCount}`, `targetReached` = `currentCount >= targetCount` (tanpa toleransi pembulatan, beda dari cardio 2%). Berbeda dari cardio/gym (baru ada target setelah user pilih opsi) dan practice-test's `currentTargetFor` (dihitung ulang tiap baca, tidak pernah disimpan): Milestone ini **auto-dibuat dan DIPERSIST** (`ensureQualifiedApplicationsMilestone`, `server/index.js`) begitu goal ini pertama kali disentuh `job-match-analyze` — targetCount SELALU 10 (keputusan founder, bukan pilihan AI/user), harus tersimpan di `goal_targets` (bukan dihitung ulang tiap baca) supaya baris `.quest-context` Primary Quest bisa membacanya. Ini yang menutup observasi founder #1.
2. **Checklist "qualified" dihitung server-side, bukan diklaim AI.** `generateJobMatchAnalysis` (`server/claude.js`) kontrak JSON dapat field baru `matchScore: number` (0-100) di samping `matchTable`/`verdict`/`relevanceNote`/`nextStep` (TIDAK diubah/dihapus — narasi 3-kategori existing tetap jadi `relevanceNote`, bukan lagi satu-satunya sinyal lolos/tidak). `cleanJobMatchResult` (`server/jobMatch.js`) menghitung `qualified = matchScore >= 70` di KODE, tidak pernah dipercaya dari klaim bebas AI (matchScore wajib ada — hilang/rusak → null, memaksa fallback jujur, pola sama dengan verdict/nextStep). Prompt diinstruksikan matchScore harus mempertimbangkan kesesuaian role & red flag (bukan cuma persentase skill overlap) — role-alignment dan anti-sycophancy jadi bagian dari SATU angka deterministik ini, bukan checklist boolean terpisah yang butuh AI call tambahan.
3. **Branching next trial berbasis `jobMatchHint`, dihitung server bukan diminta ke AI dari recentDays mentah.** `GET /api/state` (needySlots loop) menghitung hint per-goal (`db.recentDays` scoped goalIndex): analisis job-match TERBARU goal ini yang belum punya `job-application-submit` sesudahnya → `{qualified, gap?, note}`. Prompt `generateQuest` (`server/claude.js`) dapat instruksi: `qualified: true` → completionType hari ini WAJIB `job-application-submit` untuk lowongan yang sama; `qualified: false` → DILARANG mengarahkan submit ke lowongan yang baru dinyatakan gagal, pilih skill-building (`reflective`) menyasar `gap` atau `job-match-analysis` lowongan lain.
4. **Trial baru: Submit Application — completionType kelima, `job-application-submit`.** Form terstruktur (`jobApplicationFlowHTML`, `public/app.js`), BUKAN free text: `companyName`, `roleTitle`, `dateApplied`, `cvVersionUsed` (artifact id, reuse Artifacts library sama seperti Job Match), `submissionProof` (≥8 karakter, anti-fabrikasi). Validasi deterministik penuh di `server/jobApplication.js` (pola sama `structured.js`) — tanpa AI call sama sekali. `POST /api/job-application/submit` (`server/index.js`) mem-verifikasi ULANG gate qualified dari analisis TERAKHIR goal ini (bukan percaya hint lama dari klien) sebelum `currentCount` boleh naik — kombinasi qualified (matchScore≥70 + role/red-flag sudah terlipat di dalamnya) + cvVersionUsed valid + submissionProof tervalidasi = checklist penuh spec.
5. **Badge "Livelihood +3" dihapus, diganti baris progress Milestone.** `/api/job-match/analyze` tidak lagi menerapkan delta apa pun (`deltas: {}` selalu) — analisis semata adalah PEMBACAAN kecocokan, bukan bukti progress; Milestone counter (dinaikkan HANYA oleh submit tervalidasi) yang membawa sinyal "sesuatu nyata terjadi", pola sama dengan structured-physical/practice-test yang sudah lebih dulu tumbuh dari evidence bukan klik. `jobMatchResultHTML` (`public/app.js`) tambah blok MATCH SCORE (reuse class `.pt-block`/`.pt-band` dari Task 13) + baris lolos/tidak, komponen delta-chip dihilangkan untuk hasil ini, digantikan `targetPickerHTML` mode progress ("10 Qualified Applications (X/10)") — reuse komponen Milestone existing, bukan komponen baru. **Growth stats calculation/storage TIDAK disentuh** (perubahan tampilan+asal-sinyal, bukan mekanisme stats itu sendiri) — dan job-application-submit SENGAJA tidak menambahkan mekanisme poin baru (Task 7d "no arbitrary points" — Milestone counter ITU SENDIRI pengganti sinyalnya, bukan dipindah ke rute lain).
6. **Milestone #2+ — funnel metric, generalisasi mekanisme A/B/C existing.** `POST /api/job-application/submit`: begitu `currentCount === targetCount` (10/10), `targetScreen` mode `options` dengan kind baru `livelihood-funnel` (`server/targets.js`: `{metricLabel, targetValue, currentValue}`, generik bukan `qualified-applications` lagi — milestone berikutnya beda bentuk, bukan sekadar count lain). `generateTargetOptions` (`server/claude.js`) dapat cabang `qualified-applications` → `generateLivelihoodFunnelOptions`: 2 arah funnel berbeda (contoh fallback: "Response Rate" vs "Interview → Offer"), + Opsi C tulis-sendiri (`targetManualFormHTML` cabang `livelihood-funnel` baru — tetap numerik/measurable, bukan teks bebas, sama seperti Opsi C cardio/gym). Mekanisme A/B/C ITU SENDIRI (`POST /api/goal-target`, `targetPickerHTML`) **TIDAK diubah sama sekali** — cabang baru mengalir lewat kode generik yang sudah ada, cardio/gym persis seperti sebelumnya (non-goal eksplisit, diverifikasi lewat test regresi).

**Backend**: `server/targets.js` (kind `qualified-applications` + `livelihood-funnel`), `server/jobMatch.js` (matchScore/qualified deterministik), `server/jobApplication.js` (baru — validasi evidence submit application), `server/claude.js` (matchScore di prompt generateJobMatchAnalysis, completionType kelima + jobMatchHint di prompt generateQuest, generateLivelihoodFunnelOptions/fallbackLivelihoodFunnelOptions), `server/index.js` (ensureQualifiedApplicationsMilestone, jobMatchHint di needySlots loop, `/api/job-match/analyze` badge dihapus, route baru `/api/job-application/submit`).

**Frontend**: `public/app.js` (jobApplicationFlow state + `jobApplicationFlowHTML`, `[data-reflect-id]` cabang `job-application-submit`, `jobMatchResultHTML` blok MATCH SCORE, `jobApplicationSummary`, `targetManualFormHTML` cabang `livelihood-funnel`, `completedResultCardHTML` baris ringkasan `jobApplication`), `public/styles.css` (`input[type="date"]` ditambahkan ke selector `.field` yang sudah ada — tidak ada komponen visual baru).

**Diverifikasi** (`tests/livelihood.js` unit+API, `tests/livelihood.e2e.js` Playwright — keduanya keyless, Postgres scratch lokal; suite `practicetest.js`/`practicetest.e2e.js` lama dijalankan ulang, tidak ada regresi):
- Unit (23): `targets.js` kind `qualified-applications` (clean/format/reached, termasuk reject count negatif & target ≤0) dan `livelihood-funnel` (clean/format/reached) + regresi cardio/gym tidak tersentuh; `jobMatch.js` qualified dari matchScore≥70/<70, TIDAK percaya klaim `qualified` mentah di payload, matchScore hilang/rusak → null, clamp 0-100 + pembulatan, matchTable baris rusak tetap di-drop individual (perilaku Task 10b dipertahankan); `jobApplication.js` submit valid, field kosong/tanggal masa depan/format tanggal salah/bukti submit <8 karakter semua ditolak.
- API-level (9): analisis pertama auto-membuat Milestone tetap 0/10; analisis TIDAK lagi menerapkan delta stat apa pun; `goalTargets` ke klien sekarang terisi (prasyarat baris Milestone di quest card); submit ditolak kalau analisis terakhir goal ini TIDAK qualified; submit menaikkan `currentCount` kalau qualified; submit ditolak kalau evidence tidak valid; mencapai 10/10 memicu opsi A/B/C `livelihood-funnel` + memilih Opsi A berhasil lewat rute `/api/goal-target` generik yang sama; submit ulang ke quest yang sama diblok guard "sudah pernah diselesaikan"; regresi cardio/gym Target Berikutnya masih jalan.
- Playwright (3): menjalankan analisis merender blok MATCH SCORE + tanpa delta-chip + baris "Qualified Applications" (bukan badge lama); baris "→ Milestone: 10 Qualified Applications" tampil di quest card (format sama dengan Body); submit form tervalidasi (company/role/tanggal/bukti) menaikkan counter dan merender ringkasan.

**Keputusan implementasi yang diambil (bisa di-override founder, sesuai instruksi prompt sesi "implementasikan default masuk akal, jangan berhenti nanya")**:
- **Checklist 5-poin spec (matchScore/roleAligned/cvVersionTailored/submissionEvidence/notRedFlag) disederhanakan jadi satu angka deterministik + dua gate terpisah.** `matchScore>=70` membawa roleAligned+notRedFlag (dilipat ke instruksi prompt, bukan field boolean terpisah yang perlu AI call tambahan) sebagai SATU verdict "qualified" di tahap analisis; `cvVersionTailored`+`submissionEvidence` jadi gate terpisah di tahap submit (artifact id valid + form tervalidasi). Alasan: keputusan founder (b) eksplisit bilang "matchScore>=70, bukan penilaian naratif AI semata" — itu SATU ambang, bukan lima ambang independen; memaksa 5 boolean terpisah di satu AI call berisiko jadi lima klaim bebas yang sama rentannya dengan masalah yang task ini coba tutup.
- **Auto-create Milestone #1 di tahap `job-match-analyze` pertama, bukan di tahap onboarding/goal-capture.** Tidak ada cara deterministik untuk tahu goal mana yang "Livelihood" sebelum quest pertamanya digenerate (goal cuma teks bebas, tidak ada tag domain) — konsisten dengan pola existing practice-test's target yang juga baru muncul setelah sentuhan pertama, bukan di goal-capture.
- **Milestone #2+ (funnel) tidak ada instrumentasi otomatis untuk `currentValue`.** Dicatat sebagai gap di bawah, bukan ditutup dengan tracker palsu.

**Gap yang diketahui, dicatat bukan ditutup**: Milestone #2 (`livelihood-funnel`, response rate / interview→offer) TIDAK punya sensor otomatis — sistem belum bisa mendeteksi "recruiter membalas" atau "interview terjadi", jadi `currentValue`-nya tetap 0 sampai ada mekanisme evidence baru untuk funnel stage ini (di luar scope task ini, PRD ini cuma menggeneralisasi mekanisme A/B/C-nya, bukan membangun instrumentasi funnel penuh). Kualitas `matchScore` AI sungguhan (apakah benar-benar menurun untuk role tidak sejalan, bukan cuma persentase skill) tidak bisa diverifikasi keyless — sama seperti gap kualitas-AI Task 13, butuh smoke-test founder di production. `jobMatchHint`/completionType kelima di `generateQuest` juga tidak bisa diverifikasi lewat prompt AI sungguhan tanpa API key — API test memverifikasi MEKANISME hint (dihitung server, per-goal, deterministik), bukan kepatuhan AI terhadap instruksi prompt itu sendiri.

## 27. Redesign layar Login/Signup — cinematic login (design handoff, 12 Agustus) (SELESAI)

**Konteks & sumber**: `design_handoff_cinematic_login/` (README.md + prototype HTML interaktif + screenshots). `renderAuth()` dan style auth di `public/styles.css` di-rebuild total: glass panel di atas scene malam ambient (langit gradient + 40 bintang twinkle + portal arch + dune siluet + pilgrim + parallax mouse), animasi pintu di tombol submit (idle → authenticating: pintu terbuka rotateY 62° + figur berjalan masuk + glow pulse → celebrating: crossfade hijau + centang overshoot → success), lalu cinematic full-screen (portal menyala → pilgrim mengecil masuk → camera push scale 1.18 → amber flash → crossfade). Timing produksi sesuai catatan handoff: authenticating berlangsung selama request `/api/login`//`/api/signup` NYATA (floor ~500ms), celebrating hold tetap ~900ms berapa pun kecepatan network. Cinematic berakhir di layar home SUNGGUHAN (bukan placeholder): `/api/state` di-prefetch selama celebrating+cinematic, render dashboard/onboarding terjadi DI BAWAH flash penuh (elemen flash `position:fixed` di-reparent ke `<body>` supaya selamat dari swap `root.innerHTML`), lalu flash fade-out = crossfade beneran. `prefers-reduced-motion`: seluruh tabel fase di-skip, crossfade sederhana ~700ms, tanpa parallax/scale/flash. Font layar ini: Instrument Serif (headline, satu weight, tidak di-fake-bold) + Manrope — ditambahkan ke Google Fonts link; Fraunces/Inter/Plex Mono tetap dipakai layar lain.

**Deviasi sengaja dari handoff (di-flag, bukan diam-diam)**:
- **Privacy consent (Task 4) TETAP ADA di signup** — handoff menghilangkannya, tapi fitur compliance tidak bisa dihapus oleh redesign visual; di-restyle kompak menyatu dengan panel baru, tetap menggerbang tombol Daftar.
- **"Ingat saya" visual/lokal saja** — cookie-session backend fixed 30 hari, tidak ada mode sesi pendek untuk unchecked; **"Lupa password?" inert** — belum ada route reset; **Google/Apple visual-only** (handoff sendiri menyebut ini) — backend email/password saja.
- **Copy celebrating mode signup**: "Selamat datang di Eleva!" (bukan "Selamat datang kembali!" yang salah konteks untuk akun baru; handoff hanya menspesifikkan copy login).
- **Drift token**: daftar token README (`#14141c`/`#e8a33d` dst) adalah nilai PRA-audit 10 Agustus; styles.css live sudah `#0d0c12`/`#d8a355`. Layar auth memakai nilai literal handoff (scene malam self-contained, scoped `.auth-`), drift-nya di-flag balik ke design, tidak direkonsiliasi sepihak.
- **Pertanyaan terbuka README soal validasi kode beta terjawab**: `BETA_CODE` adalah env var rahasia server — klien hanya cek keberadaan, server yang menolak kode salah (perilaku existing, tidak berubah).

**Kode**: `public/app.js` (renderAuth rebuild + `runAuthCinematic` + state `authUiState`/fase; semua transisi pasca-render lewat toggle class DOM langsung, bukan re-render — re-render akan me-restart animasi CSS dari nol), `public/styles.css` (blok `.auth-` namespaced + keyframes + media query reduced-motion), `public/index.html` (tambah Instrument Serif + Manrope).

**Diverifikasi** (`tests/auth.e2e.js`, Playwright, keyless, Postgres scratch — 9 skenario): layout+copy login (termasuk font Instrument Serif terkomputasi); toggle show/hide password tanpa kehilangan nilai; validasi inline field kosong; kredensial salah → animasi pintu tahan ≥500ms lalu error server inline + tombol reset; signup menampilkan kode beta+consent gate (submit disabled sampai consent); kode beta kosong → error inline tanpa request; signup nyata → authenticating → celebrating → cinematic (portal lit, camera push) → onboarding SUNGGUHAN; login akun ber-profil → dashboard sungguhan ([data-reflect-id] muncul, flash dibersihkan); reduced-motion → tanpa camera push, handover < 3.4s. Suite lama (`practicetest.e2e.js`, `livelihood.e2e.js`) dijalankan ulang — tidak ada regresi.

**Gap yang diketahui**: kualitas motion-feel parallax/cinematic (easing per-layer) adalah creative spec per README, diverifikasi struktural bukan piksel; split sistem tipografi (Instrument Serif+Manrope di auth vs Fraunces+Plex Mono di layar lain) menunggu keputusan design satu arah; "Ingat saya" tersimpan di state lokal tanpa efek backend — kalau mau bermakna butuh mode sesi pendek server-side.

## 28. SOMA Nutrition Implementation Brief, Part A — regression: "Audit Fondasi Pemulihan" stale reflective classification (SELESAI)

**Konteks & sumber**: `SOMA_Nutrition_Implementation_Brief.pdf`, Part A (12 Agustus). Founder-reported regression: a recovery-themed quest ("Audit Fondasi Pemulihan") still shows the free-text/12-word growth-gate form instead of the structured sleep/water/protein/pain fields Task 7d item 5 already built. Root cause confirmed exactly per the brief: `completionType`/`structuredKind` are decided ONCE by the model at generation time (`generateQuest`, `server/claude.js`); `normalizeCompletionType` was a shape-only safety net (validates a known combo, downgrades anything else to `"reflective"`) — it never checked WHETHER a "reflective" classification made sense given the quest's own content. The Task 7d prompt instruction already existed and was correct; the gap was purely in the code-side backstop.

**Apa yang dibangun (dua lapis, per brief)**:
1. **Systemic (defense-in-depth)**: `looksRecoveryThemed(quest)` (`server/claude.js`) — substring match against `title`+`description` for the 8 keywords the brief specified (tidur, hidrasi, protein, pemulihan, cedera, kram, makan, nutrisi). Wired into `normalizeCompletionType`'s existing fallthrough: reached only when the model's `completionType` didn't match any of the 5 known valid combos (cardio/gym/recovery structured-physical, practice-test, job-match-analysis, job-application-submit) — at that point, before downgrading to `"reflective"`, the quest is checked against the keyword list and forced to `structured-physical`/`recovery` if it matches. Deliberately simple substring matching, not NLP — a false positive just means an extra Body/recovery quest gets the richer structured form instead of free text, never a worse outcome (same principle Task 7d itself states). Does NOT touch the cardio/gym/practice-test/job-match/job-application branches — those all `return` earlier in the function, so a coincidental keyword match inside an already-valid classification can never hijack it (verified in tests).
2. **Data-level**: `scripts/fix-stale-recovery-quests.js` — standalone maintenance script (dry-run by default, `--apply` to write) that scans all OPEN quests (`reflection IS NULL`, `is_meta = false`) for the same stale-classification pattern and fixes them via the identical `normalizeEvidenceSchema` code path a fresh generation uses. Scoped to open quests only — a quest the user already completed has real evidence already tied to whatever form was shown; rewriting it after the fact changes nothing the user experienced. **NOT YET RUN AGAINST PRODUCTION** — this Claude Code session has no production `DATABASE_URL`/credentials; verified end-to-end against the local scratch DB (seeded the exact reported quest shape, dry-run listed it, `--apply` fixed it, re-run confirmed idempotency — zero rows found on the second pass). Running it for real requires either the founder executing it with the production connection string, or a session with that access.
3. **Regression coverage**: `tests/recoveryregression.js`, named after the reported quest per the brief's explicit instruction.

**Backend**: `server/claude.js` (`looksRecoveryThemed` + `normalizeCompletionType` fallthrough branch, exported for the script/tests to reuse). `scripts/fix-stale-recovery-quests.js` (new file, new `scripts/` directory).

**Diverifikasi** (`tests/recoveryregression.js`, unit, no DB/server needed — 8 tests, all pass; existing `practicetest.js`/`livelihood.js` suites re-run clean, no regressions from touching `normalizeCompletionType`): the exact reported quest (title="Audit Fondasi Pemulihan", recovery-flavored description) forces `structured-physical`/`recovery`; keyword-in-description-alone and each of kram/hidrasi individually also trigger it; an already-correct quest passes through untouched; a cardio quest with an incidental "makan" mention is NOT hijacked (proves the backstop only fires on the fallthrough path); a genuinely reflective quest with no recovery keywords still downgrades to reflective as before (no behavior change for the common case); practice-test/job-match-analysis/job-application-submit are never hijacked by keyword coincidence; `normalizeEvidenceSchema` downstream correctly builds the recovery form shape after the backstop fires. Script itself verified end-to-end against local scratch Postgres (seed → dry-run → apply → verify → idempotent re-run).

**Gap yang diketahui, dicatat bukan ditutup**: the specific production quest instance the founder saw is still stale — the script exists and is tested, but has not been run against production from this session (no credentials). "Confirm whether other currently-open quests have the same stale classification" (brief item 1) is answered by the script's dry-run mode, but that dry-run has only been executed against the local scratch DB, not production — the actual production count is unknown until someone with DB access runs it there.

## 29. SOMA Nutrition Implementation Brief, Part B — full build: PROGRESSIVE lifecycle, food logging, Nutrition page (SELESAI)

> Diagram alur lengkap (WAJIB dibaca sebelum menyentuh stack SOMA/Nutrition): [`docs/soma-nutrition-flow.md`](docs/soma-nutrition-flow.md)

**Konteks & sumber**: `SOMA_Nutrition_Implementation_Brief.pdf`, Part B (12 Agustus) — 10 build item berurutan, dieksplisit oleh brief sebagai "multi-file, multi-layer build, bukan single-session patch", direkomendasikan jadi implementation pass sendiri terpisah dari Part A. Founder memilih dikerjakan LANGSUNG di sesi yang sama setelah Part A. Klarifikasi tambahan dari founder di sesi ini (sebelum implementasi dimulai): screenshot production menunjukkan quest "Audit Fondasi Pemulihan" (Part A) menampilkan picker Cardio/Gym/Recovery, bukan free-text — didiagnosis sebagai manifestasi lain dari root cause Part A yang sama (quest tersimpan stale `completionType: "reflective"`, belum tersentuh fix Part A karena fix itu perlu dijalankan manual ke production DB oleh founder), BUKAN bug baru. Founder lalu meminta food entry berupa "makan pagi/siang/malam" + tombol foto — brief tertulis EKSPLISIT mengecualikan "AI camera recognition" dari scope round ini (item 5: "Camera/voice entry: out of scope"). Konflik ini di-flag balik ke founder via AskUserQuestion SEBELUM baris kode ditulis; keputusan founder: **search sebagai jalur utama (sesuai brief), foto sebagai jalur sekunder/opsional di atasnya** — bukan salah satu meniadakan yang lain.

**Apa yang dibangun (10 item brief, plus deviasi yang di-flag)**:
1. **Rename META "Body" → SOMA (label saja).** `META_TOOLS` (`public/app.js`): tool key `body`→`soma`, label "Fisik / Lari"→"SOMA", desc diperluas mencakup nutrisi. Stat `body` TIDAK berubah nama/kunci — murni kosmetik di layer UI.
2. **SOMA landing/routing.** `activeSomaQuest(allOpenQuests, mode)` menghitung quest META (bukan Today's Trial) yang masih ACTIVE per domain (Activity = `structured-physical`, Nutrition = `nutrition-log`). 0 aktif → mode picker (Activity/Nutrition); tepat 1 aktif → skip picker, langsung resume; 2+ aktif → picker tetap tampil, dibadge "sedang aktif". Activity's flow (kind picker Cardio/Gym/Recovery) sama sekali TIDAK diubah, cuma dipindah jadi anak dari picker mode baru ini.
3. **Lifecycle type PROGRESSIVE** (`server/nutrition.js`, modul deterministik baru — tidak pernah memanggil AI). `initProgressiveState`/`applyContribution`/`evaluateEndOfDay`/`progressLabel`. Field baru di quest jsonb: `lifecycleType` ("session" untuk SEMUA completionType lama, TIDAK disentuh; "progressive" khusus nutrition-log) + `progressive: {requiredContributions, completedContributions, primaryMetric, targetValue, currentValue, evidenceComplete, targetMet, status}`. `status` ACTIVE→COMPLETED hanya bisa terjadi MID-DAY (begitu `evidenceComplete && targetMet` sama-sama true di satu `applyContribution`) — ATTEMPTED/INCOMPLETE HANYA PERNAH dihasilkan `evaluateEndOfDay`, tidak pernah oleh submission tunggal manapun (supaya user yang sudah penuhi jumlah makan minimum tapi belum capai target tetap punya sisa hari untuk makan lagi, bukan langsung divonis "gagal").
4. **Food entry model + evidence** (`server/db.js` tabel `food_entries` baru: food_name/serving_amount/serving_unit/calories/protein/carbohydrates/fat/meal_type/source/date/recorded_at/quest_id nullable; `server/nutritionEntry.js` validasi deterministik — mealType enum ketat 4 nilai [sarapan/makan_siang/makan_malam/camilan], plausibility cap kalori/makro). Setiap submission persisten sebagai evidence sungguhan (BUKAN cuma state UI), reusable untuk AI analysis/character progression/quest generation berikutnya — sama prinsipnya dengan setiap completion type lain di app ini.
5. **Food search MVP + barcode (arsitektur siap, scanning belum disambungkan).** `server/db.js` tabel `foods` baru (25 item kurasi Indonesia — nasi/telur/ayam/tempe/dst, kolom `barcode` nullable unique), di-seed idempotent saat `init()`. `GET /api/foods/search?q=` (substring ILIKE, bukan API eksternal — sesuai "MVP" eksplisit di brief). `GET /api/foods/barcode/:code` bekerja penuh untuk kode yang diketik/ditempel; TIDAK ADA UI kamera scan (persis "interface/architecture ready, scanning infra belum wired" per brief). Camera/voice recognition TETAP di luar scope (brief eksplisit) — path foto (lihat di bawah) BUKAN itu, ini AI vision estimasi makro dari foto, sebuah fitur TERPISAH yang diminta founder secara verbal, bukan realisasi item barcode/camera brief.
6. **Nutrition page UI.** `nutritionFlowHTML()` (`public/app.js`) — layar flow khusus (pola sama jobMatchFlowHTML/practiceTestFlowHTML, BUKAN tab nav permanen baru — tidak ada redesign navigasi yang diminta): total kalori/protein/karbo/lemak hari ini (bar mono, bahasa visual Eleva existing — bukan ring/skeuomorphic ala Yazio), primary-metric quest di-emphasize warna accent + tanda ★, baris 4 waktu makan dengan tanda ✓ kalau sudah tercatat.
7. **Home quest card live progress.** `questSummaryCard` (`public/app.js`): quest nutrition-log SELALU menampilkan tombol "Lanjut Catat" (bukan "Mulai"), progress line format persis contoh brief ("Meals 2/3 · Protein 61/90g"), TIDAK PERNAH menampilkan CTA "Selesai" selagi target belum tercapai (brief eksplisit) — countdown 24 jam existing juga disembunyikan untuk quest jenis ini (tidak relevan untuk quest yang memang dirancang tetap terbuka sepanjang hari).
8. **End-of-day evaluation, lazy** (sama idiom kondisi/Decay yang sudah ada — tidak ada infrastruktur cron di app ini). `GET /api/state` (`server/index.js`): setiap quest PROGRESSIVE yang masih ACTIVE dan `date`-nya sudah bukan hari ini dievaluasi via `evaluateEndOfDay`, diresolusi lewat `resolveNutritionQuest` — fungsi SAMA yang dipakai jalur mid-day COMPLETED, memanggil `ai.processReflection` (ctx.nutritionResult baru, `server/claude.js`) — REUSE mekanisme growth existing, BUKAN flat-bump baru (prinsip "no arbitrary points" Task 7d/14 tidak dibuka lagi). `evidenceComplete` dan `targetMet` tetap dua boolean terpisah sampai ke growth-gate: statDeltas WAJIB diisi kalau evidenceComplete true, TERLEPAS targetMet — compliance dan pencapaian target adalah dua hal berbeda, tidak pernah digabung.
9. **Structured shortfall reason.** Karena resolusi PROGRESSIVE terjadi LAZY (tidak ada momen submit sinkron seperti structured-physical), `reflection.shortfallPrompt` (ATTEMPTED/INCOMPLETE saja) di-surface lewat `pendingNutritionShortfalls` di respons `GET /api/state` (fungsi baru `db.listPendingShortfalls`), ditampilkan sebagai banner chip di Home — REUSE penuh `POST /api/quest/shortfall-reason` (Task 7d item 6, TIDAK diubah sama sekali). Free-text opsional dari brief TIDAK diimplementasikan (dicatat sebagai gap, bukan ditutup diam-diam).
10. **Prompt update** (`server/claude.js`, `generateQuest`): completionType keenam `nutrition-log` ditambah ke kontrak JSON + field `progressive` baru, instruksi kapan memilihnya (goal eksplisit soal pola makan/nutrisi terukur) dan cara mengisi requiredContributions/primaryMetric/targetValue yang wajar. `normalizeProgressiveSchema` (defense-in-depth, pola sama `normalizeEvidenceSchema`/`normalizePracticeTestSchema`) memvalidasi/menambal usulan AI sebelum dipercaya — never trust the model's numbers directly.

**Fitur tambahan atas permintaan verbal founder (BUKAN dari brief tertulis, di luar 10 item, sengaja diberi labelnya sendiri)**: entry via foto (`POST /api/nutrition/analyze-photo`, `ai.analyzeNutritionPhoto`) — AI vision SATU foto makanan → SARAN food_name+macros, TIDAK PERNAH langsung tersimpan, user tetap review/edit di step confirm yang SAMA dengan jalur search sebelum benar-benar disimpan lewat `POST /api/nutrition/log` (satu jalur penulisan evidence, dua cara sampai ke situ).

**Backend**: `server/nutrition.js` (baru), `server/nutritionEntry.js` (baru), `server/db.js` (tabel `foods`+`food_entries` baru, seed idempotent, `updateQuestProgress`/`createFoodEntry`/`listFoodEntriesForQuest`/`listFoodEntriesForDate`/`searchFoods`/`getFoodByBarcode`/`listPendingShortfalls`), `server/claude.js` (`normalizeProgressiveSchema`, completionType keenam di prompt generateQuest, `ctx.nutritionResult` branch di processReflection, `analyzeNutritionPhoto`), `server/index.js` (`resolveNutritionQuest`, lazy end-of-day loop di GET /api/state, `pendingNutritionShortfalls` di respons, rute baru: `/api/foods/search`, `/api/foods/barcode/:code`, `/api/nutrition/log`, `/api/nutrition/entries`, `/api/nutrition/analyze-photo`, `/api/meta/start` tool `nutrition`).

**Frontend**: `public/app.js` (META_TOOLS rename, `metaSomaPicking`/`nutritionFlow` state, `activeSomaQuest`, `beginStructuredOrReflectiveFlow` — diekstrak dari handler `[data-reflect-id]` supaya bisa dipakai ulang jalur resume Activity, `openNutritionFlow`, `nutritionFlowHTML`, `nutritionTotalsHTML`, `nutritionShortfallHTML` banner, `questSummaryCard` progress line).

**Diverifikasi** (`tests/nutrition.js` unit+API 26 test, `tests/nutrition.e2e.js` Playwright 9 skenario — keduanya keyless, Postgres scratch lokal; SEMUA suite lama dijalankan ulang — `practicetest.js`/`.e2e.js`, `livelihood.js`/`.e2e.js`, `recoveryregression.js`, `auth.e2e.js` — tidak ada regresi):
- Unit (17): `initProgressiveState` clamp default + honor input valid; `applyContribution` akumulasi + COMPLETED hanya saat KEDUA boolean true; evidenceComplete-tanpa-targetMet TETAP ACTIVE (bukti eksplisit "tidak pernah ATTEMPTED mid-day"); `evaluateEndOfDay` ketiga kombinasi (COMPLETED/ATTEMPTED/INCOMPLETE) + no-op kalau sudah resolved; `progressLabel` format persis contoh brief; `nutritionEntry.validateFoodEntry` valid/mealType-invalid/kalori-implausible/makro-negatif/nol-diizinkan; `normalizeCompletionType` cabang nutrition-log.
- API-level (16): META nutrition session menghasilkan progressive shape persis default; food search substring match; barcode lookup + 404 kode tidak dikenal; log meal persist evidence + increment progressive; entries queryable per quest; **mencapai kedua boolean auto-resolve COMPLETED mid-day tanpa konfirmasi manual** (poin sentral brief item 8); quest resolved hilang dari openQuests; log ke quest yang sudah selesai ditolak; evidence cacat ditolak sebelum masuk counter; **quest ACTIVE basi (tanggal kemarin) diresolusi INCOMPLETE lewat lazy check di GET /api/state**; quest INCOMPLETE itu muncul di `pendingNutritionShortfalls`; **quest COMPLETED (shortfallPrompt jsonb null) TIDAK PERNAH nyasar ke situ** (regression guard spesifik untuk bug JSONB-null yang ditemukan & diperbaiki selama development — lihat di bawah); memilih alasan shortfall menghapusnya dari pending list; regresi META body/practice-test tidak tersentuh.
- Playwright (9): quest card Today's Trial nutrition menampilkan progress line + tombol "Lanjut Catat" (bukan "Mulai"); membuka quest menampilkan 4 pilihan waktu makan; cari "telur" → pilih hasil → step confirm; simpan → progress bertambah + entri masuk "TERCATAT HARI INI"; tutup flow → dashboard menampilkan progress terbaru; label META "SOMA" tampil (label lama hilang) + picker mode saat 0 aktif; pilih Nutrition → sesi META baru mulai dari 0/3; buka SOMA lagi → picker di-skip (tepat 1 aktif) → langsung resume.

**Bug ditemukan & diperbaiki SELAMA development (dicatat sebagai bagian dari proses, bukan disembunyikan)**:
- **JSONB null gotcha di `listPendingShortfalls`**: `reflection->'shortfallPrompt' IS NOT NULL` di Postgres bernilai TRUE bahkan ketika value-nya adalah literal JSON `null` (bukan SQL NULL) — quest COMPLETED (yang sengaja menyimpan `shortfallPrompt: null`) jadi ikut lolos filter, meng-crash SETIAP pemanggilan `GET /api/state` begitu satu saja quest nutrisi selesai (`Cannot read properties of null (reading 'reasons')`). Ditemukan lewat smoke test manual sebelum test suite formal ditulis, diperbaiki dengan tambahan `AND reflection->'shortfallPrompt' <> 'null'::jsonb`, dan dikunci jadi test case eksplisit di `tests/nutrition.js` supaya tidak regresi diam-diam.
- **`openNutritionFlow` menerima bentuk objek yang tidak konsisten**: dua titik panggil (SOMA meta-tool routing, resume quest aktif) mengirim bentuk row-DB bersarang (`{id, quest:{...}}`), satu titik panggil lain (`[data-reflect-id]`) mengirim bentuk yang sudah diratakan — `nutritionFlow.quest.progressive` jadi `undefined` di separuh jalur, bikin progress line diam-diam tidak pernah tampil. Ditemukan lewat Playwright e2e (bukan smoke manual), diperbaiki dengan menstandarkan SEMUA titik panggil ke bentuk row-DB, `openNutritionFlow` sendiri yang membongkarnya.

**Gap yang diketahui, dicatat bukan ditutup**: free-text opsional untuk shortfall reason (brief item 9) belum ada, cuma fixed-option chip. Kualitas AI sungguhan (matching goal ke primaryMetric/targetValue yang wajar di generateQuest, estimasi macro dari foto) tidak bisa diverifikasi keyless — sama seperti gap kualitas-AI di setiap fitur AI-dependent sebelumnya, butuh smoke-test founder di production. Barcode benar-benar tervalidasi hanya untuk 3 kode di seed data — belum ada database barcode nyata atau integrasi API eksternal. Foto tidak pernah disimpan (by design, sama prinsip job-match screenshots) — kalau AI salah tebak dan user tidak sempat koreksi sebelum submit, tidak ada cara audit ulang dari foto aslinya.

## 30. Redesign layar META — "The Inner Realm" (design handoff, 12 Agustus) (SELESAI)

**Konteks & sumber**: `design_handoff_meta_inner_realm/` (README.md + `claude-code-prompt.txt` + prototype `.dc.html` + 3 screenshot + `meta-world-map.png`, disalin ke `public/assets/`). `metaScreenHTML()` di-rebuild total: dari grid 3 kartu datar (Fisik/Lari, Practice Test, Job Match) jadi peta dunia RPG yang bisa di-scroll — 3 klaster realm (LINGUA/SOMA/LABORA) diposisikan absolut di atas satu gambar full-bleed, tiap klaster punya sigil SVG kustom (bukan emoji, path disalin persis dari prototype), local readability vignette (radial-gradient gelap di belakang teks, bukan cuma text-shadow), kartu progress compact (ikon+judul+session-count+progress bar), avatar pathway di titik konvergensi peta (ring pulsing + label+baris sesuai pathway pengguna, palet warna final per README), dan placeholder realm terkunci ("???"). Hint card "Geser peta..." dismissible, persist via localStorage.

**Konflik penamaan SOMA vs Part B, ditanya dulu (bukan ditebak)**: prototype ini (predates Part B) menganggap SOMA = satu tool tunggal Fisik/Lari, padahal Part B (item sebelumnya, section 29) baru saja mengubah SOMA jadi mode-picker Activity+Nutrition. Ditanyakan via AskUserQuestion sebelum kode apa pun ditulis — jawaban founder: **klaster SOMA tetap 1 dari 3 realm (posisi/art sesuai prototype), tapi isinya 2 kartu progress bertumpuk (Fisik/Lari + Nutrition), masing-masing tap LANGSUNG ke tool-nya masing-masing (skip mode picker lama sepenuhnya)** — `metaSomaPicking` dan pickernya dihapus, `[data-soma-mode="activity"/"nutrition"]` sekarang jadi entry point langsung dari peta, bukan cuma dari dalam picker.

**Data wiring** (bagian yang README sendiri sebut ambigu, diverifikasi ke kode nyata sebelum dipakai, bukan ditebak):
- **Session count** — TIDAK ADA query "session count per realm" sebelumnya. Baru: `db.countSessionsSince(userId, completionType, sinceDateKey)` (`server/db.js`) — hitung quest ber-reflection (SESI = selesai, META atau Today's Trial sama-sama dihitung, sesuai bunyi literal brief "real practice-test/run/job-match session count", tidak dibatasi META-only) sejak sebuah tanggal. Window: SOMA (Activity & Nutrition) = kalender minggu ini (Senin), LINGUA/LABORA = kalender bulan ini — meniru copy asli prototype sendiri ("X sesi minggu ini" vs "X sesi bulan ini"). Progress bar tetap `/7` clamped, formula placeholder prototype dipertahankan verbatim (brief eksplisit mengizinkan "keep it simple, no fake milestone data" — target bulanan nyata berarti mengarang angka yang founder tidak pernah sebut).
- **Pathway field** — `state.pathway` (enum Architect/Warden/Weaver/Pilgrim/Specialist) TERNYATA belum pernah dikirim ke client sama sekali (`GET /api/state` cuma expose `pathwayNoun` turunannya) — ditambahkan (`pathway: fresh.pathway`) supaya avatar bisa baca pathway asli, bukan cuma noun-nya.
- **Hint-persistence pattern** — dicek dulu: TIDAK ADA `localStorage` di mana pun di codebase ini (help sheet lain murni in-memory, reset tiap reload). README mengizinkan "localStorage atau backend flag, yang mana pun sudah dipakai app ini" — karena tidak ada yang sudah dipakai, localStorage dipilih (opsi paling ringan, tanpa migrasi schema).

**Bug layout ditemukan & diperbaiki SELAMA verifikasi browser (bukan asumsi "pasti sudah beres karena disalin persis")**: posisi vertikal literal prototype (16/36/37/47/56/73.5%) diukur langsung pakai Playwright bounding-rect di lebar mobile nyata (360-420px) — kartu LINGUA tumpang tindih ke header SOMA, dan kartu SOMA/LABORA tumpang tindih ke avatar pathway (teks di atas teks, bukan cuma vignette blending). Root cause: tinggi konten tiap klaster (font-size dkk fixed-px) TIDAK ikut menyusut di lebar sempit, sementara jarak antar-anchor (persen dari tinggi peta) justru menyusut proporsional dengan lebar viewport — screenshot referensi handoff sendiri tampaknya diambil di preview pane yang jauh lebih lebar, menyembunyikan masalah ini. Diperbaiki dengan menaikkan `aspect-ratio` container peta (853/2650, bukan 853/1844 asli) — `object-fit:cover` menjaga SEMUA posisi persen tetap jatuh di titik seni yang sama persis (cuma sedikit di-crop di sisi kiri-kanan, tidak digeser vertikal) — lalu menghitung ulang anchor vertikal klaster/avatar/locked dari tinggi konten hasil ukur nyata, bukan angka literal prototype. Posisi horizontal (`left:`) dan semua token visual lain (warna/font/spacing/vignette/kartu) tidak disentuh. Diverifikasi ulang di 360/375/420/560px — semua gap positif.

**Kode**: `public/app.js` (`metaScreenHTML` rebuild, `PATHWAY_META`, `realmIconSVG`/`realmProgressCardHTML`/`realmClusterHTML`, `metaSessionPct`, handler `[data-meta-tool]`/`[data-soma-mode]` disederhanakan — cabang "soma" lama dihapus karena sudah tidak pernah dipanggil, `#metaHintClose` baru), `public/styles.css` (namespace `.meta-realm-` baru, `.meta-tool-card`/`.meta-grid` lama dihapus), `server/index.js` (`startOfWeekKey`/`startOfMonthKey`, `metaSessionCounts` + `pathway` di response `GET /api/state`), `server/db.js` (`countSessionsSince`), `public/assets/meta-world-map.png` (aset baru).

**Diverifikasi**: `tests/nutrition.e2e.js` (skenario META SOMA lama, yang mengasumsikan mode picker, ditulis ulang ke perilaku direct-tap baru — 3 skenario, semua lolos), `tests/practicetest.e2e.js` (`[data-meta-tool="practice-test"]` tidak berubah, masih lolos tanpa perubahan). Suite lama (`auth.e2e.js`, `livelihood.e2e.js`, `nutrition.js`, `practicetest.js`, `recoveryregression.js`) dijalankan ulang — tidak ada regresi. Verifikasi visual manual (Playwright screenshot, bukan cuma DOM assertion) di 360/375/420/560px + klik nyata dari kartu LINGUA sampai masuk flow Practice Test.

**Gap yang diketahui**: `design_handoff_homepage_redesign/` dan `design_handoff_onboarding_redesign/` (dalam bundle yang sama) belum disentuh — README bundle sendiri menandai `meta_inner_realm` sebagai target utama, dua lainnya materi referensi. Kartu SOMA (Fisik/Lari) dan LABORA (Job Match) bersinggungan tipis di tepi pada lebar sangat sempit (~360px) — warisan matematika horizontal prototype asli (`left:22%`/`left:79%`, tidak diubah sesuai DO NOT list), kosmetik minor bukan tumpang tindih teks.

## 31. META target-recommendation follow-up — "World Map shows Target, Realm page shows Tools" (founder feedback, 12 Agustus) (SELESAI)

**Konteks**: Feedback founder langsung setelah section 30 di-deploy — kartu di world map (Fisik/Lari, Practice Test, Job Match) terasa seperti launcher aplikasi, bukan menunjukkan apa yang sedang dikejar user. Rule baru: **World Map menampilkan Target (goal + progress nyata user), Realm page menampilkan Tools**. Contoh founder: kartu SOMA seharusnya baca "10K Run Preparation, 3.21/10 km", bukan "Fisik/Lari, 4 sesi minggu ini".

**Keputusan yang ditanya dulu (bukan ditebak)**: apakah "AI menyarankan, user approve" itu mekanisme BARU khusus META, atau cukup pakai flow "Target Berikutnya" (A/B/C picker) yang sudah ada — ditanya via AskUserQuestion karena keduanya valid secara teknis tapi beda scope kerja jauh. Jawaban founder: **flow approve baru, terpisah dari Target Berikutnya** — punya goal target lewat mekanisme lama TIDAK otomatis membuatnya "active" di META, harus ada tap approve eksplisit di kartu "ELEVA MENYARANKAN".

**Arsitektur**: setiap realm (soma/lingua/labora) kartunya adalah salah satu dari 3 state (`server/metaTargets.js`, murni derivasi dari data yang sudah ada, tidak pernah mengarang angka):
- **active** — user sudah approve satu goalIndex sebagai target realm ini (`meta_active_targets` kolom baru, `{realm: goalIndex}`). Tap kartu → buka Realm detail page (Tools).
- **recommend** — ada goal yang domain-nya cocok (lihat di bawah) tapi belum di-approve — kartu "ELEVA MENYARANKAN" + tombol "Jadikan target aktif" (`POST /api/meta/target/confirm`, domain divalidasi ULANG di server, tidak percaya `realm` dari client mentah-mentah).
- **empty** — tidak ada goal yang cocok domain-nya sama sekali — CTA inline nambah goal baru (`POST /api/goals`, reuse array `goals` yang sudah ada, cap 3 goal yang sama seperti onboarding).

Setiap 3 state tetap punya link "Lihat tools →" ke Realm detail page — tools TIDAK PERNAH digembok di belakang approval target (target itu framing motivasional, bukan gate fungsional). Ditemukan sendiri lewat e2e test yang gagal saat draft pertama menggembok Nutrition di balik kartu "active" — diperbaiki sebelum commit.

**Domain inference** (goal tidak punya tag realm eksplisit di mana pun — dibaca dari data yang SUDAH ada): `goalTargets[goalIndex].kind` cardio/gym → soma, qualified-applications/livelihood-funnel → labora; `practiceTest[goalIndex]` terisi (tracks/history) → lingua. Progress kartu: SOMA baca submission structured-physical TERBARU dibanding target tersimpan (app ini tidak pernah nyimpen running-total buat cardio/gym di mana pun — beda dari qualified-applications/livelihood-funnel yang memang punya running count/value); LABORA baca `metrics.currentCount/targetCount` atau `currentValue/targetValue` langsung; LINGUA pakai `practiceTest.js`'s `currentTargetFor()` yang sudah ada (band estimate vs target band, dipakai juga buat AI prompt context — bukan logic baru).

**Realm page (Tools)**: SOMA → Movement (cardio/gym, skip Recovery dari picker karena jadi baris sendiri), Recovery (langsung ke form recovery, skip picker), Nutrition (sama seperti sebelumnya). LINGUA → Reading/Listening (langsung preset kind, skip kind-picker lama — kind-picker sendiri jadi unreachable, dibersihkan dari `[data-meta-tool]` handler), Writing/Speaking (coming-soon, field `tracks.writing/speaking` sudah ada di schema Task 13 tapi belum ada generation path). LABORA → Job Match (unchanged), Applications (informational — cuma muncul otomatis setelah analisis qualified, bukan tombol), CV/Skill Gap/Interview (coming-soon). Baris coming-soon/informational styling baru (`.meta-realm-card-soon`/`.meta-realm-card-info`) — visible-but-unrevealed, sama prinsip locked-realm di section 30, bukan disembunyikan diam-diam.

**Kode**: `server/db.js` (kolom `meta_active_targets`, `setMetaActiveTarget`, `addGoal`), `server/metaTargets.js` (BARU — domain inference + `cardForRealm`), `server/index.js` (`POST /api/meta/target/confirm`, `POST /api/goals`, `metaTargets` di `GET /api/state`), `public/app.js` (`targetCardHTML`/`metaRealmDetailHTML`/`metaRealmToolsHTML` baru, `metaScreenHTML` branch ke detail page via `metaRealmOpen`, handler `[data-meta-realm-open]`/`[data-meta-target-confirm]`/`[data-meta-goal-submit]`/`[data-lingua-track]` baru, `[data-soma-mode]` diperluas terima mode "recovery", tab META reset `metaRealmOpen` ke null tiap dibuka ulang — konvensi tab-bar "reselect balik ke root", beda dari `jobMatchFlow`/`nutritionFlow` yang sengaja TIDAK reset), `public/styles.css` (state kartu baru + `.meta-realm-detail` sub-page).

**Diverifikasi**: `tests/nutrition.e2e.js` (skenario META ditulis ulang — buka SOMA dari empty state via "Lihat tools", bukan direct card lagi), `tests/practicetest.e2e.js` (skenario META ditulis ulang — LINGUA detail page, Reading preset kind, kind-picker lama dikonfirmasi ter-skip). Suite lama (`auth.e2e.js`, `livelihood.e2e.js`, `nutrition.js`, `practicetest.js`, `recoveryregression.js`) dijalankan ulang — tidak ada regresi. Verifikasi visual manual (Playwright screenshot + seed data langsung ke `goal_targets`/`practice_test` buat mensimulasikan ketiga state, karena AI keyless tidak bisa menghasilkan completionType terarah) untuk recommend/active/detail SOMA, detail LABORA (semua 5 baris tool), detail LINGUA (termasuk band estimate "Establish Listening baseline" ke-compute benar dari data seed).

**Gap yang diketahui**: judul kartu pakai teks goal APA ADANYA (dipotong ~42 karakter), bukan nama target yang di-distill AI seperti contoh founder ("10K Run Preparation") — brief tidak minta langkah AI baru buat itu, dan menambahkannya berarti scope creep di luar apa yang diminta. SOMA tidak pernah punya running-progress sungguhan untuk cardio/gym di app ini (lihat domain inference di atas) — pendekatan "submission terakhir vs target" adalah aproksimasi terbaik dari data yang ada, bukan tracking kumulatif asli. Writing/Speaking/CV/Skill Gap/Interview murni placeholder visual, tidak ada rencana build di brief ini.

## 32. Login screen v2 — pixel-exact port (design handoff 13 Agustus, design_handoff_login_screen) — REPLACES login v1 (SELESAI)

**Konteks & konflik ditemukan sebelum ngoding**: brief baru ini eksplisit bilang "codebase belum ada login sama sekali" — ternyata TIDAK akurat: cinematic login v1 (section 27, 12 Agustus) sudah ada dan live di production. Ditanya dulu via AskUserQuestion sebelum kode apa pun disentuh: founder konfirmasi **desain baru ini menggantikan v1 sepenuhnya**, bukan screen kedua yang berdampingan. Seluruh namespace CSS `.auth-` (scene malam/portal arch digambar CSS/pilgrim/dune, font Instrument Serif+Manrope) dihapus, diganti `.auth2-` — Instrument Serif dan Manrope juga dihapus dari `public/index.html` (tidak dipakai layar lain), Fraunces dapat tambahan weight 500 dan Inter weight 700 (kebutuhan baru desain ini).

**Fidelity**: port pixel-exact sesuai instruksi brief — setiap warna/spacing/font-size disalin literal dari `Eleva Cinematic Login.dc.html` (ground truth di atas README kalau ada konflik), termasuk detail yang brief sendiri tandai sebagai hasil beberapa ronde review ("jangan disederhanakan"): gap label-ke-input PASSWORD harus persis sama dengan EMAIL (6px, pakai `align-items:center` bukan `baseline` + `line-height:1` di input supaya titik password tidak turun), spacer `flex:1;min-height:230px` yang menempatkan portal arch pas di antara tagline dan field EMAIL, clamp headline yang sengaja lebih kecil dari insting pertama (supaya "DATANG KEMBALI" tidak wrap ke baris ke-3). 4 aset PNG (`eleva-diamond.png`, `eleva-stickman.png`, `eleva-portal-icon.png`, `eleva-login-bg-crop.png`) dipakai apa adanya di `public/assets/`, tidak digambar ulang.

**State machine tombol MASUK/DAFTAR** (5 state persis nama dari brief — `idle → loading → success → entering → complete`, plus jalur `error` terpisah): loading pakai floor 800ms di sekitar request REAL (bukan timer mock statis referensi — translasi yang sama seperti login v1 lakukan dari prototype-mock ke network sungguhan), success hold ~900ms (crossfade hijau, `/api/state` di-prefetch), entering hold ~900ms (stickman geser+fade, flash amber), complete = render REAL dashboard/onboarding di bawah flash yang masih opaque (flash `position:fixed`, di-reparent ke `<body>` supaya selamat dari `root.innerHTML` swap — trik yang sama persis dengan v1), baru flash fade-out = crossfade beneran. `prefers-reduced-motion`: SEMUA hold di-cap ~250ms (bukan skip fase, sesuai pendekatan referensi sendiri) + override CSS global `transition-duration:.05s!important` di dalam `.auth2-root` — jauh lebih simpel dari v1 yang butuh cabang kode terpisah.

**Deviasi dari handoff, semua sengaja dan ditandai** (referensi `.dc.html`-nya sendiri HANYA implementasi LOGIN — "DAFTAR" di baris bawah tidak punya `onClick` sama sekali, tidak ada state signup di component manapun):
- **Mode DAFTAR (signup) dibangun dari nol** — reuse semua token visual layar ini apa adanya, tambah field KODE BETA + consent checkbox (styling baru, disamakan ke palet gelap-emas layar ini) — dipertahankan dari v1 karena alasan fungsionalnya identik: `BETA_CODE` menggerbang pembuatan akun di server, dan privacy consent (Task 4) adalah fitur compliance yang tidak boleh hilang gara-gara redesign visual manapun.
- **"Lupa password?" tetap inert** — referensi `.dc.html` sendiri juga tidak punya `onClick` untuknya, jadi ini bukan deviasi baru, cuma konfirmasi memang sengaja non-fungsional (belum ada route reset di backend).
- **Tidak ada "Ingat saya"/OAuth/separator "ATAU"** — brief eksplisit minta ini dihapus (bagian dari desain baru, bukan deviasi).

**Kode**: `public/app.js` (`renderAuth()`+`runAuthEntering()`+`authHelpModalHTML()` full rebuild, state `authUiState`/`authHelpOpen`/`AUTH_LOADING_FLOOR`/`AUTH_SUCCESS_HOLD`/`AUTH_ENTERING_HOLD`/`authHoldMs()`; `authRemember`/`AUTH_PHASE_TIMINGS` v1 dihapus), `public/styles.css` (namespace `.auth2-` baru menggantikan seluruh blok `.auth-` v1), `public/index.html` (font import diperbarui, Instrument Serif/Manrope dihapus), `public/assets/eleva-diamond.png`/`eleva-stickman.png`/`eleva-portal-icon.png`/`eleva-login-bg-crop.png` (aset baru).

**Diverifikasi**: `tests/auth.e2e.js` ditulis ulang total (11 skenario: layout+copy, help modal, show/hide password, validasi inline, error kredensial salah dengan floor 800ms, toggle signup, validasi kode beta, signup sungguhan sampai onboarding REAL, login akun ber-profil sampai dashboard REAL, reduced-motion di bawah 2 detik, no-overflow di lebar 360px) — semua lolos. Verifikasi visual manual (Playwright screenshot) di 360/390px, termasuk toggle login↔signup, help modal, dan flow signup sungguhan sampai layar onboarding nyata ter-render. Suite lain (`livelihood.e2e.js`, `nutrition.e2e.js`, `practicetest.e2e.js`, dan seluruh unit/API suite) dijalankan ulang — tidak ada regresi.

**Gap yang diketahui**: parallax pointer-follow pada background disederhanakan jadi satu translate pada gambar saja (bukan multi-layer depth v1) — brief sendiri menandai ini "optional polish, low priority, skip if it adds complexity". Signup headline/tagline/copy field beta+consent adalah ekstrapolasi saya (brief tidak menspesifikasikannya sama sekali), bukan nilai literal dari handoff — kalau desain punya versi signup sendiri nanti, ini perlu direkonsiliasi.

## 33. Redesign layar Chapter Analysis — pisah jadi 3 layar berurutan (design handoff, `design_handoff_chapter_analysis`) + 2 field AI baru: `insightRows`/`pattern` (SELESAI)

**Konteks & keputusan arsitektur yang dikonfirmasi lebih dulu**: handoff baru menunjukkan Chapter Analysis sebagai layar mandiri (header+progress 3-segmen, headline, radar before/after, ringkasan perubahan, 2 baris insight, kartu trait, CTA "Lihat Pathway-ku →") — TANPA carousel Pathway di layar yang sama. Sejak v7 (bagian 133-136 di atas), radar+carousel memang sengaja digabung SATU layar per instruksi PRD lama. Karena ini kontradiksi produk yang nyata (bukan sekadar re-skin visual), ditanya dulu ke founder lewat AskUserQuestion sebelum ngoding — dikonfirmasi eksplisit (kedua opsi yang dipilih adalah rekomendasi): **(1) pisah jadi 3 layar berurutan** — Chapter Analysis (baru) → Pathway carousel (konten tarot lama, sekarang mandiri) → Goal Capture (sudah ada, tidak berubah) — progress bar 3-segmen menjelaskan dirinya sendiri begitu dipecah begini (segmen 1/2/3 aktif berurutan); **(2) `insightRows`/`pattern` jadi field AI baru sungguhan** (bukan teks statis dari mockup) — "Yang Eleva lihat" (2 baris) dan "Pola yang terlihat" (judul+deskripsi) tidak punya field yang cocok di respons `generateChapterAnalysis()` lama (`insight` paragraf panjang, `subPathway` katalog tetap 15 entri, `pathwayBlurb` khusus Pathway) — ditambah sebagai field baru yang genuinely personalized per pengguna, konsisten dengan prinsip lama PRD (bagian v4) yang eksplisit menolak bank teks generik.

**`server/claude.js`**: `generateChapterAnalysis()` prompt/schema tambah `"insightRows": [string, string]` (2 observasi pola perilaku pendek, sudut pandang beda — reaksi/insting vs fokus/prioritas — tunduk aturan Pathway≠Chapter dan larangan sebut nama sumbu literal yang sudah ada) dan `"pattern": {"title": string, "description": string}` (judul 2-5 kata Bahasa Indonesia natural, bukan terjemahan literal dari contoh Inggris di handoff). Validasi pakai prinsip "patch, jangan buang" yang sama seperti `normalizeSubPathway` — field informasional ini kalau bentuknya salah dari AI, ditambal pakai fallback deterministik (`fallbackInsightRows`/`fallbackPattern`, tabel frasa per-axis `AXIS_PATTERN_PHRASE` dan per-Pathway `PATHWAY_PATTERN_TITLE`), BUKAN membuang seluruh respons seperti `pathway`/`pathwayBlurb`/`rawPathwayTop2` yang tetap hard-fail. `fallbackChapterAnalysis()` (mode tanpa API key) juga dapat kedua field ini, selalu terisi (tidak pernah null, beda dari `secondaryTrait` yang opsional).

**`public/app.js`**: `adaptivePhase` dapat nilai baru `"pathway"` (carousel yang dipisah) — `"analysis"` dipakai ulang untuk layar summary baru (penerus langsung layar gabungan lama, tidak ada perubahan di orkestrasi fetch `beginPathwayBridge()`). Urutan jadi `bridge → card → analysis → pathway → goals`. `renderRadarComparisonSVG()` (dipakai HANYA di layar summary ini) ditulis ulang total ke geometri persis handoff — 5 ring (dulu 3), dot marker before/after, glow drop-shadow di polygon "after", warna hex literal persis handoff — pakai konstanta lokal sendiri (`CH_*`), BUKAN `POLY_CENTER`/`POLY_MAXR` milik radar interaktif (`renderPolygonSVG()`, layar terpisah, sama sekali tidak disentuh). Class CSS baru `.chapter-radar-*` dipakai (bukan `.poly-ring`/`.poly-axis`/`.poly-label` — class-class itu, per komentar `styles.css` sendiri, memang dipakai bareng radar interaktif, jadi mengubah warnanya di sini akan diam-diam ikut mengubah tampilan radar interaktif). `restoreOnboardingDraft()` diperluas daftar resume-nya (`analysis`/`pathway`/`goals`) supaya refresh di tengah layar carousel yang baru dipisah tidak salah jatuh ke fetch skenario-card baru. `chapterProgressHTML()` (array lokal `CHAPTER_FLOW_PHASES`, BUKAN `ONBOARD_STEPS` yang itu punya layar nama/radar sendiri) dipasang di ketiga layar. Tombol "Kembali" di Goal Capture (`backToPathway`) targetnya diganti dari `"analysis"` ke `"pathway"` — supaya kembali ke layar SEBELUMNYA langsung (carousel), bukan dua langkah ke belakang ke summary.

**Konten `insight`/`secondaryTrait` lama TIDAK lagi ditampilkan** di layar summary baru (digantikan visual oleh `insightRows`/`pattern`) — TAPI kedua field itu tetap ada di skema/respons dan tetap dikirim saat submit (`insight` masih jadi `originStory`, `secondaryTrait` masih dikirim) — ini secara eksplisit menyempitkan baris DoD lama di bagian 358 ("Chapter Analysis menampilkan insight + Pathway Recommendation + Secondary Trait") yang mengasumsikan tampilan lama. Catatan kalibrasi ("Kalibrasi radar: ...") dan ketegangan kunci ("Ketegangan kunci: ...") dari bagian v7 (231-232) TETAP dipertahankan di layar summary baru (posisi setelah baris ringkasan perubahan) — deviasi sengaja dari daftar block literal handoff (yang tidak menunjukkan baris ini), supaya requirement PRD lama yang sudah terverifikasi tidak diam-diam hilang.

**Warna**: semua nilai hex literal persis handoff (`#e5aa50`/`#f0c274` emas, `#f1ebdd`/`#e7e2d6` gading, `#95919e` muted, `#aaa6b2` abu, `#e0656a` ruby, `#1a1409` navy, gradient background `#17161d`/`#0d0d12`/`#08080c`) — TIDAK menimpa token global `--accent`/`--muted`/dst yang dipakai di seluruh app, mengikuti presedan `.auth2-*` (Login v2, bagian 32) dan layar bridge onboarding: class baru berskop `.chapter-*` dengan literal sendiri untuk handoff yang menuntut fidelity persis.

**Diverifikasi**: manual browser end-to-end (signup → nama → radar → 2 kartu skenario keyless → layar summary → tap CTA → carousel → pilih kartu → Goal Capture), screenshot 390×844 dicek visual (radar 5-ring+dot marker+glow render benar via `page.evaluate` atribut SVG, bukan cuma tebak dari gambar kecil), konten yang overflow area scroll (`fadeUp`) dikonfirmasi TIDAK overlap CTA (cek `getBoundingClientRect` — batas clip `overflow-y:auto` beres, bukan bug). `tests/onboarding-chapter-analysis.e2e.js` baru (5 skenario: markup layar summary+radar SVG ID `#polyCompareSvg`/`#polyShapeRaw`/`#polyShapeCalibrated` yang jadi requirement DoD lama, progress 1/2/3 segmen di 3 layar, CTA tidak re-fetch chapter-analysis, baris ketegangan-kunci cuma muncul kalau ada, tombol Kembali Goal Capture ke carousel bukan summary). `tests/onboarding-bridge.e2e.js`, `tests/onboarding-bridge-audio.e2e.js`, `tests/onboarding-draft-resume.e2e.js` diupdate (titik tunggu `.tarot-carousel` sekarang lewat `.chapter-headline`+tap CTA dulu) + 1 skenario baru (refresh di tengah layar carousel resume langsung, regresi guard untuk perubahan `restoreOnboardingDraft()`). Full regression suite (semua `tests/*.e2e.js` + `practicetest.js` + `recoveryregression.js`) dijalankan ulang — satu kegagalan awal (`onboarding-name.e2e.js`) terbukti flaky resource-contention dari 9 server+browser paralel sekaligus, lolos bersih saat dijalankan sendiri, bukan regresi nyata.

## 34. Chapter Analysis: fix section AI kosong pada resume + polish visual (feedback founder dari HP asli, bagian 33 baru saja deploy) (SELESAI)

**Bug nyata (bukan sekadar polish)**: founder tes deploy bagian 33 di HP asli — "Yang Eleva lihat" dan "Pola yang terlihat" tampil KOSONG total (cuma label seksi + shell ikon, tanpa teks). Akar masalah, dikonfirmasi lewat baca kode langsung: draft onboarding disimpan SERVER-SIDE (`saveOnboardingDraftNow()` → `POST /api/onboarding/draft` → kolom `users.onboarding_draft jsonb`, dipulihkan lewat `GET /api/state` → `boot()` → `restoreOnboardingDraft()`). Akun founder ini SUDAH mencapai fase `"analysis"` di sesi SEBELUM bagian 33 di-deploy — draft `chapterAnalysis` yang tersimpan ditulis oleh kode server LAMA yang belum punya `insightRows`/`pattern` sama sekali di bentuk responsnya. `restoreOnboardingDraft()` mempercayai `draft.chapterAnalysis` tanpa validasi bentuk apa pun, langsung `adaptivePhase = draft.adaptivePhase` untuk `"analysis"` — beda dari cabang `"card"` di sebelahnya yang sudah punya fallback sendiri (`beginScenarioBridge()`) untuk kasus "belum/tidak ada yang bisa ditampilkan". Ini kelas masalah yang sama seperti sebelumnya (komentar `LEGACY_STAT_LABELS`, bagian 28's stale-shape backstop) — bukan migrasi data (app ini tidak pernah migrasi data onboarding lama-bentuk), cuma jangan percaya blob tersimpan yang bentuknya tidak lengkap.

**Fix (`public/app.js`, `restoreOnboardingDraft()`)**: validasi bentuk `chapterAnalysis` khusus untuk fase `"analysis"` saja (fase `"pathway"`/`"goals"` tidak pernah menampilkan `insightRows`/`pattern`, jadi draft lama-bentuk di sana tidak berbahaya) — kalau `chapterAnalysis` tidak punya `insightRows` (array 2) DAN `pattern.title`, panggil `beginPathwayBridge()` (fetch ulang, pakai kartu-kartu yang sudah terkumpul, tampil bridge Pathway singkat sambil menunggu) alih-alih mempercayai blob basi. Diverifikasi PERSIS dengan reproduksi bug nyata: kartu Playwright signup → radar → 2 kartu skenario → capai layar summary → langsung UPDATE kolom `onboarding_draft` di Postgres via query SQL, hapus `insightRows`/`pattern` dari `chapterAnalysis` yang tersimpan (meniru persis draft era-lama) → reload halaman → dikonfirmasi kedua seksi otomatis ter-refetch dan terisi.

**Polish visual** (screenshot HP asli ~390px, 5 permintaan founder):
1. Headline "Ada pola yang mulai kelihatan." dari flat 27px (patah 2 baris) → `min(23px, calc(23/390*100vw))` mengikuti konvensi width-scaled yang sudah dipakai `.name-title`/`.qcard-question`, margin-top 14→6px (naik posisinya).
2. Subcopy 12.5→`min(10.5px,...)`, margin-bottom headline 8→4px (merapatkan jarak ke subcopy) — satu perubahan menjawab dua permintaan sekaligus karena jarak itu murni dikontrol margin-bottom headline.
3. Radar disamakan ukurannya persis dengan radar interaktif (`.poly-svg`) — reuse formula `clamp(220px, calc(var(--vh,1dvh)*52), min(460px,96vw))` yang sama persis (`--rd-svg-max` di layar radar, `--ch-radar-max` baru di layar ini), plus margin horizontal `-12px` pada `.chapter-radar-wrap` untuk mengimbangi padding `.shell-chapter` yang lebih lebar (20px) dari `.shell-radar` (8px) — tanpa kompensasi ini ukuran akhir masih beda ~24px meski formula cap-nya identik, karena keduanya dibatasi oleh lebar KONTAINER yang beda, bukan cuma oleh cap-nya. Diverifikasi presisi lewat `getBoundingClientRect()` kedua radar — 0px selisih.
4. Titik "Setelah Kalibrasi" (gold) sekarang menampilkan angka nilainya (reuse pola `poly-value` dari radar interaktif + `formatRadarValue()`), garis "Sebelum Kalibrasi" (dashed) sengaja TIDAK diberi angka — cuma yang diminta founder.

**Bug CSS tak terduga ketemu saat verifikasi** (Chromium-specific, bukan salah rumus): mengganti `.chapter-radar-wrap` dari `width:82%` polos ke `display:flex; justify-content:center;` (meniru `.poly-wrap`) ternyata memicu Chromium meng-hitung `width:100%` pada elemen `<svg>` (viewBox 380×292, tanpa atribut width/height eksplisit) jadi jauh lebih kecil dari kontainer sungguhan (188px, bukan 330px) — reproduksi lewat isolasi manual (`display:block` langsung memperbaikinya). Fix: `.chapter-radar-wrap` balik ke block-layout polos (bukan flex), `.chapter-radar-svg` dapat `display:block; margin:0 auto` untuk tetap center. Dicatat di komentar kode supaya tidak terulang di layar lain yang mau reuse pola serupa.

**Diverifikasi**: reproduksi bug stale-draft PERSIS lewat mutasi SQL langsung terhadap `onboarding_draft` (bukan simulasi/tebakan) — dikonfirmasi ter-refetch dan terisi. Ukuran radar dikonfirmasi 0px selisih dari radar interaktif via `getBoundingClientRect()` dua layar berbeda. Headline dikonfirmasi 1 baris (`boundingBox().height` < ambang 2-baris). 7 label nilai muncul di titik "Setelah Kalibrasi", 0 di titik "Sebelum Kalibrasi" (sesuai permintaan). `tests/onboarding-chapter-analysis.e2e.js`, `onboarding-bridge.e2e.js`, `onboarding-bridge-audio.e2e.js`, `onboarding-draft-resume.e2e.js` dijalankan ulang (menyentuh `restoreOnboardingDraft()` yang sama) — semua lolos. Full regression suite dijalankan ulang — dua kegagalan awal (`onboarding-bridge.e2e.js`'s dev-preview-production test, `onboarding-name.e2e.js`) sama-sama terbukti flaky resource-contention dari banyak server+browser paralel sekaligus (pola yang sudah terlihat di bagian 33), lolos bersih saat dijalankan sendiri satu-satu, bukan regresi nyata.

## 35. Chapter Analysis: rescale radar geometri sungguhan + fix layout kartu trait + fix drag horizontal (feedback founder round ke-3 terhadap layar yang sama) (SELESAI, satu item butuh verifikasi HP asli)

**Konteks**: setelah bagian 34 deploy, founder masih melihat radar lebih kecil dari radar interaktif, kartu "Pola yang terlihat" ikonnya di atas teks (bukan di kiri), dan scroll layar ini terasa "ketarik" ke kiri-kanan. Root cause radar: bagian 34 hanya menyamakan LEBAR LUAR kedua SVG (0px selisih, terverifikasi) — tapi kedua viewBox punya rasio aspek beda (heptagon radar interaktif mengisi 68.6% lebar viewBox-nya, punya Chapter Analysis cuma 52.6%), jadi lebar SVG sama ≠ heptagon sama besar. Perlu rescale geometri sungguhan, bukan cuma lebar. Root cause drag horizontal: bleed `-12px` margin (bagian 34, buat nyamain lebar kontainer) bikin konten di dalam `.fadeUp` (region scroll, `overflow-y:auto`, TANPA `overflow-x` eksplisit) jadi lebih lebar dari box-nya sendiri — memicu Chromium/Safari mendaftarkan region ini sebagai bisa di-pan horizontal via touch.

**`public/app.js` — rescale geometri (`CH_MAXR`/`CH_LABELR`/`CH_VBH`/`CH_CY` + radius dot)**: `CH_MAXR` naik dari 100 ke 130.3 (`144 × 380/420`, biar heptagon mengisi fraksi lebar viewBox yang SAMA seperti radar interaktif). `CH_LABELR` awalnya dihitung 138.3 (`CH_MAXR_baru + gap 8`, gap = 1/3 dari gap lama 24 sesuai permintaan founder), lalu diturunkan lagi ke 135.3 setelah verifikasi visual (lihat di bawah). `CH_VBH`/`CH_CY` naik dari 292/150 ke 304/152 supaya label 2-baris "Emotional Stability" tidak terpotong atas/bawah akibat heptagon yang lebih besar. Radius dot "Setelah Kalibrasi" naik dari 3 ke 12.2 (`13.5 × 380/420`, sama persis pixel dengan `.poly-dot` radar interaktif), dot "Sebelum Kalibrasi" naik dari 2.6 ke 10.6 (bukan diminta eksplisit, tapi rasio ukuran ke dot after dipertahankan — dibiarkan di 2.6 akan terlihat pincang di sebelah dot 12.2). Font angka nilai naik 8→8.6px (`.chapter-radar-after-value`, sama rasio). Diverifikasi presisi: selisih ukuran heptagon sungguhan (bukan cuma lebar SVG) antara dua layar = **0.05px** via `getBBox()` pada `.poly-outer`/`.chapter-radar-ring.outer`.

**Bug klipping label ditemukan saat verifikasi, ditelusuri lewat 3 metode pengukuran independen**: setelah `CH_LABELR` naik, label "Livelihood" (sumbu dengan sudut paling horizontal, `dx≈0.975`) risiko terpotong di tepi kanan viewBox. `overflow:visible` pada `.chapter-radar-svg` (mitigasi awal) TERNYATA tidak reliable mencegah klip di screenshot Chromium headless — dikonfirmasi lewat 3 pengukuran independen yang semua sepakat margin SEHAT (SVG `getBBox()`, `getBoundingClientRect()`, `canvas.measureText()` — semua bilang ~15-18px aman) padahal screenshot berulang kali menunjukkan huruf terakhir terpotong. Kesimpulan: screenshot headless Chromium untuk kasus SVG `overflow:visible` bersarang dalam container ber-scale ini tidak sepenuhnya bisa dipercaya sebagai bukti visual — DOM measurement lebih dipercaya. Tetap diterapkan perbaikan defensif nyata (bukan cuma andalkan overflow:visible): font `.chapter-radar-label` 12→10.5px dan `CH_LABELR` diturunkan 138.3→135.3 — margin real-px "Livelihood" ke tepi viewBox naik dari ~1.2px (nyaris nol) ke ~10px setelah kedua perbaikan ini, dikonfirmasi ulang lewat pengukuran (bukan cuma screenshot).

**`public/app.js`/`public/styles.css` — kartu "Pola yang terlihat" jadi baris (icon kiri, rata tengah)**: markup dibungkus ulang (`<div class="chapter-trait-text">` baru membungkus title+desc), `.chapter-trait-card` dapat `display:flex;align-items:center;gap:14px` (sebelumnya cuma block polos, icon-di-atas-teks — ternyata oversight dari bagian 33, handoff aslinya memang minta flex row, tidak pernah diterapkan). Sama persis pola `.chapter-insight-row` yang sudah ada di layar ini.

**`public/styles.css` — fix drag horizontal**: BUKAN `overflow-x:hidden` pada `.fadeUp` (percobaan pertama, langsung ketahuan menimbulkan REGRESI NYATA — memotong visual label "Livelihood" yang sengaja bleed keluar box `.fadeUp`, ketahuan lewat screenshot sebelum di-commit, langsung di-revert). Fix yang benar: `touch-action: pan-y;` pada `.shell-chapter .fadeUp` — properti khusus penanganan gesture (bukan clipping visual), jadi tidak menyentuh rendering sama sekali, cuma memberi tahu browser "cuma boleh pan vertikal di sini".

**Diverifikasi**: heptagon match presisi 0.05px (lihat di atas). Layout kartu trait dikonfirmasi icon di kiri + rata tengah vertikal lewat `getBoundingClientRect()` (pusat icon dan pusat teks selisih <5px). Full regression suite (semua `tests/*.e2e.js` + `practicetest.js` + `recoveryregression.js`) — dua kegagalan (`onboarding-bridge.e2e.js` dev-preview-production, `onboarding-name.e2e.js`) lagi-lagi terbukti flaky resource-contention paralel (pola berulang sejak bagian 33-34), lolos bersih sendiri-sendiri.

**Catatan jujur (tidak bisa diverifikasi penuh dari sandbox ini)**: fix `touch-action:pan-y` untuk masalah "ketarik kiri-kanan" tidak bisa diuji langsung tanpa perangkat sentuh sungguhan (sama seperti keterbatasan verifikasi fix speechSynthesis iOS di bagian 27) — logikanya benar dan merupakan fix standar untuk kelas masalah ini, tapi perlu dikonfirmasi founder di HP asli. Demikian juga klip label "Livelihood" — 3 metode pengukuran DOM independen semua bilang aman, tapi karena screenshot headless terbukti tidak reliable untuk kasus ini, minta founder cek khusus label ini di HP asli setelah deploy.
